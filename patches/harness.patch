diff --git a/apps/cli/package.json b/apps/cli/package.json
index 8916642cfe..9742929bbc 100644
--- a/apps/cli/package.json
+++ b/apps/cli/package.json
@@ -105,7 +105,20 @@
     "@deepseek-ai/dsh-http-proxy": "workspace:^",
     "@deepseek-ai/dsh-mcp-resources": "workspace:^",
     "@deepseek-ai/dsh-plugin-manager": "workspace:^",
-    "@deepseek-ai/dsh-atomic-write": "workspace:^"
+    "@deepseek-ai/dsh-atomic-write": "workspace:^",
+    "@deepseek-ai/dsh-experimental-network-usage": "workspace:^",
+    "@deepseek-ai/dsh-experimental-usage-billing": "workspace:^",
+    "@deepseek-ai/dsh-experimental-workspace-download": "workspace:^",
+    "@deepseek-ai/dsh-experimental-workspace-management": "workspace:^",
+    "@deepseek-ai/dsh-experimental-usage-dashboard": "workspace:^",
+    "@deepseek-ai/dsh-experimental-workspace-editor": "workspace:^",
+    "@deepseek-ai/dsh-experimental-task-board": "workspace:^",
+    "@deepseek-ai/dsh-experimental-interactive-results": "workspace:^",
+    "@deepseek-ai/dsh-experimental-mobile-layout": "workspace:^",
+    "@deepseek-ai/dsh-experimental-workspace-pages": "workspace:^",
+    "@deepseek-ai/dsh-experimental-glass-skin": "workspace:^",
+    "@deepseek-ai/dsh-experimental-page-login": "workspace:^",
+    "@deepseek-ai/dsh-experimental-personal-instructions": "workspace:^"
   },
   "devDependencies": {
     "@agentclientprotocol/sdk": "1.4.0",
diff --git a/docs/capability-seams.i18n.yaml b/docs/capability-seams.i18n.yaml
index 8c28ee4357..ec295876b7 100644
--- a/docs/capability-seams.i18n.yaml
+++ b/docs/capability-seams.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/capability-seams.md
-capability-seams.md: edb8cd688768ec49ee3699c3775409a66975129d
-capability-seams.zh.md: b06d9378616669407b79ffa1da205924ff6ad757
+capability-seams.md: 12951d12bda5363e0367e07247b52654565093ab
+capability-seams.zh.md: 2b3c1e4166e1a942c2250a6b7272eba753abceac
diff --git a/docs/capability-seams.md b/docs/capability-seams.md
index edb8cd6887..12951d12bd 100644
--- a/docs/capability-seams.md
+++ b/docs/capability-seams.md
@@ -7,6 +7,9 @@ A service can be a core spine service, a swappable capability seam, or a bundle/
 
 ```mermaid
 flowchart LR
+  pkg_network_usage["network-usage"]
+  svc_networkUsage["ctx.networkUsage<br/>Optional traffic ledger"]
+  svc_networkProxy["ctx.networkProxy<br/>Metered network proxy"]
   pkg_hmr["hmr"]
   svc_hmr["ctx.hmr<br/>Serialized module and configuration reloads"]
   pkg_app_boot["app-boot"]
@@ -321,6 +324,8 @@ flowchart LR
   pkg_mcp_client --> svc_mcpResources
   pkg_mcp_resources --> svc_mcpResources
   pkg_message_feedback --> svc_messageFeedback
+  pkg_network_usage --> svc_networkProxy
+  pkg_network_usage --> svc_networkUsage
   pkg_office_to_pdf --> svc_officeToPdf
   pkg_permission_presets --> svc_permissionPresets
   pkg_plan_mode --> svc_planMode
@@ -537,6 +542,8 @@ flowchart LR
 
 | ctx key | Role | Owner | Implementations | Direct consumers | Companion plugins | Note |
 | --- | --- | --- | --- | --- | --- | --- |
+| `ctx.networkUsage` | `core` | `network-usage` | - | - | - | Owns retained per-Session traffic totals, rolling-window pacing, and explicit daily spending consent. |
+| `ctx.networkProxy` | `core` | `network-usage` | - | - | - | Owns authenticated Host proxy pools and Session-specific Unix sockets for network-confined shell commands. |
 | `ctx.hmr` | `core` | [`hmr`](../packages/boot/hmr) | - | [`app-boot`](../packages/boot/app-boot) | - | Owns module and exact configuration watchers; application mutations share its queue and automatic reloads await the application file lock. |
 | `ctx.pluginManager` | `core` | [`plugin-manager`](../packages/boot/plugin-manager) | - | [`plugin-manager`](../packages/boot/plugin-manager), `ui-settings-plugin-inventory` | - | Shares profile package operations with the CLI and reports persisted and running state to Web and agent callers. |
 | `ctx.profileContext` | `core` | [`app-boot`](../packages/boot/app-boot) | - | [`plugin-manager`](../packages/boot/plugin-manager) | - | The dsh launcher supplies data-only profile locations and composition inputs; reload scheduling belongs to dsh-hmr. |
diff --git a/docs/capability-seams.zh.md b/docs/capability-seams.zh.md
index b06d937861..2b3c1e4166 100644
--- a/docs/capability-seams.zh.md
+++ b/docs/capability-seams.zh.md
@@ -9,6 +9,9 @@
 
 ```mermaid
 flowchart LR
+  pkg_network_usage["network-usage"]
+  svc_networkUsage["ctx.networkUsage<br/>Optional traffic ledger"]
+  svc_networkProxy["ctx.networkProxy<br/>Metered network proxy"]
   pkg_hmr["hmr"]
   svc_hmr["ctx.hmr<br/>Serialized module and configuration reloads"]
   pkg_app_boot["app-boot"]
@@ -323,6 +326,8 @@ flowchart LR
   pkg_mcp_client --> svc_mcpResources
   pkg_mcp_resources --> svc_mcpResources
   pkg_message_feedback --> svc_messageFeedback
+  pkg_network_usage --> svc_networkProxy
+  pkg_network_usage --> svc_networkUsage
   pkg_office_to_pdf --> svc_officeToPdf
   pkg_permission_presets --> svc_permissionPresets
   pkg_plan_mode --> svc_planMode
@@ -539,6 +544,8 @@ flowchart LR
 
 | ctx 键 | 角色 | 所属包 | 实现 | 直接消费方 | 配套插件 | 说明 |
 | --- | --- | --- | --- | --- | --- | --- |
+| `ctx.networkUsage` | `core` | `network-usage` | - | - | - | 保存按对话累计的历史流量，执行滚动窗口限速和每日费用显式确认。 |
+| `ctx.networkProxy` | `core` | `network-usage` | - | - | - | 管理已认证的 Host 代理连接池，以及受网络沙盒限制的 shell 命令所用的对话专属 Unix 套接字。 |
 | `ctx.hmr` | `core` | [`hmr`](../packages/boot/hmr) | - | [`app-boot`](../packages/boot/app-boot) | - | 负责模块和精确配置监听；应用修改共用其队列，自动重载等待应用文件锁。 |
 | `ctx.pluginManager` | `core` | [`plugin-manager`](../packages/boot/plugin-manager) | - | [`plugin-manager`](../packages/boot/plugin-manager), `ui-settings-plugin-inventory` | - | 与 CLI 共享 profile 包操作，并向 Web 和 Agent 调用方分别报告持久状态与运行状态。 |
 | `ctx.profileContext` | `core` | [`app-boot`](../packages/boot/app-boot) | - | [`plugin-manager`](../packages/boot/plugin-manager) | - | dsh launcher 提供纯数据形式的 profile 位置与组合输入；重载调度由 dsh-hmr 负责。 |
diff --git a/docs/config-catalog.i18n.yaml b/docs/config-catalog.i18n.yaml
index 7da9acd525..74cfbc01f9 100644
--- a/docs/config-catalog.i18n.yaml
+++ b/docs/config-catalog.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/config-catalog.md
-config-catalog.md: 631ce6a34b120f44534a7935ff976662287fa879
-config-catalog.zh.md: 8d72786a79ceff85e35aff72fa6622af7373a3b4
+config-catalog.md: 15f3cb719617cadb959bbe4ad8e299b8b771c994
+config-catalog.zh.md: 6f33f8f213a8c25576b05ad4b19adf8d9da8e3b6
diff --git a/docs/config-catalog.md b/docs/config-catalog.md
index 631ce6a34b..15f3cb7196 100644
--- a/docs/config-catalog.md
+++ b/docs/config-catalog.md
@@ -203,17 +203,19 @@ Source: [`packages/api/gateway/src/index.ts:119`](../packages/api/gateway/src/in
 
 ## `@deepseek-ai/dsh-api-session-controller`
 
-Requires: `agentDefaultModel` · `agents` · `attachments` · `fileUploads` · `llm` · `sessions` · `sessionProjections` · `sessionQuery` · `typert` · `workspaceRegistry`
+Requires: `agentDefaultModel` · `agents` · `attachments` · `fileUploads` · `llm` · `sessions` · `sessionPersistence` · `sessionProjections` · `sessionQuery` · `typert` · `workspaceRegistry`
 
 ```ts config-catalog
 /** Session Controller deployment policy. */
 export interface Config {
   /** Override platform desktop-opener detection. */
   readonly nativeOpen?: boolean
+  /** Workspace directory for new Sessions that select no workspace; defaults to the invocation cwd. */
+  readonly defaultCwd?: string
 }
 ```
 
-Source: [`packages/api/session-controller/src/index.ts:71`](../packages/api/session-controller/src/index.ts)
+Source: [`packages/api/session-controller/src/index.ts:72`](../packages/api/session-controller/src/index.ts)
 
 <a id="deepseek-aidsh-api-settings-controller"></a>
 
@@ -428,7 +430,7 @@ export interface ConnectionRecoveryConfig {
 }
 ```
 
-Source: [`packages/client/connection/src/index.ts:87`](../packages/client/connection/src/index.ts)
+Source: [`packages/client/connection/src/index.ts:99`](../packages/client/connection/src/index.ts)
 
 <a id="deepseek-aidsh-client-hmr"></a>
 
@@ -759,6 +761,38 @@ export interface InspectorOptions {
 
 Source: [`packages/experimental/inspector/src/index.ts:66`](../packages/experimental/inspector/src/index.ts)
 
+<a id="deepseek-aidsh-experimental-network-usage"></a>
+
+## `@deepseek-ai/dsh-experimental-network-usage`
+
+Requires: `storageDomain` · `connection` · `agents`
+
+```ts config-catalog
+/** Explicit deployment traffic policy and checkpoint frequency. */
+export interface Config extends TrafficPolicy {
+  /** Maximum normal interval between persisted traffic checkpoints, in milliseconds. */
+  checkpointMs: number
+  /** Browser dashboard refresh interval in milliseconds. */
+  refreshMs: number
+}
+
+/** Deployment policy; money is CNY and bandwidth is decimal Mbps. */
+export interface TrafficPolicy {
+  /** CNY charged per decimal GB of outgoing traffic. */
+  cnyPerGB: number
+  /** Rolling observation window in milliseconds. */
+  windowMs: number
+  /** Outgoing cost strictly above this amount activates throttling. */
+  windowCny: number
+  /** Shared outgoing rate during throttling, in decimal Mbps. */
+  throttledMbps: number
+  /** Additional CNY requiring confirmation within each Beijing calendar day. */
+  dailyStepCny: number
+}
+```
+
+Source: [`packages/experimental/network-usage/src/index.ts:32`](../packages/experimental/network-usage/src/index.ts)
+
 <a id="deepseek-aidsh-experimental-ptc-runtime-python"></a>
 
 ## `@deepseek-ai/dsh-experimental-ptc-runtime-python`
@@ -843,6 +877,81 @@ export interface Config {
 
 Source: [`packages/experimental/tool-agent-team/src/index.ts:17`](../packages/experimental/tool-agent-team/src/index.ts)
 
+<a id="deepseek-aidsh-experimental-usage-billing"></a>
+
+## `@deepseek-ai/dsh-experimental-usage-billing`
+
+Requires: `storageDomain` · `sessionPersistence` · `sessionController` · `sessions` · `credentials` · `connection`
+
+```ts config-catalog
+/** Deployment prices and bounded balance refresh settings. */
+export interface Config {
+  /** Price versions ordered by strictly increasing effective time. */
+  generations: PriceGeneration[]
+  /** Credential reference used for balance requests. */
+  apiKeyEnv: string
+  /** HTTPS endpoint returning the provider account balance. */
+  balanceUrl: string
+  /** Minimum interval between balance refreshes, in milliseconds. */
+  balanceCacheMs: number
+  /** Balance request timeout in milliseconds. */
+  requestTimeoutMs: number
+  /** Browser refresh interval in milliseconds. */
+  refreshMs: number
+}
+
+/** One dated price generation, selected by request start time. */
+export interface PriceGeneration extends PricingCalendar {
+  /** Adapter identifier whose reported usage uses these prices. */
+  provider: string
+  /** Inclusive effective time as Unix milliseconds. */
+  effectiveAt: number
+  /** Beijing holiday dates excluded from peak pricing, formatted YYYY-MM-DD. */
+  holidays: string[]
+  /** Model identifiers mapped to prices for both billing periods. */
+  models: Record<string, {
+    /** Weekday peak-period prices. */
+    peak: TokenPrices
+    /** Prices outside peak hours and on holidays. */
+    offPeak: TokenPrices
+  }>
+}
+
+/** Explicit pricing calendar; dates use Asia/Shanghai YYYY-MM-DD. */
+export interface PricingCalendar {
+  /** Beijing holiday dates excluded from peak pricing, formatted YYYY-MM-DD. */
+  holidays: readonly string[]
+}
+
+/** CNY prices for one million tokens; input excludes cache hits. */
+export interface TokenPrices {
+  /** CNY per million uncached input tokens, including cache writes. */
+  input: number
+  /** CNY per million cached input tokens. */
+  cacheRead: number
+  /** CNY per million generated output tokens. */
+  output: number
+}
+```
+
+Source: [`packages/experimental/usage-billing/src/index.ts:20`](../packages/experimental/usage-billing/src/index.ts)
+
+<a id="deepseek-aidsh-experimental-workspace-management"></a>
+
+## `@deepseek-ai/dsh-experimental-workspace-management`
+
+Requires: `connection` · `sessionController` · `workspaceRegistry` · `sessionPersistence`
+
+```ts config-catalog
+/** Explicit root whose child workspace directories can be permanently removed. */
+export interface Config {
+  /** Existing absolute directory whose child workspaces may be deleted from disk. */
+  managedWorkspaceRoot: string
+}
+```
+
+Source: [`packages/experimental/workspace-management/src/index.ts:14`](../packages/experimental/workspace-management/src/index.ts)
+
 <a id="deepseek-aidsh-file-reference-local"></a>
 
 ## `@deepseek-ai/dsh-file-reference-local`
@@ -3835,6 +3944,7 @@ These load from a `cordis.yml` entry with no `config:` block; they declare no co
 - `@deepseek-ai/dsh-experimental-auto-review` — requires `llm` · `permissionPresets` · `sessions` · `tools` ([`packages/experimental/auto-review/src/index.ts`](../packages/experimental/auto-review/src/index.ts))
 - `@deepseek-ai/dsh-experimental-client-ui-agent-team` ([`packages/experimental/client-ui-agent-team/src/index.ts`](../packages/experimental/client-ui-agent-team/src/index.ts))
 - `@deepseek-ai/dsh-experimental-computer-use-cua-driver-native` — requires `computerUse` · `tools` · `systemPrompt` ([`packages/experimental/computer-use-cua-driver-native/src/index.ts`](../packages/experimental/computer-use-cua-driver-native/src/index.ts))
+- `@deepseek-ai/dsh-experimental-workspace-download` — requires `connection` · `workspaceFiles` · `sessions` · `sessionPersistence` ([`packages/experimental/workspace-download/src/index.ts`](../packages/experimental/workspace-download/src/index.ts))
 - `@deepseek-ai/dsh-fs-observation-policy` ([`packages/fs/fs-observation-policy/src/index.ts`](../packages/fs/fs-observation-policy/src/index.ts))
 - `@deepseek-ai/dsh-fs-ssh` — requires `ssh` · `sandboxPolicy` ([`packages/ssh/fs-ssh/src/index.ts`](../packages/ssh/fs-ssh/src/index.ts))
 - `@deepseek-ai/dsh-goal-round-driver` — requires `agents` · `goals` · `sessions` ([`packages/goal/goal-round-driver/src/index.ts`](../packages/goal/goal-round-driver/src/index.ts))
diff --git a/docs/config-catalog.zh.md b/docs/config-catalog.zh.md
index 8d72786a79..6f33f8f213 100644
--- a/docs/config-catalog.zh.md
+++ b/docs/config-catalog.zh.md
@@ -205,17 +205,19 @@ export interface Config {
 
 ## `@deepseek-ai/dsh-api-session-controller`
 
-需要：`agentDefaultModel` · `agents` · `attachments` · `fileUploads` · `llm` · `sessions` · `sessionProjections` · `sessionQuery` · `typert` · `workspaceRegistry`
+需要：`agentDefaultModel` · `agents` · `attachments` · `fileUploads` · `llm` · `sessions` · `sessionPersistence` · `sessionProjections` · `sessionQuery` · `typert` · `workspaceRegistry`
 
 ```ts config-catalog
 /** Session Controller deployment policy. */
 export interface Config {
   /** Override platform desktop-opener detection. */
   readonly nativeOpen?: boolean
+  /** Workspace directory for new Sessions that select no workspace; defaults to the invocation cwd. */
+  readonly defaultCwd?: string
 }
 ```
 
-来源：[`packages/api/session-controller/src/index.ts:71`](../packages/api/session-controller/src/index.ts)
+来源：[`packages/api/session-controller/src/index.ts:72`](../packages/api/session-controller/src/index.ts)
 
 <a id="deepseek-aidsh-api-settings-controller"></a>
 
@@ -430,7 +432,7 @@ export interface ConnectionRecoveryConfig {
 }
 ```
 
-来源： [`packages/client/connection/src/index.ts:87`](../packages/client/connection/src/index.ts)
+来源： [`packages/client/connection/src/index.ts:99`](../packages/client/connection/src/index.ts)
 
 <a id="deepseek-aidsh-client-hmr"></a>
 
@@ -761,6 +763,38 @@ export interface InspectorOptions {
 
 来源：[`packages/experimental/inspector/src/index.ts:66`](../packages/experimental/inspector/src/index.ts)
 
+<a id="deepseek-aidsh-experimental-network-usage"></a>
+
+## `@deepseek-ai/dsh-experimental-network-usage`
+
+需要：`storageDomain` · `connection` · `agents`
+
+```ts config-catalog
+/** Explicit deployment traffic policy and checkpoint frequency. */
+export interface Config extends TrafficPolicy {
+  /** Maximum normal interval between persisted traffic checkpoints, in milliseconds. */
+  checkpointMs: number
+  /** Browser dashboard refresh interval in milliseconds. */
+  refreshMs: number
+}
+
+/** Deployment policy; money is CNY and bandwidth is decimal Mbps. */
+export interface TrafficPolicy {
+  /** CNY charged per decimal GB of outgoing traffic. */
+  cnyPerGB: number
+  /** Rolling observation window in milliseconds. */
+  windowMs: number
+  /** Outgoing cost strictly above this amount activates throttling. */
+  windowCny: number
+  /** Shared outgoing rate during throttling, in decimal Mbps. */
+  throttledMbps: number
+  /** Additional CNY requiring confirmation within each Beijing calendar day. */
+  dailyStepCny: number
+}
+```
+
+来源：[`packages/experimental/network-usage/src/index.ts:32`](../packages/experimental/network-usage/src/index.ts)
+
 <a id="deepseek-aidsh-experimental-ptc-runtime-python"></a>
 
 ## `@deepseek-ai/dsh-experimental-ptc-runtime-python`
@@ -845,6 +879,81 @@ export interface Config {
 
 来源：[`packages/experimental/tool-agent-team/src/index.ts:17`](../packages/experimental/tool-agent-team/src/index.ts)
 
+<a id="deepseek-aidsh-experimental-usage-billing"></a>
+
+## `@deepseek-ai/dsh-experimental-usage-billing`
+
+需要：`storageDomain` · `sessionPersistence` · `sessionController` · `sessions` · `credentials` · `connection`
+
+```ts config-catalog
+/** Deployment prices and bounded balance refresh settings. */
+export interface Config {
+  /** Price versions ordered by strictly increasing effective time. */
+  generations: PriceGeneration[]
+  /** Credential reference used for balance requests. */
+  apiKeyEnv: string
+  /** HTTPS endpoint returning the provider account balance. */
+  balanceUrl: string
+  /** Minimum interval between balance refreshes, in milliseconds. */
+  balanceCacheMs: number
+  /** Balance request timeout in milliseconds. */
+  requestTimeoutMs: number
+  /** Browser refresh interval in milliseconds. */
+  refreshMs: number
+}
+
+/** One dated price generation, selected by request start time. */
+export interface PriceGeneration extends PricingCalendar {
+  /** Adapter identifier whose reported usage uses these prices. */
+  provider: string
+  /** Inclusive effective time as Unix milliseconds. */
+  effectiveAt: number
+  /** Beijing holiday dates excluded from peak pricing, formatted YYYY-MM-DD. */
+  holidays: string[]
+  /** Model identifiers mapped to prices for both billing periods. */
+  models: Record<string, {
+    /** Weekday peak-period prices. */
+    peak: TokenPrices
+    /** Prices outside peak hours and on holidays. */
+    offPeak: TokenPrices
+  }>
+}
+
+/** Explicit pricing calendar; dates use Asia/Shanghai YYYY-MM-DD. */
+export interface PricingCalendar {
+  /** Beijing holiday dates excluded from peak pricing, formatted YYYY-MM-DD. */
+  holidays: readonly string[]
+}
+
+/** CNY prices for one million tokens; input excludes cache hits. */
+export interface TokenPrices {
+  /** CNY per million uncached input tokens, including cache writes. */
+  input: number
+  /** CNY per million cached input tokens. */
+  cacheRead: number
+  /** CNY per million generated output tokens. */
+  output: number
+}
+```
+
+来源：[`packages/experimental/usage-billing/src/index.ts:20`](../packages/experimental/usage-billing/src/index.ts)
+
+<a id="deepseek-aidsh-experimental-workspace-management"></a>
+
+## `@deepseek-ai/dsh-experimental-workspace-management`
+
+需要：`connection` · `sessionController` · `workspaceRegistry` · `sessionPersistence`
+
+```ts config-catalog
+/** Explicit root whose child workspace directories can be permanently removed. */
+export interface Config {
+  /** Existing absolute directory whose child workspaces may be deleted from disk. */
+  managedWorkspaceRoot: string
+}
+```
+
+来源：[`packages/experimental/workspace-management/src/index.ts:14`](../packages/experimental/workspace-management/src/index.ts)
+
 <a id="deepseek-aidsh-file-reference-local"></a>
 
 ## `@deepseek-ai/dsh-file-reference-local`
@@ -3837,6 +3946,7 @@ export interface Config {
 - `@deepseek-ai/dsh-experimental-auto-review` — 需要 `llm` · `permissionPresets` · `sessions` · `tools`（[`packages/experimental/auto-review/src/index.ts`](../packages/experimental/auto-review/src/index.ts)）
 - `@deepseek-ai/dsh-experimental-client-ui-agent-team`（[`packages/experimental/client-ui-agent-team/src/index.ts`](../packages/experimental/client-ui-agent-team/src/index.ts)）
 - `@deepseek-ai/dsh-experimental-computer-use-cua-driver-native` — requires `computerUse` · `tools` · `systemPrompt` ([`packages/experimental/computer-use-cua-driver-native/src/index.ts`](../packages/experimental/computer-use-cua-driver-native/src/index.ts))
+- `@deepseek-ai/dsh-experimental-workspace-download` — requires `connection` · `workspaceFiles` · `sessions` · `sessionPersistence` ([`packages/experimental/workspace-download/src/index.ts`](../packages/experimental/workspace-download/src/index.ts))
 - `@deepseek-ai/dsh-fs-observation-policy`（[`packages/fs/fs-observation-policy/src/index.ts`](../packages/fs/fs-observation-policy/src/index.ts)）
 - `@deepseek-ai/dsh-fs-ssh` — 需要 `ssh` · `sandboxPolicy`（[`packages/ssh/fs-ssh/src/index.ts`](../packages/ssh/fs-ssh/src/index.ts)）
 - `@deepseek-ai/dsh-goal-round-driver` — 需要 `agents` · `goals` · `sessions`（[`packages/goal/goal-round-driver/src/index.ts`](../packages/goal/goal-round-driver/src/index.ts)）
diff --git a/docs/event-producer-consumer.i18n.yaml b/docs/event-producer-consumer.i18n.yaml
index 8bfe85ac05..f809d65eda 100644
--- a/docs/event-producer-consumer.i18n.yaml
+++ b/docs/event-producer-consumer.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/event-producer-consumer.md
-event-producer-consumer.md: fcf95b4268cc41b84099af351f1d6fdb07c80fc4
-event-producer-consumer.zh.md: bd466a2e39f4a268e4a8b1567ae5a5f22d250977
+event-producer-consumer.md: 1d5353ecccdaf9cbc71b44da0de4cd8be6d8b155
+event-producer-consumer.zh.md: 85861ea17edc23160b684a174213c8165d7f8442
diff --git a/docs/event-producer-consumer.md b/docs/event-producer-consumer.md
index fcf95b4268..1d5353eccc 100644
--- a/docs/event-producer-consumer.md
+++ b/docs/event-producer-consumer.md
@@ -16,21 +16,23 @@ This matrix shows which packages dispatch each harness-owned event and which pac
 | `agent/inbox/claimed` | `emit` | [`packages/core/agent/src/runtime-types.ts:299`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`acp`](../packages/acp/acp), [`goal-round-driver`](../packages/goal/goal-round-driver), [`subagent`](../packages/subagent/subagent), [`tool-jobs`](../packages/jobs/tool-jobs) |
 | `agent/inbox/discarded` | `emit` | [`packages/core/agent/src/runtime-types.ts:307`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`goal-round-driver`](../packages/goal/goal-round-driver), [`subagent`](../packages/subagent/subagent) |
 | `agent/inbox/inserted` | `emit` | [`packages/core/agent/src/runtime-types.ts:288`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`goal-round-driver`](../packages/goal/goal-round-driver) |
-| `agent/pre-step` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:320`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`agent`](../packages/core/agent), [`agent-instructions`](../packages/context/agent-instructions), [`compaction-basic`](../packages/compaction/compaction-basic), [`goal-round-driver`](../packages/goal/goal-round-driver), [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`hooks-codex`](../packages/hooks/hooks-codex), [`plan-mode`](../packages/plan/plan-mode), [`repeat-tool-reminder`](../packages/guard/repeat-tool-reminder), [`session-checkpoint-policy`](../packages/session/session-checkpoint-policy), [`session-reference`](../packages/context/session-reference), [`subagent-in-process-driver`](../packages/subagent/subagent-in-process-driver), [`time-context`](../packages/context/time-context), [`tmux-context`](../packages/context/tmux-context), [`tool-skill`](../packages/skill/tool-skill), [`tool-subagent`](../packages/subagent/tool-subagent) |
+| `agent/pre-step` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:320`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`agent`](../packages/core/agent), [`agent-instructions`](../packages/context/agent-instructions), [`compaction-basic`](../packages/compaction/compaction-basic), [`goal-round-driver`](../packages/goal/goal-round-driver), [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`hooks-codex`](../packages/hooks/hooks-codex), `network-usage`, [`plan-mode`](../packages/plan/plan-mode), [`repeat-tool-reminder`](../packages/guard/repeat-tool-reminder), [`session-checkpoint-policy`](../packages/session/session-checkpoint-policy), [`session-reference`](../packages/context/session-reference), [`subagent-in-process-driver`](../packages/subagent/subagent-in-process-driver), [`time-context`](../packages/context/time-context), [`tmux-context`](../packages/context/tmux-context), [`tool-skill`](../packages/skill/tool-skill), [`tool-subagent`](../packages/subagent/tool-subagent) |
 | `agent/request` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:337`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`agent`](../packages/core/agent), [`webhook`](../packages/webhook/webhook) |
 | `agent/request-error` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:353`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`compaction-basic`](../packages/compaction/compaction-basic), [`compaction-image-offload`](../packages/compaction/compaction-image-offload), [`llm-retry`](../packages/llm/llm-retry) |
 | `agent/status` | `emit` | [`packages/core/agent/src/runtime-types.ts:280`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`agent`](../packages/core/agent), `agent-team`, [`compaction-basic`](../packages/compaction/compaction-basic), [`goal-round-driver`](../packages/goal/goal-round-driver), [`schedule`](../packages/schedule/schedule), `server`, `session-controller` |
 | `agent/turn-stopping` | `serial` | [`packages/core/agent/src/runtime-types.ts:381`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`serial`) | [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`hooks-codex`](../packages/hooks/hooks-codex), [`workspace-changes`](../packages/deliverables/workspace-changes) |
-| `api-session/activity` | `emit` | [`packages/api/session-controller/src/types.ts:587`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/added` | `emit` | [`packages/api/session-controller/src/types.ts:567`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/error` | `emit` | [`packages/api/session-controller/src/types.ts:594`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/removed` | `emit` | [`packages/api/session-controller/src/types.ts:573`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/status` | `emit` | [`packages/api/session-controller/src/types.ts:580`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/activity` | `emit` | [`packages/api/session-controller/src/types.ts:593`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/added` | `emit` | [`packages/api/session-controller/src/types.ts:573`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/before-delete` | `parallel` | [`packages/api/session-controller/src/types.ts:567`](../packages/api/session-controller/src/types.ts) | `session-controller` (`parallel`) | `usage-billing` |
+| `api-session/error` | `emit` | [`packages/api/session-controller/src/types.ts:600`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/removed` | `emit` | [`packages/api/session-controller/src/types.ts:579`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/status` | `emit` | [`packages/api/session-controller/src/types.ts:586`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
 | `approval/request` | `waterfall` | [`packages/interaction/user-approval/src/types.ts:85`](../packages/interaction/user-approval/src/types.ts) | [`user-approval`](../packages/interaction/user-approval) (`waterfall`) | [`acp`](../packages/acp/acp), `remotes` |
 | `authorization/settled` | `emit` | [`packages/credentials/authorization/src/index.ts:57`](../packages/credentials/authorization/src/index.ts) | [`authorization`](../packages/credentials/authorization) (`events.dispatch`) | [`authorization`](../packages/credentials/authorization) |
 | `commands/change` | `emit` | [`packages/interaction/commands/src/types.ts:89`](../packages/interaction/commands/src/types.ts) | [`commands`](../packages/interaction/commands) (`events.dispatch`) | `remotes` |
 | `compaction/summary-error` | `waterfall` | [`packages/compaction/compaction/src/index.ts:99`](../packages/compaction/compaction/src/index.ts) | [`compaction-basic`](../packages/compaction/compaction-basic) (`waterfall`) | [`compaction-image-offload`](../packages/compaction/compaction-image-offload) |
-| `connection/request` | `waterfall` | [`packages/client/connection/src/index.ts:62`](../packages/client/connection/src/index.ts) | `connection` (`waterfall`) | - |
+| `connection/fetch` | `waterfall` | [`packages/client/connection/src/index.ts:74`](../packages/client/connection/src/index.ts) | `connection` (`waterfall`) | `network-usage` |
+| `connection/request` | `waterfall` | [`packages/client/connection/src/index.ts:67`](../packages/client/connection/src/index.ts) | `connection` (`waterfall`) | - |
 | `cordis/dynamic-package` | `emit` | [`packages/extensions/cordis-host-runner/src/types.ts:380`](../packages/extensions/cordis-host-runner/src/types.ts) | [`cordis-host-runner`](../packages/extensions/cordis-host-runner) (`emit`) | `remotes` |
 | `cordis/dynamic-retract` | `emit` | [`packages/extensions/cordis-host-runner/src/types.ts:386`](../packages/extensions/cordis-host-runner/src/types.ts) | [`cordis-host-runner`](../packages/extensions/cordis-host-runner) (`emit`) | `remotes` |
 | `cordis/inspect-query` | `emit` | [`packages/extensions/cordis-host-runner/src/types.ts:392`](../packages/extensions/cordis-host-runner/src/types.ts) | [`cordis-host-runner`](../packages/extensions/cordis-host-runner) (`emit`) | `remotes` |
@@ -58,7 +60,7 @@ This matrix shows which packages dispatch each harness-owned event and which pac
 | `session/created` | `emit` | [`packages/core/session/src/index.ts:50`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`compaction`](../packages/compaction/compaction), [`goal`](../packages/goal/goal), [`hook-protocol`](../packages/hooks/hook-protocol), [`llm-retry`](../packages/llm/llm-retry), [`permission-presets`](../packages/interaction/permission-presets), [`plan-mode`](../packages/plan/plan-mode), [`schedule`](../packages/schedule/schedule), `server`, [`session`](../packages/core/session), `session-controller`, [`session-log-deepseek`](../packages/session/session-log-deepseek), [`session-projection`](../packages/session/session-projection), [`session-projection-cache`](../packages/session/session-projection-cache), [`session-telemetry`](../packages/session/session-telemetry), [`session-title`](../packages/session/session-title), [`time-context`](../packages/context/time-context), [`tool-todo`](../packages/todo/tool-todo), [`tool-workflow`](../packages/workflow/tool-workflow), [`tools`](../packages/core/tools), [`user-approval`](../packages/interaction/user-approval) |
 | `session/disposed` | `emit` | [`packages/core/session/src/index.ts:60`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`agent-loop`](../packages/core/agent-loop), `agent-team`, `file-upload`, `session-controller`, [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-projection-cache`](../packages/session/session-projection-cache), [`session-telemetry`](../packages/session/session-telemetry), [`session-title`](../packages/session/session-title), [`workspace-changes`](../packages/deliverables/workspace-changes) |
 | `session/event` | `emit` | [`packages/core/session/src/index.ts:72`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`acp`](../packages/acp/acp), [`agent-instructions`](../packages/context/agent-instructions), [`agent-loop`](../packages/core/agent-loop), [`agent-presets`](../packages/preset/agent-presets), `agent-team`, [`compaction`](../packages/compaction/compaction), [`compaction-basic`](../packages/compaction/compaction-basic), [`file-reference-local`](../packages/context/file-reference-local), `file-upload`, [`goal`](../packages/goal/goal), [`goal-round-driver`](../packages/goal/goal-round-driver), [`headless`](../packages/bundle/headless), [`hook-protocol`](../packages/hooks/hook-protocol), [`loader-smoke`](../packages/test-support/loader-smoke), `server`, [`session`](../packages/core/session), `session-controller`, [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-projection`](../packages/session/session-projection), [`session-projection-cache`](../packages/session/session-projection-cache), [`session-telemetry`](../packages/session/session-telemetry), [`session-telemetry-otel`](../packages/session/session-telemetry-otel), [`session-title`](../packages/session/session-title), [`token-meter`](../packages/llm/token-meter), [`tool-todo`](../packages/todo/tool-todo), [`tool-workflow`](../packages/workflow/tool-workflow), [`tools`](../packages/core/tools), [`user-approval`](../packages/interaction/user-approval), [`workspace-changes`](../packages/deliverables/workspace-changes) |
-| `session/flush` | `parallel` | [`packages/core/session/src/index.ts:81`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-telemetry`](../packages/session/session-telemetry) |
+| `session/flush` | `parallel` | [`packages/core/session/src/index.ts:81`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-telemetry`](../packages/session/session-telemetry), `usage-billing` |
 | `settings/document-updated` | `emit` | [`packages/settings/settings/src/types.ts:105`](../packages/settings/settings/src/types.ts) | [`settings`](../packages/settings/settings) (`events.dispatch`) | `remotes` |
 | `settings/updated` | `emit` | [`packages/settings/settings/src/types.ts:92`](../packages/settings/settings/src/types.ts) | [`settings`](../packages/settings/settings) (`events.dispatch`) | [`settings`](../packages/settings/settings) |
 | `skills/change` | `emit` | [`packages/skill/skill/src/index.ts:296`](../packages/skill/skill/src/index.ts) | [`skill`](../packages/skill/skill) (`events.dispatch`) | - |
diff --git a/docs/event-producer-consumer.zh.md b/docs/event-producer-consumer.zh.md
index bd466a2e39..85861ea17e 100644
--- a/docs/event-producer-consumer.zh.md
+++ b/docs/event-producer-consumer.zh.md
@@ -18,21 +18,23 @@
 | `agent/inbox/claimed` | `emit` | [`packages/core/agent/src/runtime-types.ts:299`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`acp`](../packages/acp/acp), [`goal-round-driver`](../packages/goal/goal-round-driver), [`subagent`](../packages/subagent/subagent), [`tool-jobs`](../packages/jobs/tool-jobs) |
 | `agent/inbox/discarded` | `emit` | [`packages/core/agent/src/runtime-types.ts:307`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`goal-round-driver`](../packages/goal/goal-round-driver), [`subagent`](../packages/subagent/subagent) |
 | `agent/inbox/inserted` | `emit` | [`packages/core/agent/src/runtime-types.ts:288`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`goal-round-driver`](../packages/goal/goal-round-driver) |
-| `agent/pre-step` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:320`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`agent`](../packages/core/agent), [`agent-instructions`](../packages/context/agent-instructions), [`compaction-basic`](../packages/compaction/compaction-basic), [`goal-round-driver`](../packages/goal/goal-round-driver), [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`hooks-codex`](../packages/hooks/hooks-codex), [`plan-mode`](../packages/plan/plan-mode), [`repeat-tool-reminder`](../packages/guard/repeat-tool-reminder), [`session-checkpoint-policy`](../packages/session/session-checkpoint-policy), [`session-reference`](../packages/context/session-reference), [`subagent-in-process-driver`](../packages/subagent/subagent-in-process-driver), [`time-context`](../packages/context/time-context), [`tmux-context`](../packages/context/tmux-context), [`tool-skill`](../packages/skill/tool-skill), [`tool-subagent`](../packages/subagent/tool-subagent) |
+| `agent/pre-step` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:320`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`agent`](../packages/core/agent), [`agent-instructions`](../packages/context/agent-instructions), [`compaction-basic`](../packages/compaction/compaction-basic), [`goal-round-driver`](../packages/goal/goal-round-driver), [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`hooks-codex`](../packages/hooks/hooks-codex), `network-usage`, [`plan-mode`](../packages/plan/plan-mode), [`repeat-tool-reminder`](../packages/guard/repeat-tool-reminder), [`session-checkpoint-policy`](../packages/session/session-checkpoint-policy), [`session-reference`](../packages/context/session-reference), [`subagent-in-process-driver`](../packages/subagent/subagent-in-process-driver), [`time-context`](../packages/context/time-context), [`tmux-context`](../packages/context/tmux-context), [`tool-skill`](../packages/skill/tool-skill), [`tool-subagent`](../packages/subagent/tool-subagent) |
 | `agent/request` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:337`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`agent`](../packages/core/agent), [`webhook`](../packages/webhook/webhook) |
 | `agent/request-error` | `waterfall` | [`packages/core/agent/src/runtime-types.ts:353`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`waterfall`) | [`compaction-basic`](../packages/compaction/compaction-basic), [`compaction-image-offload`](../packages/compaction/compaction-image-offload), [`llm-retry`](../packages/llm/llm-retry) |
 | `agent/status` | `emit` | [`packages/core/agent/src/runtime-types.ts:280`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`emit`) | [`agent`](../packages/core/agent), `agent-team`, [`compaction-basic`](../packages/compaction/compaction-basic), [`goal-round-driver`](../packages/goal/goal-round-driver), [`schedule`](../packages/schedule/schedule), `server`, `session-controller` |
 | `agent/turn-stopping` | `serial` | [`packages/core/agent/src/runtime-types.ts:381`](../packages/core/agent/src/runtime-types.ts) | [`agent-loop`](../packages/core/agent-loop) (`serial`) | [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`hooks-codex`](../packages/hooks/hooks-codex), [`workspace-changes`](../packages/deliverables/workspace-changes) |
-| `api-session/activity` | `emit` | [`packages/api/session-controller/src/types.ts:587`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/added` | `emit` | [`packages/api/session-controller/src/types.ts:567`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/error` | `emit` | [`packages/api/session-controller/src/types.ts:594`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/removed` | `emit` | [`packages/api/session-controller/src/types.ts:573`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
-| `api-session/status` | `emit` | [`packages/api/session-controller/src/types.ts:580`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/activity` | `emit` | [`packages/api/session-controller/src/types.ts:593`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/added` | `emit` | [`packages/api/session-controller/src/types.ts:573`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/before-delete` | `parallel` | [`packages/api/session-controller/src/types.ts:567`](../packages/api/session-controller/src/types.ts) | `session-controller` (`parallel`) | `usage-billing` |
+| `api-session/error` | `emit` | [`packages/api/session-controller/src/types.ts:600`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/removed` | `emit` | [`packages/api/session-controller/src/types.ts:579`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
+| `api-session/status` | `emit` | [`packages/api/session-controller/src/types.ts:586`](../packages/api/session-controller/src/types.ts) | `session-controller` (`emit`) | `remotes` |
 | `approval/request` | `waterfall` | [`packages/interaction/user-approval/src/types.ts:85`](../packages/interaction/user-approval/src/types.ts) | [`user-approval`](../packages/interaction/user-approval) (`waterfall`) | [`acp`](../packages/acp/acp), `remotes` |
 | `authorization/settled` | `emit` | [`packages/credentials/authorization/src/index.ts:57`](../packages/credentials/authorization/src/index.ts) | [`authorization`](../packages/credentials/authorization) (`events.dispatch`) | [`authorization`](../packages/credentials/authorization) |
 | `commands/change` | `emit` | [`packages/interaction/commands/src/types.ts:89`](../packages/interaction/commands/src/types.ts) | [`commands`](../packages/interaction/commands) (`events.dispatch`) | `remotes` |
 | `compaction/summary-error` | `waterfall` | [`packages/compaction/compaction/src/index.ts:99`](../packages/compaction/compaction/src/index.ts) | [`compaction-basic`](../packages/compaction/compaction-basic) (`waterfall`) | [`compaction-image-offload`](../packages/compaction/compaction-image-offload) |
-| `connection/request` | `waterfall` | [`packages/client/connection/src/index.ts:62`](../packages/client/connection/src/index.ts) | `connection` (`waterfall`) | - |
+| `connection/fetch` | `waterfall` | [`packages/client/connection/src/index.ts:74`](../packages/client/connection/src/index.ts) | `connection` (`waterfall`) | `network-usage` |
+| `connection/request` | `waterfall` | [`packages/client/connection/src/index.ts:67`](../packages/client/connection/src/index.ts) | `connection` (`waterfall`) | - |
 | `cordis/dynamic-package` | `emit` | [`packages/extensions/cordis-host-runner/src/types.ts:380`](../packages/extensions/cordis-host-runner/src/types.ts) | [`cordis-host-runner`](../packages/extensions/cordis-host-runner) (`emit`) | `remotes` |
 | `cordis/dynamic-retract` | `emit` | [`packages/extensions/cordis-host-runner/src/types.ts:386`](../packages/extensions/cordis-host-runner/src/types.ts) | [`cordis-host-runner`](../packages/extensions/cordis-host-runner) (`emit`) | `remotes` |
 | `cordis/inspect-query` | `emit` | [`packages/extensions/cordis-host-runner/src/types.ts:392`](../packages/extensions/cordis-host-runner/src/types.ts) | [`cordis-host-runner`](../packages/extensions/cordis-host-runner) (`emit`) | `remotes` |
@@ -60,14 +62,14 @@
 | `session/created` | `emit` | [`packages/core/session/src/index.ts:50`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`compaction`](../packages/compaction/compaction), [`goal`](../packages/goal/goal), [`hook-protocol`](../packages/hooks/hook-protocol), [`llm-retry`](../packages/llm/llm-retry), [`permission-presets`](../packages/interaction/permission-presets), [`plan-mode`](../packages/plan/plan-mode), [`schedule`](../packages/schedule/schedule), `server`, [`session`](../packages/core/session), `session-controller`, [`session-log-deepseek`](../packages/session/session-log-deepseek), [`session-projection`](../packages/session/session-projection), [`session-projection-cache`](../packages/session/session-projection-cache), [`session-telemetry`](../packages/session/session-telemetry), [`session-title`](../packages/session/session-title), [`time-context`](../packages/context/time-context), [`tool-todo`](../packages/todo/tool-todo), [`tool-workflow`](../packages/workflow/tool-workflow), [`tools`](../packages/core/tools), [`user-approval`](../packages/interaction/user-approval) |
 | `session/disposed` | `emit` | [`packages/core/session/src/index.ts:60`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`agent-loop`](../packages/core/agent-loop), `agent-team`, `file-upload`, `session-controller`, [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-projection-cache`](../packages/session/session-projection-cache), [`session-telemetry`](../packages/session/session-telemetry), [`session-title`](../packages/session/session-title), [`workspace-changes`](../packages/deliverables/workspace-changes) |
 | `session/event` | `emit` | [`packages/core/session/src/index.ts:72`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`acp`](../packages/acp/acp), [`agent-instructions`](../packages/context/agent-instructions), [`agent-loop`](../packages/core/agent-loop), [`agent-presets`](../packages/preset/agent-presets), `agent-team`, [`compaction`](../packages/compaction/compaction), [`compaction-basic`](../packages/compaction/compaction-basic), [`file-reference-local`](../packages/context/file-reference-local), `file-upload`, [`goal`](../packages/goal/goal), [`goal-round-driver`](../packages/goal/goal-round-driver), [`headless`](../packages/bundle/headless), [`hook-protocol`](../packages/hooks/hook-protocol), [`loader-smoke`](../packages/test-support/loader-smoke), `server`, [`session`](../packages/core/session), `session-controller`, [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-projection`](../packages/session/session-projection), [`session-projection-cache`](../packages/session/session-projection-cache), [`session-telemetry`](../packages/session/session-telemetry), [`session-telemetry-otel`](../packages/session/session-telemetry-otel), [`session-title`](../packages/session/session-title), [`token-meter`](../packages/llm/token-meter), [`tool-todo`](../packages/todo/tool-todo), [`tool-workflow`](../packages/workflow/tool-workflow), [`tools`](../packages/core/tools), [`user-approval`](../packages/interaction/user-approval), [`workspace-changes`](../packages/deliverables/workspace-changes) |
-| `session/flush` | `parallel` | [`packages/core/session/src/index.ts:81`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-telemetry`](../packages/session/session-telemetry) |
+| `session/flush` | `parallel` | [`packages/core/session/src/index.ts:81`](../packages/core/session/src/index.ts) | [`session`](../packages/core/session) (`events.dispatch`) | [`session-persistence-jsonl`](../packages/session/session-persistence-jsonl), [`session-telemetry`](../packages/session/session-telemetry), `usage-billing` |
 | `settings/document-updated` | `emit` | [`packages/settings/settings/src/types.ts:105`](../packages/settings/settings/src/types.ts) | [`settings`](../packages/settings/settings) (`events.dispatch`) | `remotes` |
 | `settings/updated` | `emit` | [`packages/settings/settings/src/types.ts:92`](../packages/settings/settings/src/types.ts) | [`settings`](../packages/settings/settings) (`events.dispatch`) | [`settings`](../packages/settings/settings) |
 | `skills/change` | `emit` | [`packages/skill/skill/src/index.ts:296`](../packages/skill/skill/src/index.ts) | [`skill`](../packages/skill/skill) (`events.dispatch`) | - |
-| `subagent/end` | `emit` | [`packages/subagent/subagent/src/index.ts:171`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`events.dispatch`) | [`hooks-claude-code`](../packages/hooks/hooks-claude-code), `server`, [`subagent`](../packages/subagent/subagent) |
-| `subagent/provider-added` | `emit` | [`packages/subagent/subagent/src/index.ts:145`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`emit`) | [`subagent`](../packages/subagent/subagent), [`tool-subagent`](../packages/subagent/tool-subagent) |
-| `subagent/provider-removed` | `emit` | [`packages/subagent/subagent/src/index.ts:151`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`events.dispatch`) | [`subagent`](../packages/subagent/subagent), [`tool-subagent`](../packages/subagent/tool-subagent) |
-| `subagent/start` | `emit` | [`packages/subagent/subagent/src/index.ts:162`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`events.dispatch`) | [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`subagent`](../packages/subagent/subagent) |
+| `subagent/end` | `emit` | [`packages/subagent/subagent/src/index.ts:172`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`events.dispatch`) | [`hooks-claude-code`](../packages/hooks/hooks-claude-code), `server`, [`subagent`](../packages/subagent/subagent) |
+| `subagent/provider-added` | `emit` | [`packages/subagent/subagent/src/index.ts:146`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`emit`) | [`subagent`](../packages/subagent/subagent), [`tool-subagent`](../packages/subagent/tool-subagent) |
+| `subagent/provider-removed` | `emit` | [`packages/subagent/subagent/src/index.ts:152`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`events.dispatch`) | [`subagent`](../packages/subagent/subagent), [`tool-subagent`](../packages/subagent/tool-subagent) |
+| `subagent/start` | `emit` | [`packages/subagent/subagent/src/index.ts:163`](../packages/subagent/subagent/src/index.ts) | [`subagent`](../packages/subagent/subagent) (`events.dispatch`) | [`hooks-claude-code`](../packages/hooks/hooks-claude-code), [`subagent`](../packages/subagent/subagent) |
 | `system-prompt/assemble` | `waterfall` | [`packages/core/system-prompt/src/index.ts:31`](../packages/core/system-prompt/src/index.ts) | [`system-prompt`](../packages/core/system-prompt) (`waterfall`) | [`agent`](../packages/core/agent), [`agent-presets`](../packages/preset/agent-presets), `browser-use-runtime`, [`session-reference`](../packages/context/session-reference), [`system-prompt`](../packages/core/system-prompt) |
 | `system-prompt/change` | `emit` | [`packages/core/system-prompt/src/index.ts:37`](../packages/core/system-prompt/src/index.ts) | [`system-prompt`](../packages/core/system-prompt) (`emit`) | - |
 | `tools/change` | `emit` | [`packages/core/tools/src/index.ts:201`](../packages/core/tools/src/index.ts) | [`agent-presets`](../packages/preset/agent-presets) (`emit`), [`tools`](../packages/core/tools) (`emit`) | `browser-use-runtime`, [`tool-subagent`](../packages/subagent/tool-subagent) |
diff --git a/docs/subsystems/README.i18n.yaml b/docs/subsystems/README.i18n.yaml
index f8a704b517..203bd13a1c 100644
--- a/docs/subsystems/README.i18n.yaml
+++ b/docs/subsystems/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/subsystems/README.md
-README.md: d54bd4f45c23100cd1d53080f1ca807a9c177d3d
-README.zh.md: 81ef25b69547672e016a2b72e2293daa8bb42eb1
+README.md: ac241edbb853c2852a836d3cb23415cdb71918a8
+README.zh.md: 43d686ea835e149bada3f55faf7d633b7229ae2f
diff --git a/docs/subsystems/README.md b/docs/subsystems/README.md
index d54bd4f45c..ac241edbb8 100644
--- a/docs/subsystems/README.md
+++ b/docs/subsystems/README.md
@@ -10,6 +10,7 @@ One page per subsystem of the DeepSeek Harness: what it is, the data structures
 | [core.md](core.md) | how `packages/core` controls the agent loop: the package-by-package loop description, agent creation and ownership (`AgentHandle`), the `Agent` handle's delivery/cancellation/interception contracts, and the repo-wide type patterns (`…Map → derived-union`, branded ids) |
 | [llm-streaming.md](llm-streaming.md) | the `packages/llm` conversation types — `Message`/`ContentBlock`, the assembled model request, the `StreamChunk` wire protocol and adapter contract, `BlockAssembler`, and the `LlmAdapter` provider contract |
 | [token-meter.md](token-meter.md) | immutable scalar and positional replay measurements with consumed-log revisions |
+| [network-usage.md](network-usage.md) | optional traffic accounting, spending consent, and metered proxy services |
 | [scope.md](scope.md) | scoped registration identity, dispatch carriers, and the owned `Scope` context |
 | [typert.md](typert.md) | Remote invocation descriptors, lookup/Context declarations, Typert registries, and the Host Gateway/Client API boundaries |
 | [goal.md](goal.md) | persisted goal identity, lifecycle snapshots, activation, change records, and round attribution |
diff --git a/docs/subsystems/README.zh.md b/docs/subsystems/README.zh.md
index 81ef25b695..43d686ea83 100644
--- a/docs/subsystems/README.zh.md
+++ b/docs/subsystems/README.zh.md
@@ -10,6 +10,7 @@
 | [core.md](core.zh.md) | `packages/core` 如何控制 agent loop（智能体循环）：逐包的循环说明、agent 创建与所有权（`AgentHandle`）、`Agent` 句柄的投递/取消/拦截约定，以及全仓通用类型模式（`…Map → derived-union`、品牌化 id） |
 | [llm-streaming.md](llm-streaming.zh.md) | `packages/llm` 的对话类型——`Message`/`ContentBlock`、组装完成的模型请求、`StreamChunk` wire protocol 和适配器约定（adapter contract）、`BlockAssembler`，以及 `LlmAdapter` 提供方约定 |
 | [token-meter.md](token-meter.zh.md) | 不可变的标量与位置回放度量，附带已消费日志修订号 |
+| [network-usage.md](network-usage.zh.md) | 可选的流量计量、费用确认和计量代理服务 |
 | [scope.md](scope.zh.md) | 作用域注册标识、dispatch 载体，以及拥有的 `Scope` 上下文 |
 | [typert.md](typert.zh.md) | 远程调用描述符、lookup/Context 声明、Typert 注册表，以及 Host Gateway/Client API 边界 |
 | [goal.md](goal.zh.md) | 持久 goal 标识、生命周期快照、激活、变更记录与 Round 归属 |
diff --git a/docs/subsystems/persistence.i18n.yaml b/docs/subsystems/persistence.i18n.yaml
index 1abbf9957d..0b61569397 100644
--- a/docs/subsystems/persistence.i18n.yaml
+++ b/docs/subsystems/persistence.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/subsystems/persistence.md
-persistence.md: 7e18e9bbfec8c7aa81525da33ad3cffa4681ea73
-persistence.zh.md: 8250fb93b20dc33c2df705fd2c7092bc8dea155c
+persistence.md: 20bb67dea9e5f6b376a3fc34805b69b7a46fd5b8
+persistence.zh.md: d332473eedd4ef9571c8aa1933cf006671eb29cc
diff --git a/docs/subsystems/persistence.md b/docs/subsystems/persistence.md
index 7e18e9bbfe..20bb67dea9 100644
--- a/docs/subsystems/persistence.md
+++ b/docs/subsystems/persistence.md
@@ -408,6 +408,15 @@ abstract stat(id: SessionId, options?: SessionPersistenceStatOptions): Promise<S
  * @returns one snapshot per stored session.
  */
 abstract list(options?: SessionPersistenceListOptions): Promise<readonly SessionPersistenceSnapshot[]>
+
+/**
+ * Permanently delete an entire stored Session, including every historical generation.
+ * Active write ownership must be released before calling this operation.
+ * @param id - Session explicitly selected for deletion.
+ * @returns Whether a stored Session existed.
+ * @throws When the provider does not support deletion or a writer still owns the Session.
+ */
+delete(id: SessionId): Promise<boolean>
 ```
 
 Types: [SessionId](core.md)
diff --git a/docs/subsystems/persistence.zh.md b/docs/subsystems/persistence.zh.md
index 8250fb93b2..d332473eed 100644
--- a/docs/subsystems/persistence.zh.md
+++ b/docs/subsystems/persistence.zh.md
@@ -408,6 +408,15 @@ abstract stat(id: SessionId, options?: SessionPersistenceStatOptions): Promise<S
  * @returns one snapshot per stored session.
  */
 abstract list(options?: SessionPersistenceListOptions): Promise<readonly SessionPersistenceSnapshot[]>
+
+/**
+ * Permanently delete an entire stored Session, including every historical generation.
+ * Active write ownership must be released before calling this operation.
+ * @param id - Session explicitly selected for deletion.
+ * @returns Whether a stored Session existed.
+ * @throws When the provider does not support deletion or a writer still owns the Session.
+ */
+delete(id: SessionId): Promise<boolean>
 ```
 
 Types: [SessionId](core.zh.md)
diff --git a/docs/subsystems/session.i18n.yaml b/docs/subsystems/session.i18n.yaml
index 099313090b..5c21838e3f 100644
--- a/docs/subsystems/session.i18n.yaml
+++ b/docs/subsystems/session.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/subsystems/session.md
-session.md: 7c7e244bfca17c8e22cabe45fc69126113b685d6
-session.zh.md: fecae92809bdba93588e163447d372c7a42d0260
+session.md: f6deb977445131cbd2d6a667615c0cdf60fdba0e
+session.zh.md: 79bb39fc145fa5021d83478f9b585bb24e2f7863
diff --git a/docs/subsystems/session.md b/docs/subsystems/session.md
index 7c7e244bfc..f6deb97744 100644
--- a/docs/subsystems/session.md
+++ b/docs/subsystems/session.md
@@ -769,6 +769,22 @@ resolveAgent(sessionId: SessionId): Promise<ApiSessionAgentResult>
  */
 inspect( sessionId: SessionId, signal?: AbortSignal, ): Promise<SessionInspection>
 
+/**
+ * Permanently remove an ordinary Session through an explicitly mounted management consumer.
+ * This method is Host-only; the default browser RPC namespace exposes no deletion operation.
+ * @param sessionId - Session explicitly selected by the user.
+ * @returns Whether persisted Session data existed.
+ */
+async deleteSession(sessionId: SessionId): Promise<boolean>
+
+/**
+ * Release managed writers during a Host-only workspace operation.
+ * @param sessionIds - Sessions whose workspace is being managed.
+ * @param operation - Work performed while those Sessions cannot activate.
+ * @returns The operation's result.
+ */
+withSessionsStopped<T>(sessionIds: readonly SessionId[], operation: () => Promise<T>): Promise<T>
+
 /**
  * Read all visible Session rows without resuming an Agent.
  * @param _request - reserved empty list request.
@@ -1083,6 +1099,25 @@ A Session became visible to Session list consumers.
 
 Source: [`packages/api/session-controller/src/types.ts`](../../packages/api/session-controller/src/types.ts)
 
+<a id="api-sessionbefore-delete--parallel"></a>
+
+#### `api-session/before-delete` — parallel
+
+Writers have stopped; consumers can persist independent summaries before the log is removed.
+
+```ts cordis-catalog
+/**
+ * Writers have stopped; consumers can persist independent summaries before the log is removed.
+ * @mode parallel
+ * @param sessionId - Session selected for permanent deletion.
+ */
+'api-session/before-delete'(sessionId: SessionId): Promise<void>
+```
+
+Types: [SessionId](core.md)
+
+Source: [`packages/api/session-controller/src/types.ts`](../../packages/api/session-controller/src/types.ts)
+
 <a id="api-sessionerror--emit"></a>
 
 #### `api-session/error` — emit
diff --git a/docs/subsystems/session.zh.md b/docs/subsystems/session.zh.md
index fecae92809..79bb39fc14 100644
--- a/docs/subsystems/session.zh.md
+++ b/docs/subsystems/session.zh.md
@@ -773,6 +773,22 @@ resolveAgent(sessionId: SessionId): Promise<ApiSessionAgentResult>
  */
 inspect( sessionId: SessionId, signal?: AbortSignal, ): Promise<SessionInspection>
 
+/**
+ * Permanently remove an ordinary Session through an explicitly mounted management consumer.
+ * This method is Host-only; the default browser RPC namespace exposes no deletion operation.
+ * @param sessionId - Session explicitly selected by the user.
+ * @returns Whether persisted Session data existed.
+ */
+async deleteSession(sessionId: SessionId): Promise<boolean>
+
+/**
+ * Release managed writers during a Host-only workspace operation.
+ * @param sessionIds - Sessions whose workspace is being managed.
+ * @param operation - Work performed while those Sessions cannot activate.
+ * @returns The operation's result.
+ */
+withSessionsStopped<T>(sessionIds: readonly SessionId[], operation: () => Promise<T>): Promise<T>
+
 /**
  * Read all visible Session rows without resuming an Agent.
  * @param _request - reserved empty list request.
@@ -1087,6 +1103,25 @@ A Session became visible to Session list consumers.
 
 Source: [`packages/api/session-controller/src/types.ts`](../../packages/api/session-controller/src/types.ts)
 
+<a id="api-sessionbefore-delete--parallel"></a>
+
+#### `api-session/before-delete` — parallel
+
+Writers have stopped; consumers can persist independent summaries before the log is removed.
+
+```ts cordis-catalog
+/**
+ * Writers have stopped; consumers can persist independent summaries before the log is removed.
+ * @mode parallel
+ * @param sessionId - Session selected for permanent deletion.
+ */
+'api-session/before-delete'(sessionId: SessionId): Promise<void>
+```
+
+Types: [SessionId](core.zh.md)
+
+Source: [`packages/api/session-controller/src/types.ts`](../../packages/api/session-controller/src/types.ts)
+
 <a id="api-sessionerror--emit"></a>
 
 #### `api-session/error` — emit
diff --git a/docs/subsystems/web-server.i18n.yaml b/docs/subsystems/web-server.i18n.yaml
index 5af12b3f43..bd42c718aa 100644
--- a/docs/subsystems/web-server.i18n.yaml
+++ b/docs/subsystems/web-server.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write docs/subsystems/web-server.md
-web-server.md: 619dc8ed74ea8835aaa16b90b6e0aced5da41f3a
-web-server.zh.md: c6f77099f3f8b00b07eaeefa1b1db1cb6f388546
+web-server.md: ab88fd4f8c97d9ac4a27f2dd320a7fed442d4052
+web-server.zh.md: 2c4048c8e7d74d07396963658c13f6cc80248e53
diff --git a/docs/subsystems/web-server.md b/docs/subsystems/web-server.md
index 619dc8ed74..ab88fd4f8c 100644
--- a/docs/subsystems/web-server.md
+++ b/docs/subsystems/web-server.md
@@ -173,6 +173,24 @@ Source: [`packages/host/webserver/src/index.ts`](../../packages/host/webserver/s
 
 ### `connection/*` events
 
+<a id="connectionfetch--waterfall"></a>
+
+#### `connection/fetch` — waterfall
+
+Wrap authenticated API bodies for deployment transfer accounting and pacing.
+
+```ts cordis-catalog
+/**
+ * Wrap authenticated API bodies for deployment transfer accounting and pacing.
+ * @param exchange - Authenticated request; listeners may wrap its body before delegating.
+ * @param next - Delegate to the next wrapper or registered API handler.
+ * @mode waterfall
+ */
+'connection/fetch'(exchange: ConnectionFetchRequest, next: () => Promise<Response>): Promise<Response>
+```
+
+Source: [`packages/client/connection/src/index.ts`](../../packages/client/connection/src/index.ts)
+
 <a id="connectionrequest--waterfall"></a>
 
 #### `connection/request` — waterfall
diff --git a/docs/subsystems/web-server.zh.md b/docs/subsystems/web-server.zh.md
index c6f77099f3..2c4048c8e7 100644
--- a/docs/subsystems/web-server.zh.md
+++ b/docs/subsystems/web-server.zh.md
@@ -173,6 +173,24 @@ Source: [`packages/host/webserver/src/index.ts`](../../packages/host/webserver/s
 
 ### `connection/*` events
 
+<a id="connectionfetch--waterfall"></a>
+
+#### `connection/fetch` — waterfall
+
+Wrap authenticated API bodies for deployment transfer accounting and pacing.
+
+```ts cordis-catalog
+/**
+ * Wrap authenticated API bodies for deployment transfer accounting and pacing.
+ * @param exchange - Authenticated request; listeners may wrap its body before delegating.
+ * @param next - Delegate to the next wrapper or registered API handler.
+ * @mode waterfall
+ */
+'connection/fetch'(exchange: ConnectionFetchRequest, next: () => Promise<Response>): Promise<Response>
+```
+
+Source: [`packages/client/connection/src/index.ts`](../../packages/client/connection/src/index.ts)
+
 <a id="connectionrequest--waterfall"></a>
 
 #### `connection/request` — waterfall
diff --git a/packages/api/session-controller/README.i18n.yaml b/packages/api/session-controller/README.i18n.yaml
index 544c5f0228..d5bdbe08c1 100644
--- a/packages/api/session-controller/README.i18n.yaml
+++ b/packages/api/session-controller/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/api/session-controller/README.md
-README.md: f67355be0cb9737ebc67bf589528878bccc26e6d
-README.zh.md: e52501c145c3789213aec517b0801e5e7ee2a4cb
+README.md: 96d34635a16c5d187b8bad285cddca14b222debc
+README.zh.md: 81378971524420dbd9f3e68a876fdf204d8167dd
diff --git a/packages/api/session-controller/README.md b/packages/api/session-controller/README.md
index f67355be0c..96d34635a1 100644
--- a/packages/api/session-controller/README.md
+++ b/packages/api/session-controller/README.md
@@ -64,6 +64,9 @@ References keep local Session data, scoped Contexts, and history streams alive,
 | Field | Default | Meaning |
 |---|---:|---|
 | `nativeOpen` | platform-detected | Whether Session workspace paths can be handed to a native desktop opener |
+| `defaultCwd` | process working directory | Absolute workspace path used when a new Session supplies no cwd |
+
+Host management plugins can stop owned Agents through `withSessionsStopped` or permanently delete a Session through `deleteSession`. Deletion blocks concurrent activation, notifies `api-session/before-delete` consumers before removing persisted logs, and emits the removal notification after success. Agents owned outside this controller must stop through their owner first. This ownership includes newly forked Web Agents.
 
 The generated [configuration catalog](../../../docs/config-catalog.md#deepseek-aidsh-api-session-controller) is the exhaustive source for accepted fields and their JSDoc.
 
diff --git a/packages/api/session-controller/README.zh.md b/packages/api/session-controller/README.zh.md
index e52501c145..8137897152 100644
--- a/packages/api/session-controller/README.zh.md
+++ b/packages/api/session-controller/README.zh.md
@@ -64,6 +64,9 @@ Session 对象还承载本地提交回显：`session.beginSubmission` 在调用
 | 字段 | 默认值 | 含义 |
 |---|---:|---|
 | `nativeOpen` | 平台探测 | 是否能把 Session 工作区路径交给原生桌面打开器 |
+| `defaultCwd` | 进程工作目录 | 新 Session 未指定 cwd 时使用的绝对工作区路径 |
+
+Host 管理插件可以通过 `withSessionsStopped` 停止其所属 Agent，或通过 `deleteSession` 永久删除 Session。删除会阻止并发激活，在移除持久化日志前通知 `api-session/before-delete` 消费者，并在成功后发出移除通知。不属于此控制器的 Agent 必须先通过其拥有者停止。 该所有权也包含 Web 中刚分叉出的 Agent。
 
 生成的[配置目录](../../../docs/config-catalog.zh.md#deepseek-aidsh-api-session-controller)是所有受支持字段及其 JSDoc 的完整来源。
 
diff --git a/packages/api/session-controller/src/agent.ts b/packages/api/session-controller/src/agent.ts
index 8fd5255303..c2c258defb 100644
--- a/packages/api/session-controller/src/agent.ts
+++ b/packages/api/session-controller/src/agent.ts
@@ -4,7 +4,7 @@ import { mkdir } from 'node:fs/promises'
 import type { Context } from '@deepseek-ai/cordis'
 import { installModelSelection } from '@deepseek-ai/dsh-agent'
 import type {
-  Agent, AgentOptions, AgentSetup, ModelSelection as AgentModelSelection, ModelSelectionRef,
+  Agent, AgentHandle, AgentOptions, AgentSetup, ModelSelection as AgentModelSelection, ModelSelectionRef,
 } from '@deepseek-ai/dsh-agent'
 import type {} from '@deepseek-ai/dsh-agent-default-model'
 import type {} from '@deepseek-ai/dsh-agent-presets'
@@ -142,9 +142,14 @@ export class ApiSessionAgentController {
   private readonly creations = new Map<SessionId, Promise<Agent>>()
   private readonly selections = new WeakMap<Agent, InstalledSelection>()
   private readonly imageAdmissionChains = new WeakMap<Agent, Promise<void>>()
+  private readonly handles = new Map<SessionId, AgentHandle>()
+  private readonly deleting = new Set<SessionId>()
 
   /** @param ctx - Host context carrying Agent, model, persistence, and Typert services. */
   constructor(private readonly ctx: Context) {
+    ctx.on('agent/disposed', ({ agent }) => {
+      if (this.handles.get(agent.session.id)?.agent === agent) this.handles.delete(agent.session.id)
+    })
     ctx.typert.lookups.configure('agent', async (sessionId: SessionId) => {
       const found = await this.resolveAgent(sessionId)
       if ('error' in found) throw found.error
@@ -184,6 +189,7 @@ export class ApiSessionAgentController {
     sessionId: SessionId,
     observation?: SessionObservation,
   ): Promise<ApiSessionAgentResult> {
+    if (this.deleting.has(sessionId)) return { error: new RemoteError('session/agent-busy', 'Session deletion is in progress', { reason: 'deletion in progress' }) }
     const live = this.liveAgent(sessionId)
     if (live !== undefined) return live
     const attached = this.ctx.sessions.get(sessionId)
@@ -434,11 +440,11 @@ export class ApiSessionAgentController {
     if (published !== undefined && hasApiSessionSubagentOwner(this.ctx, published, live)) {
       throw new ApiSessionSubagentOwnership(sessionId)
     }
-    return (await this.ctx.agents.resume({
+    return this.retain(this.ctx.agents.resume({
       resumeSessionId: sessionId,
       agentOptions: this.agentOptions(),
       setup: composition.setup,
-    })).agent
+    }))
   }
 
   private async createOrAdopt(
@@ -447,6 +453,7 @@ export class ApiSessionAgentController {
     checkPersistedIdentity: boolean,
     presetId: string | undefined,
   ): Promise<Agent> {
+    if (this.deleting.has(sessionId)) throw new Error('Session deletion is in progress')
     const attached = this.ctx.sessions.get(sessionId)
     const live = this.ctx.agents.get(sessionId)
     if (attached !== undefined && hasApiSessionSubagentOwner(this.ctx, attached, live)) {
@@ -466,11 +473,11 @@ export class ApiSessionAgentController {
         const storedPreset = this.presetForObservation(observation)
         this.assertPresetUnchanged(sessionId, presetId, storedPreset)
         const composition = await this.composeAgent(storedPreset)
-        return (await this.ctx.agents.resume({
+        return this.retain(this.ctx.agents.resume({
           resumeSessionId: sessionId,
           agentOptions: this.agentOptions(),
           setup: composition.setup,
-        })).agent
+        }))
       } catch (error: unknown) {
         if (!(error instanceof SessionQueryError)
           || error.code !== 'SESSION_QUERY_SESSION_NOT_FOUND') throw error
@@ -483,7 +490,7 @@ export class ApiSessionAgentController {
       throw new Error(`failed to ensure project directory "${cwd}": ${String(error)}`, { cause: error })
     }
     const composition = await this.composeAgent(presetId)
-    return (await this.ctx.agents.create({
+    return this.retain(this.ctx.agents.create({
       sessionId,
       agentOptions: this.agentOptions(),
       meta: {
@@ -491,7 +498,57 @@ export class ApiSessionAgentController {
         ...(composition.agentPreset === undefined ? {} : { agentPreset: composition.agentPreset }),
       },
       setup: composition.setup,
-    })).agent
+    }))
+  }
+
+  /**
+   * Retain a controller-created or forked Agent for later managed shutdown.
+   * @param pending - Creation or resume handle owned by this controller.
+   * @returns The retained Agent.
+   */
+  async retain(pending: Promise<AgentHandle>): Promise<Agent> {
+    const handle = await pending
+    this.handles.set(handle.agent.id, handle)
+    return handle.agent
+  }
+
+  /**
+   * Release this controller's writer before deleting persisted Session data.
+   * @param sessionId - Explicitly selected ordinary Session.
+   * @returns Whether persisted data existed.
+   */
+  async deleteSession(sessionId: SessionId): Promise<boolean> {
+    return this.withSessionsStopped([sessionId], async () => {
+      await this.ctx.parallel('api-session/before-delete', sessionId)
+      return this.ctx.sessionPersistence.delete(sessionId)
+    })
+  }
+
+  /**
+   * Suspend activation and release owned writers while a management operation runs.
+   * @param sessionIds - Ordinary Sessions affected by the operation.
+   * @param operation - Operation performed after all owned writers stop.
+   * @returns The operation's result.
+   */
+  async withSessionsStopped<T>(sessionIds: readonly SessionId[], operation: () => Promise<T>): Promise<T> {
+    const ids = [...new Set(sessionIds)]
+    if (ids.some(id => this.deleting.has(id))) throw new Error('Session management is already in progress')
+    for (const id of ids) this.deleting.add(id)
+    try {
+      for (const sessionId of ids) {
+        await Promise.all([this.creations.get(sessionId), this.resumes.get(sessionId)])
+        const live = this.ctx.agents.get(sessionId)
+        const handle = this.handles.get(sessionId)
+        if (live !== undefined && (handle?.agent !== live || hasApiSessionSubagentOwner(this.ctx, live.session, live))) {
+          throw new Error('Session is owned by another Agent; stop its owner before deleting it')
+        }
+        if (handle !== undefined) {
+          await handle.dispose()
+          this.handles.delete(sessionId)
+        }
+      }
+      return await operation()
+    } finally { for (const id of ids) this.deleting.delete(id) }
   }
 
   private agentOptions(): AgentOptions {
diff --git a/packages/api/session-controller/src/commands.ts b/packages/api/session-controller/src/commands.ts
index 80008319fe..afb660c709 100644
--- a/packages/api/session-controller/src/commands.ts
+++ b/packages/api/session-controller/src/commands.ts
@@ -255,7 +255,7 @@ export class SessionCommandController {
     const composition = await this.agents.composeAgent(this.agents.presetForObservation(source))
     try {
       const { provider, model } = this.ctx.agentDefaultModel.currentSelection()
-      await this.ctx.agents.create({
+      await this.agents.retain(this.ctx.agents.create({
         sessionId: childId,
         seed: source.events.slice(0, cut),
         inheritedEventCount: cut,
@@ -269,7 +269,7 @@ export class SessionCommandController {
         },
         agentOptions: { provider, model },
         setup: composition.setup,
-      })
+      }))
     } catch (error) {
       throw new RemoteError(
         'gateway/internal',
diff --git a/packages/api/session-controller/src/index.ts b/packages/api/session-controller/src/index.ts
index f13f24f7dc..673e64a633 100644
--- a/packages/api/session-controller/src/index.ts
+++ b/packages/api/session-controller/src/index.ts
@@ -24,6 +24,7 @@ import { buildModelCatalog } from './catalog.ts'
 import { installModelSelectionProjection } from './model-selection-projection.ts'
 import { SessionSkillCatalog } from './skill-catalog.ts'
 import { SessionMediaReferences } from './media-references.ts'
+import { isAbsolute } from 'node:path'
 import type {
   ModelCatalog,
   SessionAttachmentRequest,
@@ -71,6 +72,8 @@ declare module '@deepseek-ai/cordis' {
 export interface Config {
   /** Override platform desktop-opener detection. */
   readonly nativeOpen?: boolean
+  /** Workspace directory for new Sessions that select no workspace; defaults to the invocation cwd. */
+  readonly defaultCwd?: string
 }
 
 /** Host integrations replaceable by direct unit tests. */
@@ -92,6 +95,7 @@ export class SessionController extends TypertRemoteService {
     'fileUploads',
     'llm',
     'sessions',
+    'sessionPersistence',
     'sessionProjections',
     'sessionQuery',
     'typert',
@@ -100,6 +104,7 @@ export class SessionController extends TypertRemoteService {
 
   static Config: z<Config> = z.object({
     nativeOpen: z.boolean(),
+    defaultCwd: z.string(),
   })
 
   private readonly agents: ApiSessionAgentController
@@ -119,9 +124,10 @@ export class SessionController extends TypertRemoteService {
    */
   constructor(ctx: Context, config: Config, internals: SessionControllerInternals = {}) {
     super(ctx, 'sessionController', { namespace: 'session' })
+    if (config.defaultCwd !== undefined && !isAbsolute(config.defaultCwd)) throw new Error('defaultCwd must be absolute')
     installModelSelectionProjection(ctx)
     this.agents = new ApiSessionAgentController(ctx)
-    this.commands = new SessionCommandController(ctx, this.agents, process.cwd())
+    this.commands = new SessionCommandController(ctx, this.agents, config.defaultCwd ?? process.cwd())
     ctx.effect(() => ctx.fileUploads.registerAgentResolver(async (sessionId) => {
       const result = await this.agents.resolveAgent(sessionId)
       if ('error' in result) throw result.error
@@ -214,6 +220,28 @@ export class SessionController extends TypertRemoteService {
     return inspectApiSession(this.ctx, sessionId, signal)
   }
 
+  /**
+   * Permanently remove an ordinary Session through an explicitly mounted management consumer.
+   * This method is Host-only; the default browser RPC namespace exposes no deletion operation.
+   * @param sessionId - Session explicitly selected by the user.
+   * @returns Whether persisted Session data existed.
+   */
+  async deleteSession(sessionId: SessionId): Promise<boolean> {
+    const deleted = await this.agents.deleteSession(sessionId)
+    this.ctx.emit('api-session/removed', sessionId)
+    return deleted
+  }
+
+  /**
+   * Release managed writers during a Host-only workspace operation.
+   * @param sessionIds - Sessions whose workspace is being managed.
+   * @param operation - Work performed while those Sessions cannot activate.
+   * @returns The operation's result.
+   */
+  withSessionsStopped<T>(sessionIds: readonly SessionId[], operation: () => Promise<T>): Promise<T> {
+    return this.agents.withSessionsStopped(sessionIds, operation)
+  }
+
   /**
    * Read all visible Session rows without resuming an Agent.
    * @param _request - reserved empty list request.
diff --git a/packages/api/session-controller/src/types.ts b/packages/api/session-controller/src/types.ts
index f03d8d1b65..40548519e9 100644
--- a/packages/api/session-controller/src/types.ts
+++ b/packages/api/session-controller/src/types.ts
@@ -559,6 +559,12 @@ export type SessionControlFrame =
 
 declare module '@deepseek-ai/cordis' {
   interface Events {
+    /**
+     * Writers have stopped; consumers can persist independent summaries before the log is removed.
+     * @mode parallel
+     * @param sessionId - Session selected for permanent deletion.
+     */
+    'api-session/before-delete'(sessionId: SessionId): Promise<void>
     /**
      * A Session became visible to Session list consumers.
      * @mode emit
diff --git a/packages/api/session-controller/tests/commands-create-fork.host.spec.ts b/packages/api/session-controller/tests/commands-create-fork.host.spec.ts
index 42bb3fe508..c7ce3a9831 100644
--- a/packages/api/session-controller/tests/commands-create-fork.host.spec.ts
+++ b/packages/api/session-controller/tests/commands-create-fork.host.spec.ts
@@ -21,6 +21,7 @@ async function expectFailure(operation: Promise<unknown>, code: string): Promise
 function controllerAgents(overrides: object = {}): ApiSessionAgentController {
   return {
     ensureSession: () => Promise.resolve(),
+    retain: async (pending: Promise<AgentHandle>) => (await pending).agent,
     composeAgent: () => Promise.resolve({ setup: () => {} }),
     presetForSession: () => undefined,
     presetForObservation: () => undefined,
diff --git a/packages/client/connection/README.i18n.yaml b/packages/client/connection/README.i18n.yaml
index ca0d70fffa..29627bf9e6 100644
--- a/packages/client/connection/README.i18n.yaml
+++ b/packages/client/connection/README.i18n.yaml
@@ -1,6 +1,5 @@
-# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each
-# side as of the last confirmed-consistent state. Both languages carry equal authority;
-# after editing either side, bring the other along and re-record with:
-#   pnpm run verify-translation-pairing --write packages/client/connection/README.md
-README.md: f72dd40db4c6414c368e039300198151cf2bd96a
-README.zh.md: 4644dfb23074338bdd5888d20e0f0115811115d8
+# Bilingual-pair consistency record (docs/i18n/README.md).
+# Both languages carry equal authority.
+# Re-record with: pnpm run verify-translation-pairing --write packages/client/connection/README.md
+README.md: 2d7cec3189dbf4d3fee33544dc381e1bcdda05c8
+README.zh.md: b0ceb1b0fe779d11b633d154335d50e96f6917c2
diff --git a/packages/client/connection/README.md b/packages/client/connection/README.md
index f72dd40db4..2d7cec3189 100644
--- a/packages/client/connection/README.md
+++ b/packages/client/connection/README.md
@@ -42,6 +42,8 @@ Before authentication, every request still passes `src/api-request-trust.ts`. It
 
 Authenticated shared HTTP requests pass through the `connection/request` waterfall before body transfer. A listener may refuse new requests or await `next()` through response completion; removing its owning fiber removes admission behavior. Desktop uses this hook to lock new API work during an approved installation without canceling already-admitted work. WebSocket stream ownership remains with API Gateway.
 
+Authenticated Fetch routes also pass through `connection/fetch`. A listener may replace the exchange's Request before calling `next()` and wrap the returned Response to meter streamed bodies. Listeners own cancellation of their wrapping streams; this hook does not wrap Gateway WebSockets.
+
 <a id="connection-generation"></a>
 ## Connection generation
 
@@ -83,3 +85,5 @@ None.
 </details>
 
 **Runtime invariant:** No companion is published. Browser-session verification reads the credential record asynchronously at the request that authorizes work, while the credentials companion owns record commit-event lifetime. Stream/reconnect sequencing and rpcId round-trip discipline are exercised directly by behavior specs, and route register/dispose symmetry is audited by the webserver companion.
+
+Web deployments may register one browser authentication provider through registerBrowserAuthentication, replacing launch-token and cookie authentication while retaining Host/Origin checks. The owning plugin disposes its registration.
diff --git a/packages/client/connection/README.zh.md b/packages/client/connection/README.zh.md
index 4644dfb230..b0ceb1b0fe 100644
--- a/packages/client/connection/README.zh.md
+++ b/packages/client/connection/README.zh.md
@@ -42,6 +42,8 @@ cookie 签名密钥是 `ctx.credentials` 中由 `client-connection/browser-sessi
 
 通过认证的共享 HTTP 请求在传输请求体之前经过 `connection/request` waterfall。监听器可以拒绝新请求，或等待 `next()` 直到响应完成；释放所属 fiber 会移除准入行为。Desktop 使用此扩展点，在已批准的安装期间锁住新的 API 工作，而不取消已接纳的工作。WebSocket 流仍由 API Gateway 负责。
 
+已认证的 Fetch 路由还会经过 `connection/fetch`。监听器可以在调用 `next()` 前替换交换对象的 Request，并包装返回的 Response 来计量流式正文。监听器负责取消其包装的流；此扩展点不包装 Gateway WebSocket。
+
 <a id="connection-generation"></a>
 ## Connection generation
 
@@ -83,3 +85,5 @@ API Gateway Client 把内部 `$events` 逻辑流注册为唯一 generation sourc
 </details>
 
 **运行时不变式：** 不发布伴生入口。浏览器会话验证会在请求授权工作时异步读取凭据记录，而记录的 commit-event 生命周期由 credentials 伴生入口负责；流与重连的时序及 rpcId 往返约束由行为规范直接验证，路由注册与 dispose（资源释放）的对称性由 webserver 伴生入口审计。
+
+Web 部署可通过 registerBrowserAuthentication 注册单一身份验证提供方，替换默认启动令牌与 Cookie 验证；Host/Origin 检查仍然执行。注册由插件生命周期释放。
diff --git a/packages/client/connection/src/index.ts b/packages/client/connection/src/index.ts
index 7a4457e375..d1fd97e6d0 100644
--- a/packages/client/connection/src/index.ts
+++ b/packages/client/connection/src/index.ts
@@ -25,6 +25,7 @@ export type {
   ConnectionRequestRejection,
   ConnectionRpcResult,
   ConnectionRequestBodyMode,
+  BrowserAuthentication,
   ConnectionTrustRequest,
   ClientRequest,
   HostConnectionHandle,
@@ -49,6 +50,11 @@ export { API_PATH } from './api-path.ts'
 /** Stable Cordis plugin name. */
 export const name = 'client-connection'
 
+/** Mutable authenticated request handed through deployment body wrappers. */
+export interface ConnectionFetchRequest {
+  request: Request
+}
+
 declare module '@deepseek-ai/cordis' {
   interface Events {
     /**
@@ -60,6 +66,13 @@ declare module '@deepseek-ai/cordis' {
      * @mode waterfall
      */
     'connection/request'(request: IncomingMessage, response: ServerResponse, next: () => Promise<void>): Promise<void>
+    /**
+     * Wrap authenticated API bodies for deployment transfer accounting and pacing.
+     * @param exchange - Authenticated request; listeners may wrap its body before delegating.
+     * @param next - Delegate to the next wrapper or registered API handler.
+     * @mode waterfall
+     */
+    'connection/fetch'(exchange: ConnectionFetchRequest, next: () => Promise<Response>): Promise<Response>
   }
 }
 
@@ -147,7 +160,13 @@ export async function apply(ctx: Context, config?: ConnectionConfig): Promise<vo
           res.end(rejection === 401 ? 'unauthorized' : 'forbidden')
           return
         }
-        await webCtx.waterfall('connection/request', req, res, () => bridge(req, res, fetchHandler, maxRequestBodyBytes))
+        await webCtx.waterfall('connection/request', req, res, () => bridge(req, res, {
+          requestBodyMode: input => fetchHandler.requestBodyMode(input),
+          fetch: request => {
+            const exchange = { request }
+            return webCtx.waterfall('connection/fetch', exchange, () => fetchHandler.fetch(exchange.request))
+          },
+        }, maxRequestBodyBytes))
       },
     }
     webCtx.effect(() => webCtx.webServer.register(route), 'client-connection: /api route')
diff --git a/packages/client/connection/src/rpc-host.ts b/packages/client/connection/src/rpc-host.ts
index d74734e132..987e37bd75 100644
--- a/packages/client/connection/src/rpc-host.ts
+++ b/packages/client/connection/src/rpc-host.ts
@@ -13,6 +13,7 @@ import { isTrustedApiRequest } from './api-request-trust.ts'
 import { API_PATH } from './api-path.ts'
 import type { BrowserAuth } from './browser-auth.ts'
 import type {
+  BrowserAuthentication,
   ConnectionIndexRequest,
   ConnectionIndexResponse,
   ConnectionFetchRoute,
@@ -58,6 +59,15 @@ declare module '@deepseek-ai/cordis' {
 
 /** Host Connection service whose channel registrations belong to the caller fiber. */
 export class HostConnectionService extends Service implements HostConnectionHandle {
+  private authentication: BrowserAuthentication | undefined
+
+  /** @param provider - Deployment authentication owner. @returns registration disposer. */
+  registerBrowserAuthentication(provider: BrowserAuthentication): () => void {
+    if (this.authentication !== undefined) throw new Error('Browser authentication is already registered')
+    this.authentication = provider
+    return () => { this.authentication = undefined }
+  }
+
   private readonly interceptors = new Map<string, ConnectionRpcInterceptor>()
   private readonly fetchRoutes = new Map<string, RegisteredFetchRoute>()
 
@@ -96,17 +106,17 @@ export class HostConnectionService extends Service implements HostConnectionHand
   /** Apply the configured Host/Origin fence, then browser authentication. */
   requestRejection(request: ConnectionTrustRequest): ConnectionRequestRejection {
     if (!isTrustedApiRequest(request, this.trustedHosts)) return 403
-    return this.browserAuth.isAuthenticated(request) ? undefined : 401
+    return (this.authentication ?? this.browserAuth).isAuthenticated(request) ? undefined : 401
   }
 
   /** Authenticate an index request through the process-token exchange or cookie. */
   authorizeIndex(request: ConnectionIndexRequest, response: ConnectionIndexResponse): boolean {
-    return this.browserAuth.authorizeIndex(request, response)
+    return (this.authentication ?? this.browserAuth).authorizeIndex(request, response)
   }
 
   /** Add this process's launch token to the clean application URL. */
   authenticatedUrl(baseUrl: string): string {
-    return this.browserAuth.authenticatedUrl(baseUrl)
+    return (this.authentication ?? this.browserAuth).authenticatedUrl(baseUrl)
   }
 
   /**
diff --git a/packages/client/connection/src/rpc.ts b/packages/client/connection/src/rpc.ts
index 9474d72c36..f786aaff6b 100644
--- a/packages/client/connection/src/rpc.ts
+++ b/packages/client/connection/src/rpc.ts
@@ -77,6 +77,9 @@ export type RpcMessage = ClientRequest | ServerResponse
 
 /** HTTP request facts consumed by browser trust and authentication. */
 export interface ConnectionTrustRequest {
+  /** Request target for page-scoped credentials on native browser resources. */
+  readonly url?: string | undefined
+
   /** Request headers supplied by either the Fetch or node:http representation. */
   readonly headers: Headers | Readonly<Record<string, string | readonly string[] | undefined>>
 }
@@ -161,8 +164,21 @@ export interface HostConnectionRpc {
   ): () => Promise<void>
 }
 
-/** Host `ctx.connection` shape consumed by transport-independent adapters. */
+/** Browser authentication supplied by one deployment plugin. */
+export interface BrowserAuthentication {
+  /** @param request - HTTP headers and target. @returns whether credentials are accepted. */
+  isAuthenticated(request: ConnectionTrustRequest): boolean
+  /** @param request - Index request. @param response - Rejection response. @returns whether to serve the shell. */
+  authorizeIndex(request: ConnectionIndexRequest, response: ConnectionIndexResponse): boolean
+  /** @param baseUrl - Application origin. @returns browser launch URL. */
+  authenticatedUrl(baseUrl: string): string
+}
+
+/** Host transport registrations and browser authentication. */
 export interface HostConnectionHandle {
+  /** @param provider - Deployment authentication owner. @returns registration disposer. */
+  registerBrowserAuthentication(provider: BrowserAuthentication): () => void
+
   /** Generic RPC channel registry. */
   readonly rpc: HostConnectionRpc
   /** Exact Fetch routes for streaming or browser-native responses. */
diff --git a/packages/client/locale/src/locales/zh.ts b/packages/client/locale/src/locales/zh.ts
index 2e932cef41..497b0236a0 100644
--- a/packages/client/locale/src/locales/zh.ts
+++ b/packages/client/locale/src/locales/zh.ts
@@ -28,7 +28,7 @@ export const zh = {
   'collapse': '收起',
   'expand': '展开',
   'back': '返回',
-  'brand.localBuild': 'DSH 本地构建',
+  'brand.localBuild': 'DSH 云端部署',
   'unknown': '未知',
   'none': '无',
   'truncated': '已截断',
diff --git a/packages/client/ui-conversation/src/client/skeleton/EmptyHero.tsx b/packages/client/ui-conversation/src/client/skeleton/EmptyHero.tsx
index 22be826845..b9f94b072f 100644
--- a/packages/client/ui-conversation/src/client/skeleton/EmptyHero.tsx
+++ b/packages/client/ui-conversation/src/client/skeleton/EmptyHero.tsx
@@ -152,7 +152,6 @@ export function HeroShell({ t, renderSlot, children }: HeroShellProps) {
           <span className={css.titleGroup}>
             {/* Own element: keeps the headline text addressable apart from the badge. */}
             <span>{t('hero.headline')}</span>
-            <span className={css.previewBadge}>{t('hero.preview')}</span>
           </span>
         </div>
         <div className={css.body}>
diff --git a/packages/client/ui-conversation/tests/skeleton.client.spec.tsx b/packages/client/ui-conversation/tests/skeleton.client.spec.tsx
index e95808c1bc..111e70789d 100644
--- a/packages/client/ui-conversation/tests/skeleton.client.spec.tsx
+++ b/packages/client/ui-conversation/tests/skeleton.client.spec.tsx
@@ -526,7 +526,7 @@ describe('ConversationRoot resident composer', () => {
     expect(b.slotCalls).not.toContain('conversation.session.header.utilities')
     expect(b.slotCalls).not.toContain('conversation.session.header.actions')
     expect(b.view.getByText('探索未至之境')).toBeTruthy()
-    expect(b.view.getByText('预览版')).toBeTruthy()
+    expect(b.view.queryByText('预览版')).toBeNull()
     expect(b.view.queryByTestId('view-chat')).toBeNull()
     // The same machine-backed textarea is live in the hero, and the
     // persistence mirror stays bound (ConversationSession mounts chrome-hidden
diff --git a/packages/client/ui-deliverables/README.i18n.yaml b/packages/client/ui-deliverables/README.i18n.yaml
index 041ec3fa6a..13becb935e 100644
--- a/packages/client/ui-deliverables/README.i18n.yaml
+++ b/packages/client/ui-deliverables/README.i18n.yaml
@@ -1,6 +1,5 @@
-# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each
-# side as of the last confirmed-consistent state. Both languages carry equal authority;
-# after editing either side, bring the other along and re-record with:
-#   pnpm run verify-translation-pairing --write packages/client/ui-deliverables/README.md
-README.md: 46e289ddb572928baaee7ac1a370087fd789aebe
-README.zh.md: 804ee5c7613510df96ac48cfe08f9b0fd39f7bbc
+# Bilingual-pair consistency record (docs/i18n/README.md).
+# Both languages carry equal authority.
+# Re-record with: pnpm run verify-translation-pairing --write packages/client/ui-deliverables/README.md
+README.md: 1a67db8b12377e34fe38f1f07dd8ec13c4f18449
+README.zh.md: 78435a688f849dd0e508ec10605447fc91eff09f
diff --git a/packages/client/ui-deliverables/README.md b/packages/client/ui-deliverables/README.md
index 46e289ddb5..1a67db8b12 100644
--- a/packages/client/ui-deliverables/README.md
+++ b/packages/client/ui-deliverables/README.md
@@ -30,9 +30,9 @@ Mount this plugin alongside `ui-conversation` and the Host [workspace-changes](.
 <a id="explicit-deliveries"></a>
 ### Explicit deliveries
 
-The Web `standard`, `ptc`, and `cordis` presets expose `present` for final files accessible through the Session filesystem, including files created through Bash. Call it with `files: [{ path, description? }]` after creating the files. The [present tool](../../deliverables/tool-present/README.md) owns file-count limits and Session declarations. The closing turn shows one delivery as a full-width card and multiple deliveries in a two-column grid with 10px gaps. A list longer than four files starts collapsed and provides a control that reveals or hides the complete list. Each 60px-high card uses 8px vertical and 10px horizontal inset spacing, a 20px shared `FileTypeIcon` in a 40px frame, 13px filename text, 10px secondary text, and a 12px Open action. It shows the basename and description, or the file type when no description exists; a trailing parenthesized suffix in the description is omitted, and hovering the card replaces that line with the Sidebar-preview action. Clicking the card or the left side of its split Open control previews the file in the right Sidebar. The chevron opens the standard menu for the Host default application plus Show in Finder on macOS, Show in File Explorer on Windows and WSL, or Open containing folder through the default Linux file manager. Matching inline-code references preview the same source files in the right Sidebar; native opening requires an explicit card-menu action. Repeated declaration of a path selects its latest description before the closing reply.
+Files declared with `present` appear as one card collection: a single file fills the row, multiple files use a grid, and lists over four items collapse. Clicking a card or its primary button previews in the Sidebar. The menu includes Sidebar preview and plugin contributions through `deliverables.file.action` for new-tab viewing, editing, and downloading. Disabled plugins contribute no action. Matching inline-code references retain Sidebar preview.
 
-The `present` tool row shows running, delivered, failed, or interrupted status; expanding a settled row reveals its recorded result. The collapsible card grid retains every delivered file. Both menu actions share pending state and show progress, acknowledgement, or an action-specific retryable error. Desktop information is read when delivery cards appear and invalidated on connection replacement; responses from a replaced connection cannot publish metadata. Selecting a native menu action returns keyboard focus to the available Sidebar Open button. Pending actions close the menu until another explicit gesture. A missing desktop disables the Open menu; a failed desktop-information read offers Retry. It requires a desktop and a suitable default application on the serving Host; a remote browser does not open applications on its own device.
+The `present` tool row retains execution status and recorded results. Browser actions on file cards do not require a server desktop.
 
 ### The changed-files card
 
diff --git a/packages/client/ui-deliverables/README.zh.md b/packages/client/ui-deliverables/README.zh.md
index 804ee5c761..78435a688f 100644
--- a/packages/client/ui-deliverables/README.zh.md
+++ b/packages/client/ui-deliverables/README.zh.md
@@ -30,9 +30,9 @@ kind: "package-reference"
 <a id="explicit-deliveries"></a>
 ### 显式交付
 
-Web 的 `standard`、`ptc` 与 `cordis` preset 提供 `present` 用于声明交付会话文件系统可访问的最终文件，包括通过 Bash 创建的文件。创建文件后，以 `files: [{ path, description? }]` 调用。[present 工具](../../deliverables/tool-present/README.zh.md)拥有文件数量限制和会话声明。收尾轮次把单个交付显示为横向占满内容区的卡片，把多个交付显示为间距 10px 的双列网格。文件超过四个时，列表默认收起，并提供显示或隐藏完整列表的控件。每张卡片高 60px，上下内边距为 8px、左右为 10px；40px 图标框内使用 20px 的共享 `FileTypeIcon`，文件名为 13px、次要文本为 10px，“打开”操作为 12px。卡片显示 basename 与说明；没有说明时显示文件类型，说明末尾的括号后缀会被省略，悬停卡片时该行切换为侧栏预览提示。点击卡片或分段“打开”控件的左侧会在右侧 Sidebar 中预览文件；右侧箭头打开标准菜单，其中提供 Host 默认应用，以及 macOS 上的“在 Finder 中显示”、Windows 和 WSL 上的“在文件资源管理器中显示”或 Linux 默认文件管理器的“打开所在文件夹”。匹配的行内代码引用在右侧 Sidebar 中预览相同源文件；原生打开需要显式选择卡片菜单中的操作。同一路径重复声明时，选择收尾回复之前最近一次的说明。
+`present` 声明的文件统一显示为文件卡片：单文件占满一行，多文件使用网格，超过四项可折叠。点击卡片或主按钮默认在侧边栏预览。菜单包含侧边预览，以及插件通过 `deliverables.file.action` 提供的新标签页打开、编辑和下载操作；未启用的插件不显示对应操作。匹配的行内代码引用仍可打开侧边预览。
 
-`present` 工具行显示正在交付、已交付、失败或中断状态；展开已结束的调用可查看其记录的结果。可折叠卡片网格保留全部交付文件。菜单中的两个操作共享等待状态，并显示进度、成功确认或各自可重试的错误。交付卡片出现时读取桌面信息，连接更换时清除缓存，旧连接的响应不能更新元数据。选择原生菜单操作后，键盘焦点回到仍可用的侧边栏“打开”按钮。等待操作完成时关闭菜单，用户再次点击才会打开。Host 没有桌面时禁用“打开”菜单；桌面信息读取失败时提供“重试”。服务 Host 必须具备桌面和合适的默认应用；远程浏览器不会打开其所在设备上的应用。
+`present` 工具行保留执行状态与已记录结果。文件卡片的浏览器操作不要求服务器桌面环境。
 
 ### 改动文件卡片
 
diff --git a/packages/client/ui-deliverables/src/client/Deliverables.tsx b/packages/client/ui-deliverables/src/client/Deliverables.tsx
index 112bc68070..cd892dd5d8 100644
--- a/packages/client/ui-deliverables/src/client/Deliverables.tsx
+++ b/packages/client/ui-deliverables/src/client/Deliverables.tsx
@@ -2,7 +2,7 @@
 import { useEffect, useState } from 'react'
 import type { TurnTailOwnerProps } from '@deepseek-ai/dsh-client-ui-chat/client'
 import { Button, IconChevronDownOutline14, IconChevronUpOutline14 } from '@deepseek-ai/dsh-client-ui-primitives'
-import type { GlobalStandardProps, InjectFace, PropsLocale, PropsRuntime, SessionStandardProps } from '@deepseek-ai/dsh-client-ui-slots'
+import type { GlobalStandardProps, PropsRenderSlots, InjectFace, PropsLocale, PropsRuntime, SessionStandardProps } from '@deepseek-ai/dsh-client-ui-slots'
 import type { ObservableSnapshot } from '@deepseek-ai/dsh-client-store'
 import type { PresentedOpenController } from './present-open.ts'
 import type { ChangesSummaryStore } from './changes-summary.ts'
@@ -49,7 +49,7 @@ export function selectDeliverables(owner: TurnTailOwnerProps): DeliverablesMatch
  * @param props - closing Turn, file actions, and localized copy.
  * @returns file rows, or null when the Turn declares none.
  */
-export function DeliverablesTail(props: PropsRuntime<'conversation.chat.turnTail'> & PropsLocale<typeof NS> & InjectFace<DeliverablesInjected>) {
+export function DeliverablesTail(props: PropsRuntime<'conversation.chat.turnTail'> & PropsLocale<typeof NS> & InjectFace<DeliverablesInjected> & PropsRenderSlots<'deliverables.file.action'>) {
   const matched = selectDeliverables(props)
   return matched === null ? null : <Deliverables {...props} matched={matched} />
 }
@@ -62,11 +62,11 @@ export function DeliverablesTail(props: PropsRuntime<'conversation.chat.turnTail
  * @returns the closing turn's file rows.
  */
 export function Deliverables({
-  matched, openFile, t, sessionId, useSessions, openPresented, openChangesReview, usePresentedOpen, usePresentedHost,
+  matched, openFile, t, sessionId, renderSlot, useSessions, openPresented, openChangesReview, usePresentedOpen, usePresentedHost,
   useChangesSummary, reloadPresentedHost, loadChangesSummary,
 }: Pick<TurnTailOwnerProps, 'openFile'> & {
   matched: DeliverablesMatch
-} & PropsLocale<typeof NS> & Pick<SessionStandardProps, 'sessionId'> & Pick<GlobalStandardProps, 'useSessions'> & InjectFace<DeliverablesInjected>) {
+} & PropsLocale<typeof NS> & Pick<SessionStandardProps, 'sessionId'> & Pick<GlobalStandardProps, 'useSessions'> & InjectFace<DeliverablesInjected> & PropsRenderSlots<'deliverables.file.action'>) {
   const [expanded, setExpanded] = useState(false)
   const cwd = useSessions(state => state.byId[sessionId]?.cwd)
   const states = usePresentedOpen(value => value)
@@ -97,11 +97,11 @@ export function Deliverables({
         <span>{t('presented.hostError')}</span>
         <Button size="sm" onClick={() => { void reloadPresentedHost() }}>{t('presented.retry')}</Button>
       </div>}
-      {host !== null && host !== 'error' && !host.available && <span className={css.hostStatus}>{t('presented.unavailable')}</span>}
       <div className={css.presented} data-presented-files-row data-single={matched.presented.length === 1 ? true : undefined}>
         {presented.map(file => <PresentedFileCard key={`${file.seq}:${file.index}`} file={file} cwd={cwd}
           phase={states[presentedFileUrl(sessionId, file.seq, file.index)]}
           host={host === 'error' ? null : host} t={t}
+          extra={dismiss => renderSlot('deliverables.file.action', { path: file.path, dismiss })}
           onPreview={() => { openFile(file.path) }}
           onAction={(action) => { void openPresented(sessionId, file.seq, file.index, action) }} />)}
       </div>
diff --git a/packages/client/ui-deliverables/src/client/PresentedFileCard.tsx b/packages/client/ui-deliverables/src/client/PresentedFileCard.tsx
index 5466473999..3a55098dcd 100644
--- a/packages/client/ui-deliverables/src/client/PresentedFileCard.tsx
+++ b/packages/client/ui-deliverables/src/client/PresentedFileCard.tsx
@@ -1,9 +1,10 @@
 /** File identity and explicit default-app or file-manager actions for one delivery. */
+import type { ReactNode } from 'react'
 import { useRef, useState } from 'react'
 import { resolveWorkspacePath } from '@deepseek-ai/dsh-util-workspace-path'
 import {
-  Menu, FileTypeIcon, fileExtension, IconRightUpOutline16,
-  IconChevronDownOutline14, IconFolderOpenOutline16,
+  Menu, FileTypeIcon, fileExtension,
+  IconChevronDownOutline14,
 } from '@deepseek-ai/dsh-client-ui-primitives'
 import type { PropsLocale } from '@deepseek-ai/dsh-client-ui-slots'
 import type { PresentedAction, PresentedHost } from '../presented.ts'
@@ -22,25 +23,22 @@ function cardDescription(description: string | undefined, fallback: string): str
  * @param props - durable file metadata, Sidebar preview, Host capabilities, gesture status, and localized copy.
  * @returns the file card and its anchored action menu.
  */
-export function PresentedFileCard({ file, cwd, phase, host, onPreview, onAction, t }: {
+export function PresentedFileCard({ file, cwd, phase, host, onPreview, extra, t }: {
   file: PresentedPath
   cwd: string | undefined
   phase: PresentedOpenPhase | undefined
   host: PresentedHost | null
+  extra: (dismiss: () => void) => ReactNode
   onPreview: () => void
   onAction: (action: PresentedAction) => void
 } & PropsLocale<typeof NS>) {
   const [menuOpen, setMenuOpen] = useState(false)
   const previewRef = useRef<HTMLButtonElement>(null)
   const pending = phase === 'opening' || phase === 'revealing'
-  const menuDisabled = pending || host === null || !host.available
+  const menuDisabled = pending
   if (menuDisabled && menuOpen) setMenuOpen(false)
   const reveal = host?.fileManager ?? 'directory'
-  const act = (action: PresentedAction) => {
-    setMenuOpen(false)
-    previewRef.current?.focus()
-    onAction(action)
-  }
+
   const name = basename(file.path)
   const metadata = fileExtension(name).toUpperCase() || t('presented.file')
   const status = phase === undefined
@@ -72,13 +70,10 @@ export function PresentedFileCard({ file, cwd, phase, host, onPreview, onAction,
             onClick={() => { setMenuOpen(value => !value) }}>
             <IconChevronDownOutline14 size={11} />
           </button>}
-          items={[
-            { id: 'open', icon: <IconRightUpOutline16 size={16} className={css.menuActionIcon} />,
-              label: t('presented.defaultApp') },
-            { id: 'reveal', icon: <IconFolderOpenOutline16 />,
-              label: t(`presented.${reveal}`) },
-          ]}
-          onSelect={(id) => { act(id === 'reveal' ? 'reveal' : 'open') }} />
+          items={[{ id: 'preview', label: t('presented.action') }]}
+          extra={extra(() => { setMenuOpen(false) })}
+          onSelect={() => { setMenuOpen(false); previewRef.current?.focus(); onPreview() }}
+        />
       </div>
     </div>
   </div>
diff --git a/packages/client/ui-deliverables/src/client/index.ts b/packages/client/ui-deliverables/src/client/index.ts
index 76cecc33bc..98e95027a7 100644
--- a/packages/client/ui-deliverables/src/client/index.ts
+++ b/packages/client/ui-deliverables/src/client/index.ts
@@ -12,6 +12,7 @@ import type { Context as ClientContext } from '@deepseek-ai/cordis'
 import type {} from '@deepseek-ai/dsh-api-remotes/client'
 import type {} from '@deepseek-ai/dsh-client-connection/client'
 import type { ChatFileMentions } from '@deepseek-ai/dsh-client-ui-chat/client'
+export type { DeliverablesTurnData, PresentedPath } from './turn-deliverables.ts'
 import type {} from '@deepseek-ai/dsh-client-locale/client'
 import type {} from '@deepseek-ai/dsh-client-ui-conversation/client'
 import type {} from '@deepseek-ai/dsh-client-ui-renderer/client'
@@ -31,6 +32,10 @@ import {
 } from './turn-deliverables.ts'
 
 declare module '@deepseek-ai/dsh-client-ui-slots' {
+  interface SlotMap {
+    /** Browser actions on a delivered file. */
+    'deliverables.file.action': { kind: 'list'; scope: 'session'; owner: { path: string; dismiss: () => void } }
+  }
   interface LocaleNamespaceMap {
     /** Changed-files card, review tab, delivery card, and file-mention copy. */
     'deliverables': DeliverablesKey
@@ -61,6 +66,7 @@ export function apply(ctx: ClientContext): void {
     () => ctx.slots.register({
       name: 'conversation.chat.turnTail',
       id: '@deepseek-ai/dsh-client-ui-deliverables',
+      children: { 'deliverables.file.action': { kind: 'list', scope: 'session' } },
       locale: NS,
       inject: (): DeliverablesInjected => ({
         hooks: { presentedOpen: opener.state, presentedHost: opener.host, changesSummary: summaries.state },
diff --git a/packages/client/ui-deliverables/src/client/locales.ts b/packages/client/ui-deliverables/src/client/locales.ts
index 7eacc52c34..a567d86213 100644
--- a/packages/client/ui-deliverables/src/client/locales.ts
+++ b/packages/client/ui-deliverables/src/client/locales.ts
@@ -20,7 +20,7 @@ export const zh = {
   'presented.finder': '在 Finder 中显示',
   'presented.defaultApp': '用默认应用打开',
   'presented.more': '{name} 的更多文件操作',
-  'presented.action': '打开',
+  'presented.action': '侧边预览',
   'presented.preview': '在侧边栏预览',
   'presented.previewButton': '在侧边栏打开 {name}',
   'presented.previewCard': '在侧边栏预览 {name}',
@@ -91,7 +91,7 @@ export const en: Record<DeliverablesKey, string> = {
   'presented.finder': 'Show in Finder',
   'presented.defaultApp': 'Open in default app',
   'presented.more': 'More file actions for {name}',
-  'presented.action': 'Open',
+  'presented.action': 'Sidebar preview',
   'presented.preview': 'Preview in sidebar',
   'presented.previewButton': 'Open {name} in sidebar',
   'presented.previewCard': 'Preview {name} in sidebar',
diff --git a/packages/client/ui-deliverables/tests/deliverables.client.spec.tsx b/packages/client/ui-deliverables/tests/deliverables.client.spec.tsx
index 8cff6f2850..c5b7ed58ed 100644
--- a/packages/client/ui-deliverables/tests/deliverables.client.spec.tsx
+++ b/packages/client/ui-deliverables/tests/deliverables.client.spec.tsx
@@ -40,6 +40,7 @@ function openProps(controller = new PresentedOpenController(), summaries = new C
   controller.host.set({ name: 'desktop', available: true, fileManager: 'finder' })
   const sessions: SessionListState = { ids: [], byId: {}, phase: 'ready', subagentsByParent: {}, jobsBySession: {} }
   return {
+    renderSlot: () => null,
     useSessions: <T,>(select: (state: SessionListState) => T): T => select(sessions),
     reloadPresentedHost: vi.fn(() => controller.loadHost()),
     useChangesSummary: <T,>(select: (state: ReturnType<typeof summaries.state.getSnapshot>) => T): T =>
@@ -836,8 +837,8 @@ describe('presented files', () => {
     expect(preview).toHaveBeenCalledTimes(2)
     expect(preview).toHaveBeenLastCalledWith('report-0.docx')
     fireEvent.click(view.getByRole('button', { name: 'More file actions for report-0.docx' }))
-    fireEvent.click(view.getByRole('menuitem', { name: 'Open in default app' }))
-    expect(props.openPresented).toHaveBeenCalledWith('child-session', 2, 0, 'open')
+    fireEvent.click(view.getByRole('menuitem', { name: 'Sidebar preview' }))
+    expect(preview).toHaveBeenCalledTimes(3)
     fireEvent.click(view.getByRole('button', { name: 'Collapse delivered files' }))
     expect(view.container.querySelectorAll('[data-presented-file]')).toHaveLength(4)
     expect(view.container.querySelector('[data-changed-files]')).toBeNull()
@@ -915,7 +916,7 @@ it.each(['opening', 'opened', 'error'] as const)('shows the %s state and permits
 })
 
 
-it('explains a missing desktop and retries failed Host metadata', () => {
+it('omits missing-desktop warnings and retries failed Host metadata', () => {
   const controller = new PresentedOpenController()
   const props = openProps(controller)
   const matched = { changes: null, presented: [{ path: 'file.txt', seq: 2, index: 0 }] }
@@ -926,7 +927,7 @@ it('explains a missing desktop and retries failed Host metadata', () => {
   expect(props.reloadPresentedHost).toHaveBeenCalledOnce()
   controller.host.set({ name: 'server', available: false, fileManager: null })
   view.rerender(<Deliverables {...props} matched={matched} openFile={() => {}} sessionId={SessionId('session')} t={makeTranslate(en)} />)
-  expect(view.getByText(en['presented.unavailable'])).toBeTruthy()
+  expect(view.queryByText(en['presented.unavailable'])).toBeNull()
 })
 
 
diff --git a/packages/client/ui-deliverables/tests/presented-file-card.client.spec.tsx b/packages/client/ui-deliverables/tests/presented-file-card.client.spec.tsx
index 28207b5a29..6b90bcae68 100644
--- a/packages/client/ui-deliverables/tests/presented-file-card.client.spec.tsx
+++ b/packages/client/ui-deliverables/tests/presented-file-card.client.spec.tsx
@@ -8,6 +8,7 @@ import { en, zh } from '../src/client/locales.ts'
 
 afterEach(cleanup)
 const props = () => ({
+  extra: (dismiss: () => void) => <button role="menuitem" onClick={dismiss}>Download</button>,
   cwd: undefined,
   file: { path: 'out/report.pdf', description: 'Final report', seq: 4, index: 1 },
   host: { name: 'remote-desktop', available: true, fileManager: 'finder' as const },
@@ -17,19 +18,14 @@ const props = () => ({
   t: makeTranslate(en),
 })
 
-it.each([
-  ['finder', 'Show in Finder'], ['explorer', 'Show in File Explorer'], ['directory', 'Open containing folder'],
-] as const)('uses the Host %s action and closes the menu after selection', (fileManager, label) => {
+it('offers sidebar preview and contributed actions without a desktop', () => {
   const p = props()
-  const view = render(<PresentedFileCard {...p} host={{ ...p.host, fileManager }} />)
+  const view = render(<PresentedFileCard {...p} host={null} />)
   fireEvent.click(view.getByRole('button', { name: 'More file actions for out/report.pdf' }))
-  fireEvent.click(view.getByRole('menuitem', { name: new RegExp(label) }))
-  expect(p.onAction).toHaveBeenCalledWith('reveal')
+  expect(view.getAllByRole('menuitem').map(item => item.textContent)).toEqual(['Sidebar preview', 'Download'])
+  fireEvent.click(view.getByRole('menuitem', { name: 'Sidebar preview' }))
+  expect(p.onPreview).toHaveBeenCalledOnce()
   expect(view.queryByRole('menu')).toBeNull()
-  fireEvent.click(view.getByRole('button', { name: 'More file actions for out/report.pdf' }))
-  fireEvent.click(view.getByRole('menuitem', { name: /Open in default app/ }))
-  expect(p.onAction).toHaveBeenLastCalledWith('open')
-  expect(p.onAction).toHaveBeenCalledTimes(2)
 })
 
 it('dismisses the menu with Escape or an outside click without launching anything', () => {
@@ -55,13 +51,13 @@ it.each(['opening', 'revealing'] as const)('keeps sidebar previews available whi
   expect(p.onAction).not.toHaveBeenCalled()
 })
 
-it('keeps the native menu disabled until a desktop is available', () => {
+it('keeps browser actions enabled without a desktop', () => {
   const p = props()
   const view = render(<PresentedFileCard {...p} host={null} />)
-  expect((view.getByRole('button', { name: 'More file actions for out/report.pdf' }) as HTMLButtonElement).disabled).toBe(true)
+  expect((view.getByRole('button', { name: 'More file actions for out/report.pdf' }) as HTMLButtonElement).disabled).toBe(false)
   expect((view.getByRole('button', { name: 'Open out/report.pdf in sidebar' }) as HTMLButtonElement).disabled).toBe(false)
   view.rerender(<PresentedFileCard {...p} host={{ ...p.host, available: false, fileManager: null }} />)
-  expect((view.getByRole('button', { name: 'More file actions for out/report.pdf' }) as HTMLButtonElement).disabled).toBe(true)
+  expect((view.getByRole('button', { name: 'More file actions for out/report.pdf' }) as HTMLButtonElement).disabled).toBe(false)
 })
 
 it('opens the right sidebar from either the card or its primary button', () => {
@@ -112,8 +108,8 @@ it('shows the basename while retaining the full location for hover and actions',
   expect(view.getByTitle(path)).toBeTruthy()
   expect(view.getByText('result.pdf')).toBeTruthy()
   fireEvent.click(view.getByRole('button', { name: `More file actions for ${path}` }))
-  fireEvent.click(view.getByRole('menuitem', { name: 'Open in default app' }))
-  expect(p.onAction).toHaveBeenCalledWith('open')
+  fireEvent.click(view.getByRole('menuitem', { name: 'Sidebar preview' }))
+  expect(p.onPreview).toHaveBeenCalledOnce()
   view.rerender(<PresentedFileCard {...p} cwd="/work" />)
   expect(view.getByTitle('/work/out/report.pdf')).toBeTruthy()
   expect(view.getByText('report.pdf')).toBeTruthy()
@@ -141,11 +137,11 @@ it('does not reopen a menu after a shared native request settles', () => {
   expect(view.queryByRole('menu')).toBeNull()
 })
 
-it('keeps focus on the available preview button after selecting a native action', () => {
+it('keeps focus on the available preview button after selecting a sidebar preview', () => {
   const p = props()
   const view = render(<PresentedFileCard {...p} />)
   fireEvent.click(view.getByRole('button', { name: 'More file actions for out/report.pdf' }))
-  const item = view.getByRole('menuitem', { name: 'Show in Finder' })
+  const item = view.getByRole('menuitem', { name: 'Sidebar preview' })
   item.focus()
   fireEvent.click(item)
   view.rerender(<PresentedFileCard {...p} phase="revealing" />)
diff --git a/packages/client/ui-layout/README.i18n.yaml b/packages/client/ui-layout/README.i18n.yaml
index 07dc90eb4c..8e96ac9aa4 100644
--- a/packages/client/ui-layout/README.i18n.yaml
+++ b/packages/client/ui-layout/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-layout/README.md
-README.md: 582c5fbea271caf0f45f9ee22c10d21f7412bf38
-README.zh.md: 14e3f429284c5c3a519fa47e1156a510b7271f91
+README.md: 83fb74d8169bb4b256d0a6f6955589f9ad28bb83
+README.zh.md: 36c1890c7953f2cd325fd2c26c7c904ffe219782
diff --git a/packages/client/ui-layout/README.md b/packages/client/ui-layout/README.md
index 582c5fbea2..83fb74d816 100644
--- a/packages/client/ui-layout/README.md
+++ b/packages/client/ui-layout/README.md
@@ -9,7 +9,7 @@ English | [中文](README.zh.md)
 
 ## Summary
 
-This package provides the Web GUI's three-column AppFrame, edge-column widths, and `ctx.layout` presentation control. The right column concedes space before the center; its occupant renders fullscreen while the frame retains the wide-screen track underneath. The theme presenter owns color scheme, alias tokens, content font size, and document metadata. Layout state resets on reload.
+This package provides the Web GUI's three-column AppFrame, edge-column widths, and `ctx.layout` presentation control. The right column concedes space before the center; its occupant renders fullscreen while the frame retains the wide-screen track underneath. The theme presenter owns color scheme, alias tokens, content font size, and document metadata. Layout state resets on reload. The frame exposes data-app-frame, data-sidebar-col, and data-center-col attributes for optional responsive layout plugins.
 
 ## Table of Contents
 
@@ -25,7 +25,7 @@ This package provides the Web GUI's three-column AppFrame, edge-column widths, a
 <a id="use-this-package"></a>
 ## Use this package
 
-The root slot composes the sidebar, main content, and right column. The sidebar spans 264–420px, defaults to 280px, and retains a 56px rail when collapsed; below 1024px it collapses automatically, and opening the right panel collapses a manually expanded sidebar. The right panel first opens at 45% of the viewport, then retains the user's pixel preference, capped at 70%. To protect 400px for the center, the frame first reduces the right panel to 300px, then reports insufficient room so its occupant closes it, and only then compresses the center further. Dragging has no transition delay; the right handle is absent while closed or fullscreen.
+The root slot composes the sidebar, main content, and right column. The sidebar spans 264–420px, defaults to 264px, and retains a 56px rail when collapsed; below 1024px it collapses automatically, and opening the right panel collapses a manually expanded sidebar. The right panel first opens at 400px, then retains the user's pixel preference, capped at 70%. To protect 400px for the center, the frame first reduces the right panel to 300px, then reports insufficient room so its occupant closes it, and only then compresses the center further. Dragging has no transition delay; the right handle is absent while closed or fullscreen.
 
 Global panels occupy the root-scoped `main` keyed slot; `conversation` is the reserved key for the Conversation. `ctx.layout.selectPanel(id)` selects a registered panel, and `null` selects the Conversation without changing the current Session. No global panel is registered by the shipped composition.
 
diff --git a/packages/client/ui-layout/README.zh.md b/packages/client/ui-layout/README.zh.md
index 14e3f42928..36c1890c79 100644
--- a/packages/client/ui-layout/README.zh.md
+++ b/packages/client/ui-layout/README.zh.md
@@ -9,7 +9,7 @@ kind: "package-reference"
 
 ## 概述
 
-本包提供 Web GUI 的三栏 AppFrame、左右栏宽度与 `ctx.layout` 呈现控制。右栏先让步以保护中栏空间，全屏由占用方呈现，框架保留宽屏底层轨道。主题呈现器负责配色、别名 token、正文字号与 document 元数据；布局状态在刷新后重置。
+本包提供 Web GUI 的三栏 AppFrame、左右栏宽度与 `ctx.layout` 呈现控制。右栏先让步以保护中栏空间，全屏由占用方呈现，框架保留宽屏底层轨道。主题呈现器负责配色、别名 token、正文字号与 document 元数据；布局状态在刷新后重置。 框架提供 data-app-frame、data-sidebar-col 与 data-center-col 属性，供可选的响应式布局插件定位。
 
 ## 目录
 
@@ -25,7 +25,7 @@ kind: "package-reference"
 <a id="use-this-package"></a>
 ## 使用本包
 
-本插件在 root slot 中组合侧边栏、主内容和右栏。侧边栏宽度为 264～420px，默认为 280px，收起后保留 56px 控制栏；窗口宽度低于 1024px 时自动收起，打开右侧面板也会收起手动展开的侧边栏。右侧面板首次打开时使用视口宽度的 45%，之后保留用户的像素宽度偏好，上限为 70%。为给中栏保留 400px，框架先将右侧面板缩减至 300px，再报告空间不足，使占用方将其关闭，最后才进一步压缩中栏。拖动没有过渡延迟；右侧手柄在关闭或全屏时不显示。
+本插件在 root slot 中组合侧边栏、主内容和右栏。侧边栏宽度为 264～420px，默认为 264px，收起后保留 56px 控制栏；窗口宽度低于 1024px 时自动收起，打开右侧面板也会收起手动展开的侧边栏。右侧面板首次打开时使用 400px，之后保留用户的像素宽度偏好，上限为 70%。为给中栏保留 400px，框架先将右侧面板缩减至 300px，再报告空间不足，使占用方将其关闭，最后才进一步压缩中栏。拖动没有过渡延迟；右侧手柄在关闭或全屏时不显示。
 
 全局面板占据 root 作用域的 `main` keyed slot；`conversation` 是为会话界面保留的 key。`ctx.layout.selectPanel(id)` 选中已注册面板，`null` 则选中会话界面，但不改变当前会话。默认组合不注册任何全局面板。
 
diff --git a/packages/client/ui-layout/src/client/AppFrame.tsx b/packages/client/ui-layout/src/client/AppFrame.tsx
index 323318f3e1..3a4322f18e 100644
--- a/packages/client/ui-layout/src/client/AppFrame.tsx
+++ b/packages/client/ui-layout/src/client/AppFrame.tsx
@@ -19,7 +19,7 @@ import type { ReactNode } from 'react'
 import type {
   PropsLocale, PropsRenderSlots, PropsRuntime, PropsStore,
 } from '@deepseek-ai/dsh-client-ui-slots'
-import { computeColumns, RIGHTBAR_DEFAULT_RATIO, SIDEBAR_AUTO_COLLAPSE, SIDEBAR_COLLAPSED, SIDEBAR_DEFAULT } from './columns.ts'
+import { computeColumns, RIGHTBAR_DEFAULT_WIDTH, SIDEBAR_AUTO_COLLAPSE, SIDEBAR_COLLAPSED, SIDEBAR_DEFAULT } from './columns.ts'
 import { DocumentTitle } from './DocumentTitle.tsx'
 import type { createLayoutStore } from './stores.ts'
 import css from './AppFrame.module.css'
@@ -33,7 +33,7 @@ export type AppFrameProps =
 
 /** Center column grid item (session-body building block). */
 function CenterColumn(props: { children?: ReactNode }) {
-  return <div className={css.centerCol}>{props.children}</div>
+  return <div className={css.centerCol} data-center-col>{props.children}</div>
 }
 
 /** Subscribe to the main key without subscribing the column frame to each panel id. */
@@ -162,7 +162,7 @@ export function AppFrame({
   const sidebarPreference = sidebarCollapsed
     ? 0
     : layoutInfo.sidebar === 0 ? SIDEBAR_DEFAULT : layoutInfo.sidebar
-  const rightbarPreference = layoutInfo.rightbar ?? viewport * RIGHTBAR_DEFAULT_RATIO
+  const rightbarPreference = layoutInfo.rightbar ?? RIGHTBAR_DEFAULT_WIDTH
   // Desktop reopen controls occupy the macOS session header or Windows caption row.
   const collapsedWidth = document.documentElement.dataset.platform === 'darwin'
     || document.documentElement.hasAttribute('data-windows-titlebar') ? 0 : SIDEBAR_COLLAPSED
@@ -205,6 +205,7 @@ export function AppFrame({
   return (
     <div
       ref={frameRef}
+      data-app-frame
       className={css.frame}
       style={{
         ...(document.documentElement.hasAttribute('data-windows-titlebar')
@@ -223,7 +224,7 @@ export function AppFrame({
         useSessions={useSessions}
         usePanelInfo={usePanelInfo}
       />
-      <div className={css.sidebarCol}>
+      <div className={css.sidebarCol} data-sidebar-col>
         {sidebar}
       </div>
       <>
diff --git a/packages/client/ui-layout/src/client/columns.ts b/packages/client/ui-layout/src/client/columns.ts
index b5ffd9f732..b76387b4d7 100644
--- a/packages/client/ui-layout/src/client/columns.ts
+++ b/packages/client/ui-layout/src/client/columns.ts
@@ -14,7 +14,7 @@ export const SIDEBAR_MIN = 264
 /** Sidebar drag clamp ceiling. */
 export const SIDEBAR_MAX = 420
 /** Sidebar width before any user drag. */
-export const SIDEBAR_DEFAULT = 280
+export const SIDEBAR_DEFAULT = SIDEBAR_MIN
 /** Closed-sidebar rail: a 24px icon column between 16px horizontal paddings. */
 export const SIDEBAR_COLLAPSED = 56
 /** Viewport width below which the sidebar auto-collapses to the rail (deepsuite
@@ -25,8 +25,8 @@ export const SIDEBAR_AUTO_COLLAPSE = 1024
 export const RIGHTBAR_MIN = 300
 /** Maximum normal right panel width as a fraction of the frame. */
 export const RIGHTBAR_MAX_RATIO = 0.7
-/** First-open right panel preference as a fraction of the frame. */
-export const RIGHTBAR_DEFAULT_RATIO = 0.45
+/** First-open right panel width in pixels. */
+export const RIGHTBAR_DEFAULT_WIDTH = 400
 
 /**
  * Clamp a panel width into its contract range.
diff --git a/packages/client/ui-layout/src/client/stores.ts b/packages/client/ui-layout/src/client/stores.ts
index 3a53caf4f1..4b49cc69df 100644
--- a/packages/client/ui-layout/src/client/stores.ts
+++ b/packages/client/ui-layout/src/client/stores.ts
@@ -5,7 +5,7 @@
 import { defineStore, type EngineStoreHandle } from '@deepseek-ai/dsh-client-store'
 import type { MainPanelId } from './service.ts'
 import {
-  clampWidth, RIGHTBAR_DEFAULT_RATIO, RIGHTBAR_MAX_RATIO, RIGHTBAR_MIN,
+  clampWidth, RIGHTBAR_DEFAULT_WIDTH, RIGHTBAR_MAX_RATIO, RIGHTBAR_MIN,
   SIDEBAR_AUTO_COLLAPSE, SIDEBAR_DEFAULT, SIDEBAR_MAX, SIDEBAR_MIN,
 } from './columns.ts'
 
@@ -129,7 +129,7 @@ export function createLayoutStore(): EngineStoreHandle<LayoutState, LayoutAction
           d.layoutInfo.rightbarInstant = d.layoutInfo.rightbarFullscreen && !fullscreen
         }
         if (!d.layoutInfo.rightbarShown && d.layoutInfo.viewportWidth < SIDEBAR_AUTO_COLLAPSE) d.layoutInfo.narrowExpanded = false
-        d.layoutInfo.rightbar ??= Math.max(RIGHTBAR_MIN, Math.round(d.layoutInfo.viewportWidth * RIGHTBAR_DEFAULT_RATIO))
+        d.layoutInfo.rightbar ??= RIGHTBAR_DEFAULT_WIDTH
         d.layoutInfo.rightbarShown = true
         d.layoutInfo.rightbarTrack = track
         d.layoutInfo.rightbarFullscreen = fullscreen
diff --git a/packages/client/ui-layout/tests/app-frame.client.spec.tsx b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
index b7bf4e10e1..0e03dadc9a 100644
--- a/packages/client/ui-layout/tests/app-frame.client.spec.tsx
+++ b/packages/client/ui-layout/tests/app-frame.client.spec.tsx
@@ -60,9 +60,11 @@ function resize(width: number): void {
   })
 }
 
-function mountFrame(windowWidth = frameWidth) {
+function mountFrame(windowWidth = frameWidth, initialWidths: { sidebar: number; rightbar: number | null } = { sidebar: 280, rightbar: Math.round(frameWidth * 0.45) }) {
   vi.stubGlobal('innerWidth', windowWidth)
   const instance = createLayoutStore().create()
+  instance.actions.setSidebar(initialWidths.sidebar)
+  if (initialWidths.rightbar !== null) instance.actions.setRightbar(initialWidths.rightbar)
   const slotCalls: { key: string; props: object; options: RenderOpts | undefined }[] = []
   const renderSlot: AppFrameProps['renderSlot'] = (key, owner, options) => {
     slotCalls.push({ key, props: owner, options })
@@ -196,7 +198,7 @@ describe('AppFrame', () => {
     expect(document.title).toBe('Product')
   })
 
-  it('renders owner props for the default sidebar and prospective right panel', () => {
+  it('renders owner props for selected sidebar and prospective right panel', () => {
     const { frame, rightOwner, sidebarOwner, slotCalls } = mountFrame()
     expect(tracks(frame)).toEqual([280, 0])
     expect(sidebarOwner()).toEqual({ collapsed: false, width: 280 })
@@ -262,7 +264,7 @@ describe('AppFrame', () => {
 })
 
 describe('AppFrame normal width concessions', () => {
-  it('measures the frame, not the window, before choosing the first-open preference', () => {
+  it('measures the frame, not the window, when resolving panel geometry', () => {
     frameWidth = 1000
     const { instance, rightOwner } = mountFrame(1920)
     expect(instance.getSnapshot().layoutInfo.viewportWidth).toBe(1000)
@@ -341,7 +343,7 @@ describe('AppFrame normal width concessions', () => {
     act(() => { instance.actions.toggleSidebar() })
     resize(980)
     act(() => { instance.actions.toggleSidebar() })
-    expect(tracks(frame)[0]).toBe(280)
+    expect(tracks(frame)[0]).toBe(264)
     expect(instance.getSnapshot().layoutInfo.sidebar).toBe(0)
   })
 })
@@ -565,7 +567,7 @@ describe('AppFrame pointer resizing', () => {
 
 describe('AppFrame frame measurement lifecycle', () => {
   it('coalesces observer reports and publishes the latest frame measurement', () => {
-    const { instance, rightOwner } = mountFrame()
+    const { instance, rightOwner } = mountFrame(frameWidth, { sidebar: 264, rightbar: null })
     const observer = observers.at(-1)!
     act(() => {
       frameWidth = 900
@@ -602,3 +604,10 @@ describe('AppFrame frame measurement lifecycle', () => {
     expect(animationFrames.size).toBe(0)
   })
 })
+
+it('starts with a minimum-width sidebar and a collapsed 400px right panel', () => {
+  const instance = createLayoutStore().create()
+  expect(instance.getSnapshot().layoutInfo).toMatchObject({ sidebar: 264, rightbarShown: false })
+  instance.actions.openRightbar(true, false)
+  expect(instance.getSnapshot().layoutInfo.rightbar).toBe(400)
+})
diff --git a/packages/client/ui-primitives/README.i18n.yaml b/packages/client/ui-primitives/README.i18n.yaml
index e7dd304b2f..56ed582479 100644
--- a/packages/client/ui-primitives/README.i18n.yaml
+++ b/packages/client/ui-primitives/README.i18n.yaml
@@ -1,6 +1,5 @@
-# Bilingual-pair consistency record (docs/i18n/README.md): the git blob hash of each
-# side as of the last confirmed-consistent state. Both languages carry equal authority;
-# after editing either side, bring the other along and re-record with:
-#   pnpm run verify-translation-pairing --write packages/client/ui-primitives/README.md
-README.md: cefa67e49146d5fc2976d3d43b8ae39c3d4b99f6
-README.zh.md: 3c069d05b2948e8494cef934f87270e86a14d978
+# Bilingual-pair consistency record (docs/i18n/README.md).
+# Both languages carry equal authority.
+# Re-record with: pnpm run verify-translation-pairing --write packages/client/ui-primitives/README.md
+README.md: 157a88e3c8fccb6a552cd260f406c9a82fc6331d
+README.zh.md: 2def9ddc537efb89ce8dbb2bc73193a8a70fa334
diff --git a/packages/client/ui-primitives/README.md b/packages/client/ui-primitives/README.md
index cefa67e491..157a88e3c8 100644
--- a/packages/client/ui-primitives/README.md
+++ b/packages/client/ui-primitives/README.md
@@ -25,6 +25,10 @@ Use `dsh-client-ui-primitives` to build web-client controls and render agent out
 <a id="use-this-package"></a>
 ## Use this package
 
+`Menu.extra` appends caller-owned menu controls after standard items; callers provide localized labels and menuitem roles. Nested links and buttons share row styling and keyboard navigation.
+
+Bottom-anchored overlays limit their height to the viewport and clipping ancestors, keeping the menu's top edge visible inside scroll containers.
+
 This package is a Web-shell build input. Its static ESM retains third-party imports and styles for Vite; independent consumers supply its development dependencies ([dependency rules](../AGENTS.md#dependency-declaration)).
 
 Compose feature UI from these atoms whenever the web client needs a standard control or an agent-output renderer. They render through React only and take `--dsw-*` design tokens from the theme, so they fit any plugin without importing the theme or the slot system.
@@ -170,3 +174,5 @@ None.
 </details>
 
 **Runtime invariant:** No companion is published. Pure props-in React atoms with no Cordis API — no events, no services, no mutable cross-plugin state; rendering contracts are asserted directly by this package's component specs.
+
+FileTypeIcon exposes its category through data-file-icon-kind for skin artwork. Tooltip placement uses measured dimensions without transforms, preserving viewport-aligned wallpaper backgrounds.
diff --git a/packages/client/ui-primitives/README.zh.md b/packages/client/ui-primitives/README.zh.md
index 3c069d05b2..2def9ddc53 100644
--- a/packages/client/ui-primitives/README.zh.md
+++ b/packages/client/ui-primitives/README.zh.md
@@ -25,6 +25,10 @@ kind: "package-library"
 <a id="use-this-package"></a>
 ## 使用本包
 
+`Menu.extra` 在标准条目后追加调用方拥有的菜单控件；调用方提供本地化标签及 menuitem 角色。嵌套的链接和按钮共享菜单行样式及键盘导航。
+
+底部锚定浮层根据视口及裁切祖先限制高度，使菜单顶部在滚动容器中完整显示。
+
 本包是 Web 壳的构建输入。静态 ESM 为 Vite 保留第三方导入和样式；独立消费方自行提供开发依赖（[依赖规则](../AGENTS.md#dependency-declaration)）。
 
 只要 Web 客户端需要标准控件或 agent 输出渲染器，就用这些原子组件拼装功能 UI。它们只经 React 渲染，并从主题取得 `--dsw-*` 设计 token，因此无需导入主题或 slot 系统即可适配任意插件。
@@ -170,3 +174,5 @@ kind: "package-library"
 </details>
 
 **运行时不变式：** 不发布伴生入口。这些是纯 props-in React atom，没有 Cordis API、事件、service 或跨插件可变状态；渲染约定由组件测试覆盖。
+
+FileTypeIcon 通过 data-file-icon-kind 标识文件类型，允许皮肤替换图标。Tooltip 按实际尺寸定位，不使用位移变换，壁纸背景保持视口对齐。
diff --git a/packages/client/ui-primitives/src/FileTypeIcon.tsx b/packages/client/ui-primitives/src/FileTypeIcon.tsx
index 0a88317401..767f1084b7 100644
--- a/packages/client/ui-primitives/src/FileTypeIcon.tsx
+++ b/packages/client/ui-primitives/src/FileTypeIcon.tsx
@@ -254,7 +254,7 @@ function assertNever(value: never): never {
 /**
  * Render a decorative file-type glyph for a path or an explicitly resolved kind.
  * @param props - Path or kind selection, optional project context, size, and CSS class.
- * @returns The category-colored SVG; the caller owns the accessible name and may override
+ * @returns The category-marked icon wrapper; the caller owns the accessible name and may override
  * the color through `--dsh-file-type-icon-color`.
  */
 export function FileTypeIcon(props: FileTypeIconProps): ReactNode {
@@ -262,7 +262,7 @@ export function FileTypeIcon(props: FileTypeIconProps): ReactNode {
   const resolvedType = 'path' in props
     ? classifyFileType(props.path, props.context)
     : props.kind
-  return isCodeFileType(resolvedType)
+  return <span data-file-icon-kind={resolvedType} style={{ display: 'inline-flex', width: size, height: size, flexShrink: 0 }}>{isCodeFileType(resolvedType)
     ? <CodeFileIcon type={resolvedType} size={size} className={className} />
-    : glyph(resolvedType, size, clsx(css.icon, css[resolvedType], className))
+    : glyph(resolvedType, size, clsx(css.icon, css[resolvedType], className))}</span>
 }
diff --git a/packages/client/ui-primitives/src/Menu.module.css b/packages/client/ui-primitives/src/Menu.module.css
index 0525f2c6e4..686e6acccd 100644
--- a/packages/client/ui-primitives/src/Menu.module.css
+++ b/packages/client/ui-primitives/src/Menu.module.css
@@ -95,10 +95,12 @@
 
 /* Menu cell (figma .Menu_cell): min-h 40, r10, pad 10/8, 14/22 primary,
  * gap 8 between leading icon / label / trailing check. */
-.item {
+.item,
+.viewport [role='menuitem'] {
   display: flex;
   align-items: center;
   gap: 8px;
+  box-sizing: border-box;
   width: 100%;
   min-height: 40px;
   padding: 8px 10px;
@@ -110,21 +112,26 @@
   line-height: 22px;
   color: var(--dsw-alias-label-primary);
   text-align: left;
+  text-decoration: none;
+  justify-content: flex-start;
 }
 
-.item:hover:not(:disabled) {
+.item:hover:not(:disabled),
+.viewport [role='menuitem']:hover:not(:disabled) {
   background: var(--dsw-alias-interactive-bg-hover);
 }
 
 /* Arrow navigation moves real focus, so the row the keyboard is on carries the
    same fill the pointer gets: the fill is the row's focus indication, and the
    browser's default ring would double it. */
-.item:focus-visible:not(:disabled) {
+.item:focus-visible:not(:disabled),
+.viewport [role='menuitem']:focus-visible:not(:disabled) {
   background: var(--dsw-alias-interactive-bg-hover);
   outline: none;
 }
 
-.denseList .item {
+.denseList .item,
+.denseList .viewport [role='menuitem'] {
   min-height: 34px;
   padding-block: 5px;
 }
@@ -140,7 +147,8 @@
   border-radius: 7px;
 }
 
-.compactList .item {
+.compactList .item,
+.compactList .viewport [role='menuitem'] {
   min-height: 26px;
   gap: 6px;
   padding: 3px 7px;
@@ -164,7 +172,8 @@
   line-height: 16px;
 }
 
-.item:disabled {
+.item:disabled,
+.viewport [role='menuitem']:disabled {
   opacity: 0.4;
   cursor: not-allowed;
 }
diff --git a/packages/client/ui-primitives/src/Menu.tsx b/packages/client/ui-primitives/src/Menu.tsx
index bb04912293..75b8eabdc6 100644
--- a/packages/client/ui-primitives/src/Menu.tsx
+++ b/packages/client/ui-primitives/src/Menu.tsx
@@ -57,6 +57,7 @@ const MEASURE_STYLE: CSSProperties = { visibility: 'hidden', left: 0, top: 0 }
  * @param props.open - whether the list is showing (owner-controlled).
  * @param props.anchor - the trigger element (rendered in place).
  * @param props.items - selectable rows and optional separators.
+ * @param props.extra - Feature-owned menu items appended after the standard rows.
  * @param props.selectedId - row shown as selected.
  * @param props.selectedIds - rows shown as selected when a menu contains independent option groups.
  * @param props.onSelect - row click callback (not called for disabled rows or submenu parents that only open children).
@@ -89,11 +90,12 @@ const MEASURE_STYLE: CSSProperties = { visibility: 'hidden', left: 0, top: 0 }
  * crowds the cell).
  * @returns anchor wrapper with the conditional list.
  */
-export function Menu({ open, anchor, items, selectedId, selectedIds, onSelect, onClose, align = 'start', side = 'bottom', portal = false, closeOnPointerLeave = false, dense = false, compact = false, autoFocus = false, selection = 'check', getAnchorRect, footer, className }: {
+export function Menu({ open, anchor, items, extra, selectedId, selectedIds, onSelect, onClose, align = 'start', side = 'bottom', portal = false, closeOnPointerLeave = false, dense = false, compact = false, autoFocus = false, selection = 'check', getAnchorRect, footer, className }: {
   open: boolean
   autoFocus?: boolean
   anchor: ReactNode
   items: readonly MenuEntry[]
+  extra?: ReactNode
   footer?: readonly MenuEntry[]
   selectedId?: string | undefined
   selectedIds?: readonly string[] | undefined
@@ -223,7 +225,7 @@ export function Menu({ open, anchor, items, selectedId, selectedIds, onSelect, o
 
   useEffect(() => {
     if (!open || !autoFocus) return
-    const first = listRef.current?.querySelector<HTMLButtonElement>('button:not(:disabled)')
+    const first = listRef.current?.querySelector<HTMLElement>('[role=menuitem]:not(:disabled):not([aria-disabled=true])')
     walkIndex.current = first === undefined || first === null ? null : 0
     first?.focus()
   }, [open, autoFocus])
@@ -278,7 +280,7 @@ export function Menu({ open, anchor, items, selectedId, selectedIds, onSelect, o
           }
           return
         }
-        const row = list.querySelector<HTMLButtonElement>('button:not(:disabled)')
+        const row = list.querySelector<HTMLElement>('[role=menuitem]:not(:disabled):not([aria-disabled=true])')
         if (row === null) return
         e.preventDefault()
         row.focus()
@@ -296,7 +298,7 @@ export function Menu({ open, anchor, items, selectedId, selectedIds, onSelect, o
       if (!['ArrowDown', 'ArrowUp', 'Home', 'End'].includes(e.key)) return
       const list = listRef.current
       if (list === null || !anchored) return
-      const buttons = Array.from(list.querySelectorAll<HTMLButtonElement>('button:not(:disabled)'))
+      const buttons = Array.from(list.querySelectorAll<HTMLElement>('[role=menuitem]:not(:disabled):not([aria-disabled=true])'))
       if (buttons.length === 0) return
       const index = buttons.indexOf(focused as HTMLButtonElement)
       const from = index >= 0 ? index : walkIndex.current
@@ -414,6 +416,7 @@ export function Menu({ open, anchor, items, selectedId, selectedIds, onSelect, o
     >
       <div className={css.viewport} role="presentation">
         {items.map(renderEntry)}
+        {extra}
       </div>
       {footer !== undefined && footer.length > 0 && (
         <div className={css.footer} role="presentation">
diff --git a/packages/client/ui-primitives/src/Tooltip.tsx b/packages/client/ui-primitives/src/Tooltip.tsx
index 41baf5b785..dc80a49767 100644
--- a/packages/client/ui-primitives/src/Tooltip.tsx
+++ b/packages/client/ui-primitives/src/Tooltip.tsx
@@ -77,12 +77,15 @@ export function Tooltip({ label, side = 'right', delayMs = 0, disabled = false,
       const el = bubble.current
       /* v8 ignore next -- pos is set only while the bubble is mounted. */
       if (el === null) return
-      el.style.left = `${pos.x}px`
+      el.style.transform = 'none'
+      const x = pos.x - (placement === 'right' ? 0 : el.offsetWidth / 2)
+      el.style.top = `${y - (placement === 'top' ? el.offsetHeight : placement === 'right' ? el.offsetHeight / 2 : 0)}px`
+      el.style.left = `${x}px`
       const r = el.getBoundingClientRect()
       let dx = 0
       if (r.right > window.innerWidth - EDGE_MARGIN) dx = window.innerWidth - EDGE_MARGIN - r.right
       if (r.left + dx < EDGE_MARGIN) dx = EDGE_MARGIN - r.left
-      el.style.left = `${pos.x + dx}px`
+      el.style.left = `${x + dx}px`
       if (side === 'right') return
       // Flip only into a side that genuinely fits, so an anchor with room on
       // neither side keeps the requested placement instead of oscillating.
@@ -94,7 +97,7 @@ export function Tooltip({ label, side = 'right', delayMs = 0, disabled = false,
     fit()
     window.addEventListener('resize', fit)
     return () => { window.removeEventListener('resize', fit) }
-  }, [placement, pos, resolvedLabel, side])
+  }, [placement, pos, resolvedLabel, side, y])
   const showTimer = useRef<ReturnType<typeof setTimeout> | null>(null)
   // Hover and focus are independent triggers: the bubble hides only after
   // BOTH clear (hovering away from a focused anchor must not drop it).
diff --git a/packages/client/ui-primitives/src/useAnchoredMaxHeight.ts b/packages/client/ui-primitives/src/useAnchoredMaxHeight.ts
index 5f3bc52bab..73194135ba 100644
--- a/packages/client/ui-primitives/src/useAnchoredMaxHeight.ts
+++ b/packages/client/ui-primitives/src/useAnchoredMaxHeight.ts
@@ -1,8 +1,7 @@
 /**
  * Viewport-fit hook for bottom-anchored overlays (slash menu, popupSelect):
  * the element's bottom edge is laid out independent of its height, so it
- * grows upward and only the top edge can collide with the viewport — clamp
- * the design cap to the space between that edge and the viewport top.
+ * grows upward. Clipping ancestors and the viewport bound the available space.
  */
 import { useLayoutEffect, useState } from 'react'
 import type { RefObject } from 'react'
@@ -11,7 +10,7 @@ import type { RefObject } from 'react'
 const MARGIN = 12
 
 /**
- * Clamp a bottom-anchored overlay's max-height to the viewport.
+ * Clamp a bottom-anchored overlay's max-height to its visible ancestor area.
  * @param ref - the overlay element; a null current (overlay closed) skips measuring.
  * @param cap - design max-height in px (the clamp never exceeds it).
  * @param signal - re-measure trigger: pass the overlay's render state so anchor
@@ -24,7 +23,13 @@ export function useAnchoredMaxHeight(ref: RefObject<HTMLElement>, cap: number, s
     const el = ref.current
     if (el === null) return
     const fit = () => {
-      setMaxHeight(Math.min(cap, Math.max(0, el.getBoundingClientRect().bottom - MARGIN)))
+      let top = 0
+      for (let parent = el.parentElement; parent !== null; parent = parent.parentElement) {
+        if (/(auto|scroll|hidden|clip)/.test(getComputedStyle(parent).overflowY)) {
+          top = Math.max(top, parent.getBoundingClientRect().top + parent.clientTop)
+        }
+      }
+      setMaxHeight(Math.min(cap, Math.max(0, el.getBoundingClientRect().bottom - top - MARGIN)))
     }
     fit()
     window.addEventListener('resize', fit)
diff --git a/packages/client/ui-primitives/tests/atoms.client.spec.tsx b/packages/client/ui-primitives/tests/atoms.client.spec.tsx
index a2f6b70fb9..d2757a6a28 100644
--- a/packages/client/ui-primitives/tests/atoms.client.spec.tsx
+++ b/packages/client/ui-primitives/tests/atoms.client.spec.tsx
@@ -60,6 +60,18 @@ describe('Input', () => {
 })
 
 describe('Menu', () => {
+  it('includes feature-owned links and nested buttons in keyboard navigation', () => {
+    render(<Menu open autoFocus anchor={<button>trigger</button>}
+      items={[{ id: 'preview', label: 'Preview' }]}
+      extra={<div><a role="menuitem" href="#file">Open tab</a><button role="menuitem">Edit</button><a role="menuitem" href="#download">Download</a></div>}
+      onSelect={() => {}} onClose={() => {}} />)
+    expect(document.activeElement).toBe(screen.getByRole('menuitem', { name: 'Preview' }))
+    for (const name of ['Open tab', 'Edit', 'Download', 'Preview']) {
+      fireEvent.keyDown(document, { key: 'ArrowDown' })
+      expect(document.activeElement).toBe(screen.getByRole('menuitem', { name }))
+    }
+  })
+
   const items = [
     { id: 'a', label: 'Alpha' },
     { id: 'b', label: 'Beta', disabled: true },
diff --git a/packages/client/ui-settings-unarchive-sessions/README.i18n.yaml b/packages/client/ui-settings-unarchive-sessions/README.i18n.yaml
index 2a027aaa3f..daa576a122 100644
--- a/packages/client/ui-settings-unarchive-sessions/README.i18n.yaml
+++ b/packages/client/ui-settings-unarchive-sessions/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-settings-unarchive-sessions/README.md
-README.md: 88a5835c2bf2a2e4799ee02e94985db9162e4ef0
-README.zh.md: 9e2a907d75a84a4e3262fc157023d26f93919a5d
+README.md: f97b8cc75eb3eb76de4c3b5ce5045fd0e2aef816
+README.zh.md: fb38c2119248d2ec61b2e2cacd75489a0d88668e
diff --git a/packages/client/ui-settings-unarchive-sessions/README.md b/packages/client/ui-settings-unarchive-sessions/README.md
index 88a5835c2b..f97b8cc75e 100644
--- a/packages/client/ui-settings-unarchive-sessions/README.md
+++ b/packages/client/ui-settings-unarchive-sessions/README.md
@@ -25,6 +25,8 @@ The **Archived sessions** Settings page is the restore point for sessions hidden
 <a id="use-this-package"></a>
 ## Use this package
 
+Each row exposes the root-scoped `settings.archivedSessions.action` list slot with the target Session id and title.
+
 Open Settings and select **Archived sessions** to see the sessions currently hidden from every grouping surface. Mount `@deepseek-ai/dsh-client-ui-settings-unarchive-sessions` in a Web composition that already provides the settings shell, the Workspace service, and the Session list; the page registers its own navigation entry and needs no configuration.
 
 ### Reading a row
diff --git a/packages/client/ui-settings-unarchive-sessions/README.zh.md b/packages/client/ui-settings-unarchive-sessions/README.zh.md
index 9e2a907d75..fb38c21192 100644
--- a/packages/client/ui-settings-unarchive-sessions/README.zh.md
+++ b/packages/client/ui-settings-unarchive-sessions/README.zh.md
@@ -25,6 +25,8 @@ kind: "package-reference"
 <a id="use-this-package"></a>
 ## 使用本包
 
+每行通过根作用域列表插槽 `settings.archivedSessions.action` 提供目标对话标识及标题。
+
 打开设置并选择**已归档会话**，即可看到当前从所有分组视图中隐藏的会话。在已提供设置外壳、Workspace 服务与 Session 列表的 Web 组合中挂载 `@deepseek-ai/dsh-client-ui-settings-unarchive-sessions`；该页面注册自己的导航条目，无需配置。
 
 ### 阅读一行
diff --git a/packages/client/ui-settings-unarchive-sessions/src/client/ArchivedSessionsSection.tsx b/packages/client/ui-settings-unarchive-sessions/src/client/ArchivedSessionsSection.tsx
index 889b1aa843..37a0b1ff6a 100644
--- a/packages/client/ui-settings-unarchive-sessions/src/client/ArchivedSessionsSection.tsx
+++ b/packages/client/ui-settings-unarchive-sessions/src/client/ArchivedSessionsSection.tsx
@@ -6,7 +6,7 @@
  */
 import { useMemo, useState, type ReactNode } from 'react'
 import { Button, IconSearchOutline16, relativeTime } from '@deepseek-ai/dsh-client-ui-primitives'
-import type { InjectFace, PropsLocale, PropsRuntime } from '@deepseek-ai/dsh-client-ui-slots'
+import type { InjectFace, PropsLocale, PropsRuntime, PropsRenderSlots } from '@deepseek-ai/dsh-client-ui-slots'
 import type { SessionId } from '@deepseek-ai/dsh-session/types'
 import css from './ArchivedSessionsSection.module.css'
 
@@ -23,6 +23,7 @@ export interface ArchivedSessionsSectionInjected {
 export type ArchivedSessionsSectionProps =
   PropsRuntime<'settings.section'>
   & PropsLocale<'settings.archivedSessions'>
+  & PropsRenderSlots<'settings.archivedSessions.action'>
   & InjectFace<ArchivedSessionsSectionInjected>
 
 type Translate = ArchivedSessionsSectionProps['t']
@@ -55,7 +56,7 @@ function matches(row: ArchivedRow, normalizedQuery: string): boolean {
  * @returns the settings page element tree.
  */
 export function ArchivedSessionsSection(props: ArchivedSessionsSectionProps): ReactNode {
-  const { t, unarchive, useSessions, useWorkspaces } = props
+  const { t, unarchive, useSessions, useWorkspaces, renderSlot } = props
   const sessions = useSessions(state => state)
   const workspaces = useWorkspaces(state => state.items)
   const archivedSessionIds = useWorkspaces(state => state.archivedSessionIds)
@@ -113,6 +114,7 @@ export function ArchivedSessionsSection(props: ArchivedSessionsSectionProps): Re
                   {[row.workspace, timeLabel(row.updatedAt, now, t)].join(' · ')}
                 </span>
               </span>
+              {renderSlot('settings.archivedSessions.action', { targetSessionId: row.id, title: row.title })}
               <Button
                 variant="outline"
                 size="sm"
diff --git a/packages/client/ui-settings-unarchive-sessions/src/client/index.ts b/packages/client/ui-settings-unarchive-sessions/src/client/index.ts
index 353e6c072b..8585a9ebfc 100644
--- a/packages/client/ui-settings-unarchive-sessions/src/client/index.ts
+++ b/packages/client/ui-settings-unarchive-sessions/src/client/index.ts
@@ -17,6 +17,10 @@ export type { ArchivedSessionsSectionInjected, ArchivedSessionsSectionProps } fr
 export type { ArchivedSessionsLocaleKey } from './locales.ts'
 
 declare module '@deepseek-ai/dsh-client-ui-slots' {
+  interface SlotMap {
+    /** Feature-owned controls beside an archived Session. */
+    'settings.archivedSessions.action': { kind: 'list'; scope: 'root'; owner: { targetSessionId: import('@deepseek-ai/dsh-session/types').SessionId; title: string } }
+  }
   interface LocaleNamespaceMap {
     /** Archived-session page copy. */
     'settings.archivedSessions': ArchivedSessionsLocaleKey
@@ -41,6 +45,7 @@ export function apply(ctx: ClientContext): void {
   ctx.slots.inject('settings.section', () => ctx.slots.register({
     name: 'settings.section',
     id: 'archived-sessions',
+    children: { 'settings.archivedSessions.action': { kind: 'list', scope: 'root' } },
     order: 25,
     label: () => t('nav'),
     locale: NS,
diff --git a/packages/client/ui-sidebar-files/README.i18n.yaml b/packages/client/ui-sidebar-files/README.i18n.yaml
index fefd4561f0..02fcb1d6cc 100644
--- a/packages/client/ui-sidebar-files/README.i18n.yaml
+++ b/packages/client/ui-sidebar-files/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-sidebar-files/README.md
-README.md: efaf0a584e0e8617d0cb97442f9ebb5fa1fe7b59
-README.zh.md: a0d0abffcd6e9f4e979d9a6548537745d0a74ba6
+README.md: 79a8e815d45269460c51ffe1527478d298b0110d
+README.zh.md: 341d2f6f958b5d4404626702f40909cb76d9fe73
diff --git a/packages/client/ui-sidebar-files/README.md b/packages/client/ui-sidebar-files/README.md
index efaf0a584e..79a8e815d4 100644
--- a/packages/client/ui-sidebar-files/README.md
+++ b/packages/client/ui-sidebar-files/README.md
@@ -11,6 +11,8 @@ English | [中文](README.zh.md)
 
 The right Sidebar's navigator tab type: the session's workspace root as a tree, listed one level at a time over the wire, opening files into the Sidebar. It is a page type reached from the guide and claims no address; it opens files by address for the `dsh-resource://file` viewers to claim — nothing in `ui-sidebar-right` knows this package.
 
+Each regular file exposes the Session-scoped `sidebar.files.file.action` list slot with its absolute path. Actions marked with `data-file-action` share compact icon styling and larger touch targets.
+
 ## Table of Contents
 
 - [What it registers](#what-it-registers)
@@ -21,6 +23,8 @@ The right Sidebar's navigator tab type: the session's workspace root as a tree,
 
 -----
 
+Directories expose the independent sidebar.files.directory.action slot, while regular files retain sidebar.files.file.action.
+
 <a id="what-it-registers"></a>
 ## What it registers
 
diff --git a/packages/client/ui-sidebar-files/README.zh.md b/packages/client/ui-sidebar-files/README.zh.md
index a0d0abffcd..341d2f6f95 100644
--- a/packages/client/ui-sidebar-files/README.zh.md
+++ b/packages/client/ui-sidebar-files/README.zh.md
@@ -11,6 +11,8 @@ kind: "package-reference"
 
 右侧 Sidebar 的导航器 tab 类型：把会话的工作区根目录画成一棵树，逐层经线上列出，并把文件打开到 Sidebar 里。它是从引导页进入的页类型，不认领任何地址；它按地址打开文件，交给 `dsh-resource://file` 的查看器认领：`ui-sidebar-right` 里没有任何东西认识本包。
 
+每个普通文件通过对话作用域列表插槽 `sidebar.files.file.action` 提供其绝对路径。 标有 `data-file-action` 的操作使用统一紧凑图标样式，并为触屏提供更大的点击区域。
+
 ## 目录
 
 - [注册了什么](#what-it-registers)
@@ -21,6 +23,8 @@ kind: "package-reference"
 
 -----
 
+目录提供独立的 sidebar.files.directory.action 插槽，普通文件保留 sidebar.files.file.action。
+
 <a id="what-it-registers"></a>
 ## 注册了什么
 
diff --git a/packages/client/ui-sidebar-files/src/client/FilesBody.module.css b/packages/client/ui-sidebar-files/src/client/FilesBody.module.css
index a49f6688a3..4ac8201cd9 100644
--- a/packages/client/ui-sidebar-files/src/client/FilesBody.module.css
+++ b/packages/client/ui-sidebar-files/src/client/FilesBody.module.css
@@ -206,3 +206,11 @@
 .titleIcon {
   flex: none;
 }
+
+.fileRow { display: flex; align-items: center; min-width: 0; }
+.fileRow > button { flex: 1; min-width: 0; }
+
+.fileRow [data-file-action] { display: inline-flex; flex: 0 0 30px; align-items: center; justify-content: center; box-sizing: border-box; width: 30px; height: 30px; margin: 0 1px; padding: 6px; border: 0; border-radius: 8px; background: transparent; color: var(--dsw-alias-label-secondary); text-decoration: none; cursor: pointer; }
+.fileRow [data-file-action]:hover, .fileRow [data-file-action]:focus-visible { background: var(--dsw-alias-interactive-bg-hover); color: var(--dsw-alias-label-primary); }
+.fileRow [data-file-action] svg { stroke-width: 1.7; stroke-linecap: round; stroke-linejoin: round; }
+@media (pointer: coarse) { .fileRow [data-file-action] { flex-basis: 40px; width: 40px; height: 44px; } }
diff --git a/packages/client/ui-sidebar-files/src/client/FilesBody.tsx b/packages/client/ui-sidebar-files/src/client/FilesBody.tsx
index 7a8c70f501..0064ab391e 100644
--- a/packages/client/ui-sidebar-files/src/client/FilesBody.tsx
+++ b/packages/client/ui-sidebar-files/src/client/FilesBody.tsx
@@ -14,7 +14,7 @@ import { useEffect, useLayoutEffect, useRef } from 'react'
 import type { ReactNode, RefObject } from 'react'
 import clsx from 'clsx'
 import type { RemoteFailure } from '@deepseek-ai/dsh-api-remotes/client'
-import type { PropsLocale, PropsRuntime, PropsStore, TranslateNS } from '@deepseek-ai/dsh-client-ui-slots'
+import type { PropsLocale, PropsRuntime, PropsStore, PropsRenderSlots, TranslateNS } from '@deepseek-ai/dsh-client-ui-slots'
 import {
   FileTypeIcon, IconFolderClose16, IconFolderOpen16, IconRefreshOutline16, classifyFileType,
 } from '@deepseek-ai/dsh-client-ui-primitives'
@@ -32,6 +32,7 @@ export type FilesBodyProps =
   & PropsStore<ReturnType<typeof createFilesStore>>
   & FilesInjected
   & PropsLocale<'sidebarFiles'>
+  & PropsRenderSlots<'sidebar.files.file.action' | 'sidebar.files.directory.action'>
 
 /** Natural, case-insensitive name order, so `file2` precedes `file10`. */
 const byName = new Intl.Collator(undefined, { numeric: true, sensitivity: 'base' })
@@ -102,6 +103,8 @@ function usePathClipped(
 
 /** What every level shares: the tab's tree and the two gestures. */
 interface TreeContext {
+  readonly renderDirectoryActions: (path: string) => ReactNode
+  readonly renderActions: (path: string) => ReactNode
   readonly state: FilesTabState
   readonly onToggle: (path: string) => void
   readonly onOpen: (path: string) => void
@@ -115,21 +118,22 @@ function Entry({ parent, entry, tree }: { parent: string; entry: WorkspaceDirect
     const expanded = tree.state.expanded.includes(path)
     return (
       <li className={css.item} data-files-entry="directory" data-files-path={path}>
-        <button type="button" className={css.row} aria-expanded={expanded} onClick={() => { tree.onToggle(path) }}>
+        <div className={css.fileRow}><button type="button" className={css.row} aria-expanded={expanded} onClick={() => { tree.onToggle(path) }}>
           {expanded ? <IconFolderOpen16 className={css.icon} /> : <IconFolderClose16 className={css.icon} />}
           <span className={css.name}>{entry.name}</span>
-        </button>
+        </button>{tree.renderDirectoryActions(path)}</div>
         {expanded && <ul className={css.level}><Level path={path} tree={tree} /></ul>}
       </li>
     )
   }
   if (entry.type === 'file') {
     return (
-      <li className={css.item} data-files-entry="file" data-files-path={path}>
+      <li className={css.fileRow} data-files-entry="file" data-files-path={path}>
         <button type="button" className={css.row} onClick={() => { tree.onOpen(path) }}>
           <FileTypeIcon kind={classifyFileType(entry.name)} size={16} className={css.fileIcon} />
           <span className={css.name}>{entry.name}</span>
         </button>
+        {tree.renderActions(path)}
       </li>
     )
   }
@@ -169,6 +173,7 @@ function Level({ path, tree }: { path: string; tree: TreeContext }): ReactNode {
 /** The file tree's body: the workspace root and whatever the reader has opened under it. */
 export function FilesBody({
   useTabInfo, sessionId, useSessions, useStore, actions, start, load, toggle, t,
+  renderSlot,
 }: FilesBodyProps): ReactNode {
   const { tab } = useTabInfo()
   const { signal, actions: tabActions } = tab
@@ -212,6 +217,8 @@ export function FilesBody({
   }
   if (state === undefined) return null
   const tree: TreeContext = {
+    renderDirectoryActions: path => renderSlot('sidebar.files.directory.action', { path }),
+    renderActions: path => renderSlot('sidebar.files.file.action', { path }),
     state,
     onToggle: (path) => { toggle(tab.id, path, state.levels[path] !== undefined, signal) },
     // Every row is under the tree's root, so its address is session-relative.
diff --git a/packages/client/ui-sidebar-files/src/client/index.ts b/packages/client/ui-sidebar-files/src/client/index.ts
index 572f5583b5..1e02b7a4b4 100644
--- a/packages/client/ui-sidebar-files/src/client/index.ts
+++ b/packages/client/ui-sidebar-files/src/client/index.ts
@@ -27,6 +27,15 @@ export type { DirLevel, FilesState, FilesTabState, LevelState } from './store.ts
 export type { FilesInjected, ListWorkspaceDirectory, WorkspaceFilesListRemote } from './face.ts'
 export type { FilesBodyProps } from './FilesBody.tsx'
 
+declare module '@deepseek-ai/dsh-client-ui-slots' {
+  interface SlotMap {
+    /** Actions on one directory in the Session workspace tree. */
+    'sidebar.files.directory.action': { kind: 'list'; scope: 'session'; owner: { path: string } }
+    /** Actions on one regular file in the Session workspace tree. */
+    'sidebar.files.file.action': { kind: 'list'; scope: 'session'; owner: { path: string } }
+  }
+}
+
 /** This package's copy namespace. */
 const NS = 'sidebarFiles'
 
@@ -48,7 +57,7 @@ export function apply(ctx: ClientContext): void {
   const store = createFilesStore()
   const inject = filesFace(createList(ctx.remote))
   ctx.effect(() => ctx.slots.inject('sidebar.right.pane.tab', () => ctx.slots.register(
-    { name: 'sidebar.right.pane.tab', key: FILES_ID, locale: NS, store, inject },
+    { name: 'sidebar.right.pane.tab', key: FILES_ID, locale: NS, store, inject, children: { 'sidebar.files.directory.action': { kind: 'list', scope: 'session' }, 'sidebar.files.file.action': { kind: 'list', scope: 'session' } } },
     FilesBody,
   )), 'ui-sidebar-files: files tab body')
   ctx.effect(() => ctx.slots.inject('sidebar.right.pane.tab.title', () => ctx.slots.register(
diff --git a/packages/client/ui-sidebar/README.i18n.yaml b/packages/client/ui-sidebar/README.i18n.yaml
index b9e04d95cb..bfb4ee9913 100644
--- a/packages/client/ui-sidebar/README.i18n.yaml
+++ b/packages/client/ui-sidebar/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-sidebar/README.md
-README.md: a0b9ffe8ca601fade50ff86fc1921d6b41b84748
-README.zh.md: 44b08c8ef76ec266c813c8199db8291f47db3d53
+README.md: 37e00a664af363501c8b8670e07aa30d8d60297e
+README.zh.md: 7097292ad74fadac432eb14a90191a58ccedafc8
diff --git a/packages/client/ui-sidebar/README.md b/packages/client/ui-sidebar/README.md
index a0b9ffe8ca..37e00a664a 100644
--- a/packages/client/ui-sidebar/README.md
+++ b/packages/client/ui-sidebar/README.md
@@ -9,7 +9,7 @@ English | [中文](README.zh.md)
 
 ## Summary
 
-The dsh web client sidebar lets users recognize the active build, start a new session, collapse navigation to a 56px rail, browse Workspaces and Sessions, and open Settings. It preserves a bottom-pinned Settings entry and hides idle scrollbars without moving browser rows. New Session uses an explicitly selected Workspace, then the current Session's Workspace, then the most recently active Workspace; if none exists, it opens a blank New Session page. Deployments can replace the brand mark or name while retaining the navigation controls and rail geometry.
+The dsh web client sidebar lets users recognize the active build, start a new session, collapse navigation to a 56px rail, browse Workspaces and Sessions, and open Settings. It preserves a bottom-pinned Settings entry and hides idle scrollbars without moving browser rows. New Session uses an explicitly selected Workspace, then the current Session's Workspace, then the most recently active Workspace; if none exists, it opens a blank New Session page. Deployments can replace the brand mark or name while retaining the navigation controls and rail geometry. Panel navigation buttons expose data-sidebar-panel so responsive plugins can identify navigation actions.
 
 ## Table of Contents
 
diff --git a/packages/client/ui-sidebar/README.zh.md b/packages/client/ui-sidebar/README.zh.md
index 44b08c8ef7..7097292ad7 100644
--- a/packages/client/ui-sidebar/README.zh.md
+++ b/packages/client/ui-sidebar/README.zh.md
@@ -9,7 +9,7 @@ kind: "package-reference"
 
 ## 概述
 
-dsh Web 客户端的侧边栏让用户识别当前构建、启动新会话、将导航折叠为 56px 轨道、浏览 Workspace 与 Session，以及打开 Settings。它会将 Settings 入口固定在底部，并在隐藏空闲滚动条时避免浏览器行发生位移。New Session 优先使用显式选择的 Workspace，其次使用当前 Session 所属的 Workspace，再其次使用最近活跃的 Workspace；如果都不存在，则打开空白的 New Session 页面。部署可以替换品牌标记或名称，同时保留导航控件和轨道几何。
+dsh Web 客户端的侧边栏让用户识别当前构建、启动新会话、将导航折叠为 56px 轨道、浏览 Workspace 与 Session，以及打开 Settings。它会将 Settings 入口固定在底部，并在隐藏空闲滚动条时避免浏览器行发生位移。New Session 优先使用显式选择的 Workspace，其次使用当前 Session 所属的 Workspace，再其次使用最近活跃的 Workspace；如果都不存在，则打开空白的 New Session 页面。部署可以替换品牌标记或名称，同时保留导航控件和轨道几何。 面板导航按钮提供 data-sidebar-panel 属性，供响应式插件识别导航操作。
 
 ## 目录
 
diff --git a/packages/client/ui-sidebar/src/client/SidebarRoot.tsx b/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
index 6ed94f3b0d..8f9e196edb 100644
--- a/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
+++ b/packages/client/ui-sidebar/src/client/SidebarRoot.tsx
@@ -62,6 +62,7 @@ function PanelRow({ id, label, wide, usePanelInfo, selectPanel, renderSlot }: Pa
     <Tooltip label={label} delayMs={500} disabled={wide}>
       <button
         type="button"
+        data-sidebar-panel={id}
         className={clsx(css.panelRow, active && css.panelActive)}
         aria-label={label}
         aria-current={active ? 'page' : undefined}
diff --git a/packages/client/ui-workspace/README.i18n.yaml b/packages/client/ui-workspace/README.i18n.yaml
index 0bdecf2b08..87341fe81a 100644
--- a/packages/client/ui-workspace/README.i18n.yaml
+++ b/packages/client/ui-workspace/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/client/ui-workspace/README.md
-README.md: 3fbbf52fd5fabca3416b7fe566f579f4a74dd3f2
-README.zh.md: 04b0565f8fc17f183fd9c1b1b48a7edbd1ba32ba
+README.md: 3c4cd85158e70f0627a75c52a937a130882985dd
+README.zh.md: 6dfb58e870d16182764df281075d93184ff1382e
diff --git a/packages/client/ui-workspace/README.md b/packages/client/ui-workspace/README.md
index 3fbbf52fd5..3c4cd85158 100644
--- a/packages/client/ui-workspace/README.md
+++ b/packages/client/ui-workspace/README.md
@@ -25,6 +25,8 @@ This package lets users browse grouped or flat Session lists, choose a Workspace
 <a id="use-this-package"></a>
 ## Use this package
 
+The `sidebar.workspaces.session.menu` list slot appends feature-owned menu actions. The `sidebar.workspaces.delete` single slot can replace the Workspace confirmation; without a contribution the built-in confirmation remains.
+
 Use the sidebar to browse Workspaces and their Sessions, reorder them, and start new ones; use the picker in the Session Intent hero to choose a Workspace for a new session. An open Workspace shows five non-blank Sessions by default and keeps the selected blank **New Session** as one provisional extra row until its first prompt. **Show more** reveals the hidden remainder; closing and reopening the Workspace restores this folded projection.
 
 ### Reordering and view options
diff --git a/packages/client/ui-workspace/README.zh.md b/packages/client/ui-workspace/README.zh.md
index 04b0565f8f..6dfb58e870 100644
--- a/packages/client/ui-workspace/README.zh.md
+++ b/packages/client/ui-workspace/README.zh.md
@@ -25,6 +25,8 @@ kind: "package-reference"
 <a id="use-this-package"></a>
 ## 使用本包
 
+列表插槽 `sidebar.workspaces.session.menu` 可追加功能插件拥有的菜单操作。单项插槽 `sidebar.workspaces.delete` 可替换工作区确认弹窗；没有扩展时保留内置弹窗。
+
 用侧边栏浏览 Workspace 及其 Session、重排它们并新建会话；在 Session Intent 主视觉区用选择器为新会话选择 Workspace。打开的 Workspace 默认显示五条非空白 Session，并在首条提示词落地前把当前选中的空白**新会话**作为一条临时额外行。**展开其余**会显示隐藏条目；关闭再打开 Workspace 会恢复该折叠投影。
 
 ### 重排序与视图选项
diff --git a/packages/client/ui-workspace/src/client/contract/slots.ts b/packages/client/ui-workspace/src/client/contract/slots.ts
index baa263b1a6..b0653898d4 100644
--- a/packages/client/ui-workspace/src/client/contract/slots.ts
+++ b/packages/client/ui-workspace/src/client/contract/slots.ts
@@ -53,6 +53,10 @@ export interface DirectoryFlowOwnerProps {
 
 declare module '@deepseek-ai/dsh-client-ui-slots' {
   interface SlotMap {
+    /** Feature-owned actions appended to a Session row menu. */
+    'sidebar.workspaces.session.menu': { kind: 'list'; scope: 'root'; owner: { targetSessionId: SessionId; title: string; dismiss: () => void } }
+    /** Optional replacement for the Workspace deletion confirmation. */
+    'sidebar.workspaces.delete': { kind: 'single'; scope: 'root'; owner: { workspaceId: WorkspaceId; title: string; close: () => void } }
     /** Directory-flow hole under the conversation empty-state picker (declared by the WorkspacePicker entry). */
     'conversation.hero.workspace.directoryFlow': { kind: 'single'; scope: 'root'; owner: DirectoryFlowOwnerProps }
     /** Directory-flow hole under the sidebar browsing region (declared by the WorkspaceBrowser entry). */
@@ -141,7 +145,7 @@ export type WorkspaceBrowserInjected = {
 /** Full browser props: shell owner share + viewing store + injected actions + the locale seat. */
 export type WorkspaceBrowserProps =
   PropsRuntime<'sidebar.workspaces'>
-  & PropsRenderSlots<'sidebar.workspaces.directoryFlow'>
+  & PropsRenderSlots<'sidebar.workspaces.directoryFlow' | 'sidebar.workspaces.session.menu' | 'sidebar.workspaces.delete'>
   & PropsStore<ReturnType<typeof createWorkspaceViewStore>>
   & Omit<WorkspaceBrowserInjected, 'hooks'>
   & PropsHooks<WorkspaceBrowserInjected['hooks']>
diff --git a/packages/client/ui-workspace/src/client/index.ts b/packages/client/ui-workspace/src/client/index.ts
index 514ddd9e92..99c80c1b58 100644
--- a/packages/client/ui-workspace/src/client/index.ts
+++ b/packages/client/ui-workspace/src/client/index.ts
@@ -144,7 +144,11 @@ export function apply(ctx: Context): void {
   ctx.slots.inject('sidebar.workspaces', () => ctx.slots.register(
     {
       name: 'sidebar.workspaces',
-      children: { 'sidebar.workspaces.directoryFlow': { kind: 'single', scope: 'root' } },
+      children: {
+        'sidebar.workspaces.directoryFlow': { kind: 'single', scope: 'root' },
+        'sidebar.workspaces.session.menu': { kind: 'list', scope: 'root' },
+        'sidebar.workspaces.delete': { kind: 'single', scope: 'root' },
+      },
       store: createWorkspaceViewStore(),
       inject: browserInjected,
       locale: NS,
diff --git a/packages/client/ui-workspace/src/client/rows/Rows.tsx b/packages/client/ui-workspace/src/client/rows/Rows.tsx
index 3d015f3c57..9c385855ad 100644
--- a/packages/client/ui-workspace/src/client/rows/Rows.tsx
+++ b/packages/client/ui-workspace/src/client/rows/Rows.tsx
@@ -6,7 +6,7 @@
  * visual-only except workspace Rename/Delete and session Rename/Fork/Archive; the
  * session and workspace hover cards are suppressed while a menu is open.
  */
-import { useEffect, useRef, useState } from 'react'
+import { useEffect, useRef, useState, type ReactNode } from 'react'
 import clsx from 'clsx'
 import {
   HoverCard, IconAlarmClockOutline16, IconArchiveOutline20, IconBranchOutline16,
@@ -404,9 +404,11 @@ export function SearchResultItem({ result, currentId, onOpen, t }: {
  * @returns the session row.
  */
 export function SessionNodeItem({
-  node, currentId, now, onOpen, onRename, onFork, onArchive, onReveal, drag, flat = false, t,
+  node, currentId, now, onOpen, onRename, onFork, onArchive, onReveal, drag, flat = false, t, renderActions,
 }: {
   node: SessionNode
+  /** Feature-owned entries appended to this row's menu. */
+  renderActions?: ((id: SessionNode['id'], title: string, dismiss: () => void) => ReactNode) | undefined
   currentId: string | undefined
   now: number
   onOpen: (id: SessionNode['id']) => void
@@ -508,6 +510,7 @@ export function SessionNodeItem({
             open={menuOpen}
             onClose={() => { setMenuOpen(false) }}
             items={sessionMenuItems}
+            extra={renderActions?.(node.id, title, () => { setMenuOpen(false) })}
             onSelect={(id) => {
               setMenuOpen(false)
               if (id === 'rename') onRename(node.id, row.title)
diff --git a/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx b/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx
index e41a695b0d..10c2753841 100644
--- a/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx
+++ b/packages/client/ui-workspace/src/client/rows/WorkspaceBrowser.tsx
@@ -199,6 +199,7 @@ type SessionTreeProps = Pick<
   /** Open the browser-owned session rename dialog. */
   onSessionRename: (sessionId: SessionNode['id'], currentTitle: string) => void
   /** Archive a session (row menu action; the row disappears on the state echo). */
+  renderSessionActions?: (id: SessionNode['id'], title: string, dismiss: () => void) => ReactNode
   onSessionArchive: (sessionId: SessionNode['id']) => void
   /** One Session chosen from search that must be exposed and scrolled into view. */
   revealSessionId?: SessionId | undefined
@@ -211,7 +212,7 @@ function SessionTree({
   list, useSessionStatus, startSession, open, forkSession, workspaces, ungroupedSessionIds,
   archivedSessionIds,
   workspaceReady, usePanelInfo,
-  onRenameRequest, onDeleteRequest, onSessionRename, onSessionArchive,
+  onRenameRequest, onDeleteRequest, onSessionRename, onSessionArchive, renderSessionActions,
   insertWorkspaceBefore,
   nestWorkspaces, groupExpansion, setGroupExpanded,
   setSessionOrder, home, t,
@@ -522,6 +523,7 @@ function SessionTree({
               onRename={onSessionRename}
               onFork={forkSession}
               onArchive={onSessionArchive}
+              renderActions={renderSessionActions}
               onReveal={node.id === revealSessionId && group.key === revealGroup
                 ? () => { onSessionRevealed(node.id) }
                 : undefined}
@@ -566,7 +568,7 @@ function SessionTree({
 
 /** The flat "In one list" body: every session is one draggable top-level row. */
 function FlatList({
-  list, sessionIds, useSessionStatus, open, forkSession, onSessionRename, onSessionArchive,
+  list, sessionIds, useSessionStatus, open, forkSession, onSessionRename, onSessionArchive, renderSessionActions,
   usePanelInfo, setSessionOrder,
   revealSessionId, onSessionRevealed, t,
 }: Pick<
@@ -576,6 +578,7 @@ function FlatList({
   | 'forkSession'
   | 'onSessionRename'
   | 'onSessionArchive'
+  | 'renderSessionActions'
   | 'usePanelInfo'
   | 'setSessionOrder'
   | 'revealSessionId'
@@ -635,6 +638,7 @@ function FlatList({
               onRename={onSessionRename}
               onFork={forkSession}
               onArchive={onSessionArchive}
+              renderActions={renderSessionActions}
               onReveal={node.id === revealSessionId
                 ? () => { onSessionRevealed(node.id) }
                 : undefined}
@@ -1267,6 +1271,7 @@ export function WorkspaceBrowser({
                 useSessionStatus={useSessionStatus}
                 open={open} forkSession={forkSession}
                 onSessionRename={onSessionRename} onSessionArchive={onSessionArchive}
+                renderSessionActions={(targetSessionId, title, dismiss) => renderSlot('sidebar.workspaces.session.menu', { targetSessionId, title, dismiss })}
                 setSessionOrder={saveSessionOrder}
                 revealSessionId={revealSessionId}
                 onSessionRevealed={acknowledgeSessionReveal}
@@ -1280,6 +1285,7 @@ export function WorkspaceBrowser({
                 useSessionStatus={useSessionStatus}
                 onSessionRename={onSessionRename}
                 onSessionArchive={onSessionArchive}
+                renderSessionActions={(targetSessionId, title, dismiss) => renderSlot('sidebar.workspaces.session.menu', { targetSessionId, title, dismiss })}
                 forkSession={forkSession}
                 workspaces={orderedWorkspaces}
                 ungroupedSessionIds={orderedUngroupedSessionIds}
@@ -1375,7 +1381,7 @@ export function WorkspaceBrowser({
         />
         {sessionRenameError !== null && <div className={css.renameError} role="alert">{sessionRenameError}</div>}
       </Modal>
-      <Modal
+      {deleteTarget !== null && renderSlot('sidebar.workspaces.delete', { ...deleteTarget, close: closeDelete }, { fallback: <Modal
         open={deleteTarget !== null}
         onClose={closeDelete}
         closeLabel={t('close')}
@@ -1399,7 +1405,7 @@ export function WorkspaceBrowser({
       >
         {deleting && <div className={css.deleteStatus} role="status">{t('delete.pending')}</div>}
         {deleteError !== null && <div className={css.renameError} role="alert">{deleteError}</div>}
-      </Modal>
+      </Modal> })}
     </div>
   )
 }
diff --git a/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts b/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts
index 0caa5b38bf..82aa793824 100644
--- a/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts
+++ b/packages/extensions/cordis-client-runner/src/client/slot-catalog.ts
@@ -351,6 +351,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     occupants: [
       'client-ui-deliverables DeliverablesTail id \'@deepseek-ai/dsh-client-ui-deliverables\'',
       'client-ui-plan PlanCards',
+      'experimental-workspace-download Deliveries id \'workspace-download\'',
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.chat.turnTail\', () => ctx.slots.register(\n      { name: \'conversation.chat.turnTail\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
@@ -498,6 +499,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     declaredBy: 'an entry in \'conversation.composer.bar\' (client-ui-conversation), so it exists while that entry is mounted',
     occupants: [
       'client-ui-chat StatsPills id \'stats\'',
+      'experimental-usage-billing BillingBadge id \'usage-billing\'',
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.composer.dock\', () => ctx.slots.register(\n      { name: \'conversation.composer.dock\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
@@ -632,7 +634,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'conversation.hero.workspace.directoryFlow\', () => ctx.slots.register(\n      { name: \'conversation.hero.workspace.directoryFlow\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-workspace/src/client/contract/slots.ts:57',
+    source: 'packages/client/ui-workspace/src/client/contract/slots.ts:61',
   },
   {
     key: 'conversation.input.attachments',
@@ -1565,13 +1567,14 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
       'useSessionRetainInfo: UseSessionRetainInfo',
       'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
     ],
-    keyDomain: 'open: any string the owner dispatches (no compile-time key set), already taken: conversation',
+    keyDomain: 'open: any string the owner dispatches (no compile-time key set), already taken: conversation, usage-dashboard',
     hookContext: '',
     slotInject: '',
     declaredBy: 'an entry in \'root\' (client-ui-layout), so it exists while that entry is mounted',
     occupants: [
       'client-ui-conversation ConversationPanel key \'conversation\'',
       'client-ui-plugin-manager PluginManagerPage',
+      'experimental-usage-dashboard Dashboard key \'usage-dashboard\'',
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'main\', () => ctx.slots.register(\n      { name: \'main\', key: \'<one key the owner dispatches>\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
@@ -1888,6 +1891,58 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'settings.action\', () => ctx.slots.register(\n      { name: \'settings.action\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
     source: 'packages/client/ui-settings/src/client/contract/slots.ts:36',
   },
+  {
+    key: 'settings.archivedSessions.action',
+    kind: 'list',
+    scope: 'root',
+    summary: 'Feature-owned controls beside an archived Session.',
+    doc: 'Feature-owned controls beside an archived Session.',
+    registerOptions: [
+      {
+        name: 'id',
+        requirement: 'required',
+        type: 'string',
+        doc: 'Your cell key. Use an id of your own: a fresh id is added beside the shipped entries, while reusing a shipped id puts you in THAT cell and replaces it. Owners that filter by id address you by it.',
+      },
+      {
+        name: 'order',
+        requirement: 'optional',
+        type: 'number',
+        doc: 'Position among the entries, ascending (default 0).',
+      },
+      {
+        name: 'label',
+        requirement: 'optional',
+        type: 'string | (() => string)',
+        doc: 'Display text where the owner projects one (nav rows, tabs). A thunk is re-read on every projection, so localized text follows the active locale without re-registering.',
+      },
+    ],
+    ownerProps: [
+      '/** Identifies one session in the store (and its persistence artifacts). */\nexport type SessionId = Branded<\'SessionId\'>',
+    ],
+    ownerPropsReferences: [
+      'Branded',
+    ],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionStatus: UseSessionStatus',
+      'useSessionRetainInfo: UseSessionRetainInfo',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'settings.section\' (client-ui-settings-unarchive-sessions), so it exists while that entry is mounted',
+    occupants: [
+      'experimental-workspace-management SessionAction id \'delete-session\'',
+    ],
+    replaceRisk: 'none',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'settings.archivedSessions.action\', () => ctx.slots.register(\n      { name: \'settings.archivedSessions.action\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/client/ui-settings-unarchive-sessions/src/client/index.ts:22',
+  },
   {
     key: 'settings.close',
     kind: 'single',
@@ -2320,6 +2375,8 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     declaredBy: 'an entry in \'root\' (client-ui-layout), so it exists while that entry is mounted',
     occupants: [
       'client-ui-sidebar-terminal TerminalCleanup',
+      'experimental-network-usage TrafficNotice id \'network-usage\'',
+      'experimental-workspace-management SessionDialog id \'delete-session\'',
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'shell.overlay\', () => ctx.slots.register(\n      { name: \'shell.overlay\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
@@ -2455,6 +2512,62 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.chat.conversation\', () => ctx.slots.register(\n      { name: \'sidebar.chat.conversation\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
     source: 'packages/client/ui-subagent/src/client/sidebar-chat/index.tsx:42',
   },
+  {
+    key: 'sidebar.files.file.action',
+    kind: 'list',
+    scope: 'session',
+    summary: 'Actions on one regular file in the Session workspace tree.',
+    doc: 'Actions on one regular file in the Session workspace tree.',
+    registerOptions: [
+      {
+        name: 'id',
+        requirement: 'required',
+        type: 'string',
+        doc: 'Your cell key. Use an id of your own: a fresh id is added beside the shipped entries, while reusing a shipped id puts you in THAT cell and replaces it. Owners that filter by id address you by it.',
+      },
+      {
+        name: 'order',
+        requirement: 'optional',
+        type: 'number',
+        doc: 'Position among the entries, ascending (default 0).',
+      },
+      {
+        name: 'label',
+        requirement: 'optional',
+        type: 'string | (() => string)',
+        doc: 'Display text where the owner projects one (nav rows, tabs). A thunk is re-read on every projection, so localized text follows the active locale without re-registering.',
+      },
+    ],
+    ownerProps: [],
+    ownerPropsReferences: [],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionStatus: UseSessionStatus',
+      'useSessionRetainInfo: UseSessionRetainInfo',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'useChat: UseChat',
+      'useConversation: UseConversation',
+      'useInput: SnapshotSelectorHook<InputState>',
+      'inputActions: InputActions',
+      'useSession: SessionSnapshotSelector',
+      'sessionId: SessionId',
+      'useProjection: UseProjection',
+      'useTrajectory: UseTrajectory',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'sidebar.right.pane.tab\' (client-ui-sidebar-files), so it exists while that entry is mounted',
+    occupants: [
+      'experimental-workspace-download FileDownload id \'download\'',
+    ],
+    replaceRisk: 'none',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.files.file.action\', () => ctx.slots.register(\n      { name: \'sidebar.files.file.action\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/client/ui-sidebar-files/src/client/index.ts:33',
+  },
   {
     key: 'sidebar.footer.action',
     kind: 'list',
@@ -2550,6 +2663,7 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     declaredBy: 'an entry in \'sidebar\' (client-ui-sidebar), so it exists while that entry is mounted',
     occupants: [
       'client-ui-plugin-manager PluginsPanelIcon',
+      'experimental-usage-dashboard DashboardIcon id \'usage-dashboard\'',
     ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.panellist\', () => ctx.slots.register(\n      { name: \'sidebar.panellist\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
@@ -2898,7 +3012,9 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     hookContext: '',
     slotInject: '',
     declaredBy: 'an entry in \'rightbar.session\' (client-ui-sidebar-right), so it exists while that entry is mounted',
-    occupants: [],
+    occupants: [
+      'experimental-workspace-download DownloadMenu id \'workspace-download\'',
+    ],
     replaceRisk: 'none',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.right.tab.menu.item\', () => ctx.slots.register(\n      { name: \'sidebar.right.tab.menu.item\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
     source: 'packages/client/ui-sidebar-right/src/client/contract/slots.ts:94',
@@ -2994,6 +3110,35 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.workspaces\', () => ctx.slots.register(\n      { name: \'sidebar.workspaces\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
     source: 'packages/client/ui-sidebar/src/client/contract/slots.ts:41',
   },
+  {
+    key: 'sidebar.workspaces.delete',
+    kind: 'single',
+    scope: 'root',
+    summary: 'Optional replacement for the Workspace deletion confirmation.',
+    doc: 'Optional replacement for the Workspace deletion confirmation.',
+    registerOptions: [],
+    ownerProps: [],
+    ownerPropsReferences: [],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionStatus: UseSessionStatus',
+      'useSessionRetainInfo: UseSessionRetainInfo',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'sidebar.workspaces\' (client-ui-workspace), so it exists while that entry is mounted',
+    occupants: [
+      'experimental-workspace-management WorkspaceDialog',
+    ],
+    replaceRisk: 'shadows-shipped-ui',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.workspaces.delete\', () => ctx.slots.register(\n      { name: \'sidebar.workspaces.delete\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/client/ui-workspace/src/client/contract/slots.ts:59',
+  },
   {
     key: 'sidebar.workspaces.directoryFlow',
     kind: 'single',
@@ -3024,7 +3169,59 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     ],
     replaceRisk: 'shadows-shipped-ui',
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.workspaces.directoryFlow\', () => ctx.slots.register(\n      { name: \'sidebar.workspaces.directoryFlow\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
-    source: 'packages/client/ui-workspace/src/client/contract/slots.ts:59',
+    source: 'packages/client/ui-workspace/src/client/contract/slots.ts:63',
+  },
+  {
+    key: 'sidebar.workspaces.session.menu',
+    kind: 'list',
+    scope: 'root',
+    summary: 'Feature-owned actions appended to a Session row menu.',
+    doc: 'Feature-owned actions appended to a Session row menu.',
+    registerOptions: [
+      {
+        name: 'id',
+        requirement: 'required',
+        type: 'string',
+        doc: 'Your cell key. Use an id of your own: a fresh id is added beside the shipped entries, while reusing a shipped id puts you in THAT cell and replaces it. Owners that filter by id address you by it.',
+      },
+      {
+        name: 'order',
+        requirement: 'optional',
+        type: 'number',
+        doc: 'Position among the entries, ascending (default 0).',
+      },
+      {
+        name: 'label',
+        requirement: 'optional',
+        type: 'string | (() => string)',
+        doc: 'Display text where the owner projects one (nav rows, tabs). A thunk is re-read on every projection, so localized text follows the active locale without re-registering.',
+      },
+    ],
+    ownerProps: [
+      '/** Identifies one session in the store (and its persistence artifacts). */\nexport type SessionId = Branded<\'SessionId\'>',
+    ],
+    ownerPropsReferences: [
+      'Branded',
+    ],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionStatus: UseSessionStatus',
+      'useSessionRetainInfo: UseSessionRetainInfo',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'sidebar.workspaces\' (client-ui-workspace), so it exists while that entry is mounted',
+    occupants: [
+      'experimental-workspace-management SessionAction id \'delete-session\'',
+    ],
+    replaceRisk: 'none',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'sidebar.workspaces.session.menu\', () => ctx.slots.register(\n      { name: \'sidebar.workspaces.session.menu\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/client/ui-workspace/src/client/contract/slots.ts:57',
   },
   {
     key: 'tool.call.images',
@@ -3182,5 +3379,54 @@ export const CLIENT_SLOT_API: readonly ClientSlotEntry[] = [
     example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'tool.view.cordis\', () => ctx.slots.register(\n      { name: \'tool.view.cordis\', key: \'<one key the owner dispatches>\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
     source: 'packages/extensions/ui-cordis/src/client/slots.ts:31',
   },
+  {
+    key: 'usage-dashboard.section',
+    kind: 'list',
+    scope: 'root',
+    summary: 'Independently owned statistics sections in the unified dashboard.',
+    doc: 'Independently owned statistics sections in the unified dashboard.',
+    registerOptions: [
+      {
+        name: 'id',
+        requirement: 'required',
+        type: 'string',
+        doc: 'Your cell key. Use an id of your own: a fresh id is added beside the shipped entries, while reusing a shipped id puts you in THAT cell and replaces it. Owners that filter by id address you by it.',
+      },
+      {
+        name: 'order',
+        requirement: 'optional',
+        type: 'number',
+        doc: 'Position among the entries, ascending (default 0).',
+      },
+      {
+        name: 'label',
+        requirement: 'optional',
+        type: 'string | (() => string)',
+        doc: 'Display text where the owner projects one (nav rows, tabs). A thunk is re-read on every projection, so localized text follows the active locale without re-registering.',
+      },
+    ],
+    ownerProps: [],
+    ownerPropsReferences: [],
+    standardProps: [
+      'useResource: UseResource',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+      'usePanelInfo: UsePanelInfo',
+      'useSessions: UseSessions',
+      'useSessionStatus: UseSessionStatus',
+      'useSessionRetainInfo: UseSessionRetainInfo',
+      'useWorkspaces: SnapshotSelectorHook<WorkspaceSnapshot>',
+    ],
+    keyDomain: '',
+    hookContext: '',
+    slotInject: '',
+    declaredBy: 'an entry in \'main\' (experimental-usage-dashboard), so it exists while that entry is mounted',
+    occupants: [
+      'experimental-network-usage NetworkPage id \'network-usage\'',
+      'experimental-usage-billing BillingPage id \'usage-billing\'',
+    ],
+    replaceRisk: 'none',
+    example: 'return {\n  inject: [\'slots\'],\n  apply(ctx) {\n    ctx.slots.inject(\'usage-dashboard.section\', () => ctx.slots.register(\n      { name: \'usage-dashboard.section\', id: \'my-entry\', order: 100, label: \'My entry\' },\n      () => React.createElement(\'div\', null, \'hello\'),\n    ))\n  },\n}',
+    source: 'packages/experimental/usage-dashboard/src/client/index.ts:14',
+  },
 ]
 /* jscpd:ignore-end */
diff --git a/packages/extensions/tool-cordis/src/api-catalog.ts b/packages/extensions/tool-cordis/src/api-catalog.ts
index 5e6d2a4bcf..5c22dc29d6 100644
--- a/packages/extensions/tool-cordis/src/api-catalog.ts
+++ b/packages/extensions/tool-cordis/src/api-catalog.ts
@@ -1411,6 +1411,47 @@ export const SERVICE_API: readonly ServiceApiEntry[] = [
       },
     ],
   },
+  {
+    key: 'networkProxy',
+    summary: 'Authenticated Host proxy and fixed-identity shell endpoints.',
+    description: 'Authenticated Host proxy and fixed-identity shell endpoints.',
+    methods: [
+      {
+        signature: 'socketFor(sessionId: SessionId): Promise<string>',
+        description: 'Obtain a Session-owned Unix proxy socket for a network-isolated shell.',
+        parameters: [{ name: 'sessionId', description: 'Session whose traffic this socket records.' }],
+        returns: 'Absolute Unix socket path, inaccessible to other Sessions\' sandbox mounts.',
+      },
+    ],
+  },
+  {
+    key: 'networkUsage',
+    summary: 'Shared meter for proxy and browser transfers; confirmations never consume a model turn.',
+    description: 'Shared meter for proxy and browser transfers; confirmations never consume a model turn.',
+    methods: [
+      {
+        signature: 'async allow(signal: AbortSignal): Promise<void>',
+        description: 'Wait for the current spending warning to be acknowledged, or for midnight reset.',
+        parameters: [{ name: 'signal', description: 'Owning transfer or Agent cancellation.' }],
+      },
+      {
+        signature: 'async pace(bytes: number, signal: AbortSignal): Promise<void>',
+        description: 'Apply spending consent and one shared throttle across outgoing streams.',
+        parameters: [{ name: 'bytes', description: 'Next bounded chunk length.' }, { name: 'signal', description: 'Transfer cancellation.' }],
+      },
+      {
+        signature: 'record(owner: TrafficOwner, direction: \'incoming\' | \'outgoing\', bytes: number): void',
+        description: 'Account bytes transferred at a proxy or browser carrier.',
+        parameters: [{ name: 'owner', description: 'Session identity or agentless Host traffic.' }, { name: 'direction', description: 'Direction relative to the server.' }, { name: 'bytes', description: 'Transferred payload bytes, excluding IP/TCP retransmission overhead.' }],
+      },
+      {
+        signature: 'snapshot(): object',
+        description: 'Read dashboard counters and policy decisions.',
+        parameters: [],
+        returns: 'Detached lifetime totals and current enforcement state.',
+      },
+    ],
+  },
   {
     key: 'officeToPdf',
     summary: 'A provider lifetime owns all converters, queued calls, and temporary files.',
@@ -1676,6 +1717,18 @@ export const SERVICE_API: readonly ServiceApiEntry[] = [
         parameters: [{ name: 'sessionId', description: 'durable Session identity.' }, { name: 'signal', description: 'optional caller cancellation for persistence reads.' }],
         returns: 'the current attached state or persisted header and event prefix.',
       },
+      {
+        signature: 'async deleteSession(sessionId: SessionId): Promise<boolean>',
+        description: 'Permanently remove an ordinary Session through an explicitly mounted management consumer. This method is Host-only; the default browser RPC namespace exposes no deletion operation.',
+        parameters: [{ name: 'sessionId', description: 'Session explicitly selected by the user.' }],
+        returns: 'Whether persisted Session data existed.',
+      },
+      {
+        signature: 'withSessionsStopped<T>(sessionIds: readonly SessionId[], operation: () => Promise<T>): Promise<T>',
+        description: 'Release managed writers during a Host-only workspace operation.',
+        parameters: [{ name: 'sessionIds', description: 'Sessions whose workspace is being managed.' }, { name: 'operation', description: 'Work performed while those Sessions cannot activate.' }],
+        returns: 'The operation\'s result.',
+      },
       {
         signature: '@Remote(\'list\') async list(_request: SessionListRequest, signal: AbortSignal): Promise<SessionListValue>',
         description: 'Read all visible Session rows without resuming an Agent.',
@@ -1845,6 +1898,13 @@ export const SERVICE_API: readonly ServiceApiEntry[] = [
         parameters: [{ name: 'options', description: 'optional cancellation.' }],
         returns: 'one snapshot per stored session.',
       },
+      {
+        signature: 'delete(id: SessionId): Promise<boolean>',
+        description: 'Permanently delete an entire stored Session, including every historical generation. Active write ownership must be released before calling this operation.',
+        parameters: [{ name: 'id', description: 'Session explicitly selected for deletion.' }],
+        returns: 'Whether a stored Session existed.',
+        throws: ['When the provider does not support deletion or a writer still owns the Session.'],
+      },
     ],
   },
   {
@@ -3512,6 +3572,14 @@ export const EVENT_API: readonly EventApiEntry[] = [
     description: 'A Session became visible to Session list consumers.',
     parameters: [{ name: 'summary', description: 'initial list row for the Session.' }],
   },
+  {
+    name: 'api-session/before-delete',
+    mode: 'parallel',
+    signature: '\'api-session/before-delete\'(sessionId: SessionId): Promise<void>',
+    summary: 'Writers have stopped; consumers can persist independent summaries before the log is removed.',
+    description: 'Writers have stopped; consumers can persist independent summaries before the log is removed.',
+    parameters: [{ name: 'sessionId', description: 'Session selected for permanent deletion.' }],
+  },
   {
     name: 'api-session/error',
     mode: 'emit',
@@ -3568,6 +3636,14 @@ export const EVENT_API: readonly EventApiEntry[] = [
     description: 'Recover a failed summary request by synchronously recording a durable change to its selected input. Return true only after making progress; the provider re-derives and re-prices the selection before retrying. Call next() when the failure cannot be recovered. Decisions survive a later summary failure or cancellation.',
     parameters: [{ name: 'payload', description: '.signal - optional compaction cancellation signal.' }, { name: 'next', description: 'delegate to the next recovery listener.' }],
   },
+  {
+    name: 'connection/fetch',
+    mode: 'waterfall',
+    signature: '\'connection/fetch\'(exchange: ConnectionFetchRequest, next: () => Promise<Response>): Promise<Response>',
+    summary: 'Wrap authenticated API bodies for deployment transfer accounting and pacing.',
+    description: 'Wrap authenticated API bodies for deployment transfer accounting and pacing.',
+    parameters: [{ name: 'exchange', description: 'Authenticated request; listeners may wrap its body before delegating.' }, { name: 'next', description: 'Delegate to the next wrapper or registered API handler.' }],
+  },
   {
     name: 'connection/request',
     mode: 'waterfall',
@@ -4336,6 +4412,10 @@ export const TYPE_API: readonly TypeApiEntry[] = [
     name: 'ConnectionFetchMethod',
     declaration: 'export type ConnectionFetchMethod = \'GET\' | \'HEAD\' | \'POST\';',
   },
+  {
+    name: 'ConnectionFetchRequest',
+    declaration: 'export interface ConnectionFetchRequest {\n    request: Request;\n}',
+  },
   {
     name: 'ConnectionFetchRoute',
     declaration: 'export interface ConnectionFetchRoute {\n    readonly path: string;\n    readonly methods: readonly ConnectionFetchMethod[];\n    readonly requestBody: ConnectionRequestBodyMode;\n    readonly fetch: (request: Request) => Promise<Response>;\n}',
@@ -6804,6 +6884,10 @@ export const TYPE_API: readonly TypeApiEntry[] = [
     name: 'ToolSchema',
     declaration: 'export interface ToolSchema {\n    name: string;\n    description: string;\n    parameters: Record<string, unknown>;\n}',
   },
+  {
+    name: 'TrafficOwner',
+    declaration: 'export type TrafficOwner = SessionId | \'host\';',
+  },
   {
     name: 'TurnEndCancelCause',
     declaration: 'export type TurnEndCancelCause = AgentCancelCause | {\n    readonly kind: \'legacy\';\n};',
diff --git a/packages/session/session-persistence-jsonl/README.i18n.yaml b/packages/session/session-persistence-jsonl/README.i18n.yaml
index b0af7efebf..3d173912eb 100644
--- a/packages/session/session-persistence-jsonl/README.i18n.yaml
+++ b/packages/session/session-persistence-jsonl/README.i18n.yaml
@@ -2,5 +2,5 @@
 # side as of the last confirmed-consistent state. Both languages carry equal authority;
 # after editing either side, bring the other along and re-record with:
 #   pnpm run verify-translation-pairing --write packages/session/session-persistence-jsonl/README.md
-README.md: fc431ff27bcd236c4d44b8d3e7b20d4291b936d4
-README.zh.md: 3b265bf43f058d4c0711568df156825febed4d24
+README.md: 51b3bdc9dccc4dc494913bddf0415aa60472d5db
+README.zh.md: d205a5be0a57512ae3b113f34cc26edeeb5d366e
diff --git a/packages/session/session-persistence-jsonl/README.md b/packages/session/session-persistence-jsonl/README.md
index fc431ff27b..51b3bdc9dc 100644
--- a/packages/session/session-persistence-jsonl/README.md
+++ b/packages/session/session-persistence-jsonl/README.md
@@ -79,6 +79,8 @@ The current-generation scanner applies the current codec owner’s structural ad
 
 ### Reading the logs
 
+An explicitly mounted management consumer can call `delete(id)` to permanently remove a Session directory and its generations. Active writers must close first; another backend's held write lease also prevents deletion. This operation retains neighboring Sessions and the workspace directory.
+
 `open(id, 'read'|'write')` selects the highest canonical generation. Current input follows the ordinary fast path. For historical input, a read open decodes and migrates the source once, validates the current logical result, and returns it without publishing a successor. A write open reuses that revision-keyed preparation when available, or performs the same preparation, then encodes a same-directory temporary file in bounded chunks, verifies it in a Worker Thread, rechecks the source revision, and publishes the current successor without overwrite before returning. The source remains byte-identical. Source drift after preparation rejects that write open without replacing the logical history already returned to readers; a later write open prepares the new revision. The backend marks decoded event graphs `shared-frozen` when it freezes them before memoization; handle reads and slices preserve that state, including empty slices. Only an unmaterialized pending log reports `detached`. `stat(id)` and `list()` select and translate only the highest generation header without reading event rows or starting migration; snapshots carry `sizeBytes` and a best-effort stat-derived revision for the selected file. With `compression: 'none'`, the log is newline-delimited text an external reader can consume directly; the compressed default must be read through the backend.
 
 -----
diff --git a/packages/session/session-persistence-jsonl/README.zh.md b/packages/session/session-persistence-jsonl/README.zh.md
index 3b265bf43f..d205a5be0a 100644
--- a/packages/session/session-persistence-jsonl/README.zh.md
+++ b/packages/session/session-persistence-jsonl/README.zh.md
@@ -79,6 +79,8 @@ kind: "package-reference"
 
 ### 读取日志
 
+显式挂载的管理方可调用 `delete(id)`，永久删除一个会话目录及其所有代际。活动写入者必须先关闭；其他后端仍持有的写租约也会阻止删除。此操作保留相邻会话和工作区目录。
+
 `open(id, 'read'|'write')` 选择最高规范 generation。当前格式输入走普通快速路径。对于历史输入，只读 open 会单遍解码并迁移源、校验当前逻辑结果，然后在不发布后继的情况下返回。写 open 会在可用时复用按 revision 为键的 preparation，否则执行同一套 preparation，再按有界分片编码同目录临时文件、在 Worker Thread 中校验、复查源修订，并在返回前以不覆盖方式发布当前后继。源保持逐字节不变。如果源在 preparation 后发生变化，该次写 open 会失败，已经返回给读方的逻辑历史不会被替换；后续写 open 会针对新的 revision 重新执行 preparation。后端在 memo 化前冻结已解码的 event graph，并在此时将其标记为 `shared-frozen`；句柄读取和 slice 即使为空也保留该状态。只有尚未实体化的 pending 空日志报告 `detached`。`stat(id)` 与 `list()` 只选择并转换最高 generation 的 header，不读取事件行，也不启动迁移；快照携带所选文件的 `sizeBytes` 与尽力而为的 stat 派生修订号。选择 `compression: 'none'` 后，日志是外部读取方可直接消费的换行分隔文本；压缩默认值必须经后端读取。
 
 -----
diff --git a/packages/session/session-persistence-jsonl/src/index.ts b/packages/session/session-persistence-jsonl/src/index.ts
index ba4f8996f0..b7917d2cb6 100644
--- a/packages/session/session-persistence-jsonl/src/index.ts
+++ b/packages/session/session-persistence-jsonl/src/index.ts
@@ -14,7 +14,7 @@ import {
 } from '@deepseek-ai/dsh-session-format-catalog'
 import { readdirSync, type Dirent } from 'node:fs'
 import { open, mkdir, readdir, realpath, link, rm, stat, truncate } from 'node:fs/promises'
-import { dirname, join, resolve } from 'node:path'
+import { dirname, isAbsolute, join, relative, resolve } from 'node:path'
 import { performance } from 'node:perf_hooks'
 import { scheduler } from 'node:timers/promises'
 import { randomBytes } from 'node:crypto'
@@ -285,6 +285,32 @@ class JsonlSessionPersistence extends SessionPersistence {
     this.tracker.install(ctx)
   }
 
+  /**
+   * Delete all generations of one Session after excluding active writers.
+   * @param id - Session explicitly selected for permanent deletion.
+   * @returns False when no Session exists.
+   */
+  override async delete(id: SessionId): Promise<boolean> {
+    this.tracker.claimWrite(id)
+    let lease: SessionWriteLease | undefined
+    try {
+      const resolved = await this.findLog(id)
+      if (resolved === undefined) return false
+      const directory = dirname(resolved.currentPath)
+      const child = relative(this.root, directory)
+      if (child === '' || child === '..' || child.startsWith(`..${process.platform === 'win32' ? '\\' : '/'}`) || isAbsolute(child)) {
+        throw new Error('Session directory must be below the persistence root')
+      }
+      lease = await this.acquireLease(id, undefined, directory)
+      await rm(directory, { recursive: true })
+      this.coldLogMemo.delete(id)
+      return true
+    } finally {
+      try { await lease?.release() }
+      finally { this.tracker.releaseClaim(id) }
+    }
+  }
+
   /**
    * Refusal-diagnostics hook: the absolute target path, without touching the filesystem.
    * @param meta - the stored header naming the session and its cwd.
diff --git a/packages/session/session-persistence/src/index.ts b/packages/session/session-persistence/src/index.ts
index f17ec2e777..152d738960 100644
--- a/packages/session/session-persistence/src/index.ts
+++ b/packages/session/session-persistence/src/index.ts
@@ -196,6 +196,17 @@ export abstract class SessionPersistence extends Service {
    * @returns one snapshot per stored session.
    */
   abstract list(options?: SessionPersistenceListOptions): Promise<readonly SessionPersistenceSnapshot[]>
+
+  /**
+   * Permanently delete an entire stored Session, including every historical generation.
+   * Active write ownership must be released before calling this operation.
+   * @param id - Session explicitly selected for deletion.
+   * @returns Whether a stored Session existed.
+   * @throws When the provider does not support deletion or a writer still owns the Session.
+   */
+  delete(id: SessionId): Promise<boolean> {
+    return Promise.reject(new Error(`Session deletion is not supported by ${this.constructor.name}: ${id}`))
+  }
 }
 
 export default SessionPersistence
diff --git a/packages/util/http-proxy/src/install.ts b/packages/util/http-proxy/src/install.ts
index b3aacad2da..6d5d47aa45 100644
--- a/packages/util/http-proxy/src/install.ts
+++ b/packages/util/http-proxy/src/install.ts
@@ -171,7 +171,10 @@ async function createPolicyDispatcher(policy: ProxyPolicy): Promise<Dispatcher>
  * @param policy - the resolved policy to install.
  * @returns a disposer restoring the previous dispatcher, policy, and environment, then closing the agent.
  */
-async function installGlobalProxy(policy: ProxyPolicy): Promise<() => Promise<void>> {
+async function installGlobalProxy(
+  policy: ProxyPolicy,
+  decorate?: (dispatcher: Dispatcher) => Dispatcher,
+): Promise<() => Promise<void>> {
   const previousPolicy = active
   if (policy.source === 'none') {
     // A direct policy mounted over an installed one must actually stop proxying. Recording the policy
@@ -209,7 +212,8 @@ async function installGlobalProxy(policy: ProxyPolicy): Promise<() => Promise<vo
   const { getGlobalDispatcher, setGlobalDispatcher } = await import('undici')
   const previousDispatcher = getGlobalDispatcher()
   const previousInstalled = installed
-  const agent = await createPolicyDispatcher(policy)
+  const baseAgent = await createPolicyDispatcher(policy)
+  const agent = decorate === undefined ? baseAgent : decorate(baseAgent)
   setGlobalDispatcher(agent)
   active = policy
   installed = agent
@@ -291,15 +295,17 @@ export function proxyEnvironmentForChild(): Readonly<Record<string, string | und
  *
  * @param env - the launch environment, whose own layering already prefers real variables over `.env` files.
  * @param report - receives one message per rejected value, in the order the values were considered.
+ * @param decorate - Optional owner of the created dispatcher, used for Session-attributed proxy pools; its close must close the supplied dispatcher.
  * @returns a disposer restoring the previous dispatcher, policy, and environment.
  */
 export async function installProxyFromEnvironment(
   env: EnvLookup,
   report: (message: string) => void,
+  decorate?: (dispatcher: Dispatcher) => Dispatcher,
 ): Promise<() => Promise<void>> {
   const { policy, diagnostics } = resolveProxyPolicy(env)
   for (const diagnostic of diagnostics) report(diagnostic.message)
-  return await installGlobalProxy(policy)
+  return await installGlobalProxy(policy, decorate)
 }
 
 /**
diff --git a/pnpm-lock.yaml b/pnpm-lock.yaml
index 13ab4ef874..eaa916aa19 100644
--- a/pnpm-lock.yaml
+++ b/pnpm-lock.yaml
@@ -86,7 +86,7 @@ importers:
         version: 4.1.8(vitest@4.1.8)
       '@yao-pkg/pkg':
         specifier: 6.21.0
-        version: 6.21.0(patch_hash=07f08c630d0d3886d81fd4600116d07e4871ddcd57f92cb22746b6d246cdc337)(supports-color@9.4.0)
+        version: 6.21.0(patch_hash=07f08c630d0d3886d81fd4600116d07e4871ddcd57f92cb22746b6d246cdc337)
       '@yarnpkg/cli-dist':
         specifier: 4.17.1
         version: 4.17.1
@@ -104,7 +104,7 @@ importers:
         version: 3.0.1
       js-yaml:
         specifier: ^4.2.0
-        version: 4.2.0
+        version: 4.3.1
       jscpd:
         specifier: ^5.0.12
         version: 5.0.12
@@ -119,7 +119,7 @@ importers:
         version: 1.32.0
       mdast-util-from-markdown:
         specifier: ^2.0.3
-        version: 2.0.3(supports-color@9.4.0)
+        version: 2.0.3
       mdast-util-gfm:
         specifier: ^3.1.0
         version: 3.1.0
@@ -155,7 +155,7 @@ importers:
         version: 6.0.3
       vite-tsconfig-paths:
         specifier: ^6.1.1
-        version: 6.1.1(supports-color@9.4.0)(typescript@6.0.3)(vite@8.0.16(@types/node@22.20.0)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
+        version: 6.1.1(typescript@6.0.3)(vite@8.0.16(@types/node@22.20.0)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
       vitest:
         specifier: ^4.1.8
         version: 4.1.8(@opentelemetry/api@1.9.1)(@types/node@22.20.0)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@22.20.0)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
@@ -222,6 +222,45 @@ importers:
       '@deepseek-ai/dsh-experimental-agent-team-web-profile':
         specifier: workspace:^
         version: link:../../packages/experimental/agent-team-web-profile
+      '@deepseek-ai/dsh-experimental-glass-skin':
+        specifier: workspace:^
+        version: link:../../packages/experimental/glass-skin
+      '@deepseek-ai/dsh-experimental-interactive-results':
+        specifier: workspace:^
+        version: link:../../packages/experimental/interactive-results
+      '@deepseek-ai/dsh-experimental-mobile-layout':
+        specifier: workspace:^
+        version: link:../../packages/experimental/mobile-layout
+      '@deepseek-ai/dsh-experimental-network-usage':
+        specifier: workspace:^
+        version: link:../../packages/experimental/network-usage
+      '@deepseek-ai/dsh-experimental-page-login':
+        specifier: workspace:^
+        version: link:../../packages/experimental/page-login
+      '@deepseek-ai/dsh-experimental-personal-instructions':
+        specifier: workspace:^
+        version: link:../../packages/experimental/personal-instructions
+      '@deepseek-ai/dsh-experimental-task-board':
+        specifier: workspace:^
+        version: link:../../packages/experimental/task-board
+      '@deepseek-ai/dsh-experimental-usage-billing':
+        specifier: workspace:^
+        version: link:../../packages/experimental/usage-billing
+      '@deepseek-ai/dsh-experimental-usage-dashboard':
+        specifier: workspace:^
+        version: link:../../packages/experimental/usage-dashboard
+      '@deepseek-ai/dsh-experimental-workspace-download':
+        specifier: workspace:^
+        version: link:../../packages/experimental/workspace-download
+      '@deepseek-ai/dsh-experimental-workspace-editor':
+        specifier: workspace:^
+        version: link:../../packages/experimental/workspace-editor
+      '@deepseek-ai/dsh-experimental-workspace-management':
+        specifier: workspace:^
+        version: link:../../packages/experimental/workspace-management
+      '@deepseek-ai/dsh-experimental-workspace-pages':
+        specifier: workspace:^
+        version: link:../../packages/experimental/workspace-pages
       '@deepseek-ai/dsh-fs-local':
         specifier: workspace:^
         version: link:../../packages/fs/fs-local
@@ -389,7 +428,7 @@ importers:
         version: 15.0.0
       js-yaml:
         specifier: ^4.2.0
-        version: 4.2.0
+        version: 4.3.1
       node-addon-require-builtin:
         specifier: ^0.1.6
         version: 0.1.6
@@ -544,7 +583,7 @@ importers:
         version: link:../../packages/util/home-paths
       '@electron/get':
         specifier: ^5.1.0
-        version: 5.1.0(supports-color@9.4.0)
+        version: 5.1.0
       '@electron/notarize':
         specifier: 2.5.0
         version: 2.5.0
@@ -571,7 +610,7 @@ importers:
         version: 26.15.3(electron-builder-squirrel-windows@26.15.3)
       extract-zip:
         specifier: ^2.0.1
-        version: 2.0.1(supports-color@9.4.0)
+        version: 2.0.1
       fflate:
         specifier: ^0.8.2
         version: 0.8.3
@@ -688,13 +727,13 @@ importers:
         version: 8.18.1
       '@vitejs/plugin-react':
         specifier: ^4.0.0
-        version: 4.7.0(supports-color@9.4.0)(vite@6.4.3(@types/node@22.20.0)(jiti@2.7.0)(lightningcss@1.32.0)(tsx@4.22.4)(yaml@2.9.0))
+        version: 4.7.0(vite@6.4.3(@types/node@22.20.0)(jiti@2.7.0)(lightningcss@1.32.0)(tsx@4.22.4)(yaml@2.9.0))
       fflate:
         specifier: ^0.8.2
         version: 0.8.3
       http-server:
         specifier: ^14.1.1
-        version: 14.1.1(supports-color@9.4.0)
+        version: 14.1.1
       playwright:
         specifier: ^1.49.0
         version: 1.61.1
@@ -1416,7 +1455,7 @@ importers:
         version: link:../../util/package-manifest
       js-yaml:
         specifier: ^4.2.0
-        version: 4.2.0
+        version: 4.3.1
       node-addon-require-builtin:
         specifier: ^0.1.6
         version: 0.1.6
@@ -3805,7 +3844,7 @@ importers:
         version: 0.16.47
       mdast-util-from-markdown:
         specifier: ^2.0.3
-        version: 2.0.3(supports-color@9.4.0)
+        version: 2.0.3
       mdast-util-gfm:
         specifier: ^3.1.0
         version: 3.1.0
@@ -6704,6 +6743,40 @@ importers:
         specifier: workspace:^
         version: link:../../core/tools
 
+  packages/experimental/glass-skin:
+    dependencies:
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-settings':
+        specifier: workspace:^
+        version: link:../../client/ui-settings
+      '@deepseek-ai/dsh-client-ui-sidebar-right':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar-right
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
   packages/experimental/inspector:
     dependencies:
       '@deepseek-ai/dsh-app-boot':
@@ -6747,198 +6820,847 @@ importers:
         specifier: ^4.19.2
         version: 4.22.4
 
-  packages/experimental/ptc-runtime-python:
+  packages/experimental/interactive-results:
+    dependencies:
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
+        specifier: workspace:^
+        version: link:../../session/session-persistence
+      '@deepseek-ai/dsh-system-prompt':
+        specifier: workspace:^
+        version: link:../../core/system-prompt
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+      zod:
+        specifier: ^4.4.3
+        version: 4.4.3
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-chat':
+        specifier: workspace:^
+        version: link:../../client/ui-chat
+      '@deepseek-ai/dsh-client-ui-conversation':
+        specifier: workspace:^
+        version: link:../../client/ui-conversation
+      '@deepseek-ai/dsh-client-ui-deliverables':
+        specifier: workspace:^
+        version: link:../../client/ui-deliverables
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
+        specifier: workspace:^
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/mobile-layout:
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-sidebar-right':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar-right
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/network-usage:
+    dependencies:
+      '@deepseek-ai/dsh-agent':
+        specifier: workspace:^
+        version: link:../../core/agent
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-http-proxy':
+        specifier: workspace:^
+        version: link:../../util/http-proxy
+      '@deepseek-ai/dsh-sandbox':
+        specifier: workspace:^
+        version: link:../../sandbox/sandbox
+      '@deepseek-ai/dsh-sandbox-local':
+        specifier: workspace:^
+        version: link:../../sandbox/sandbox-local
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
+        specifier: workspace:^
+        version: link:../../session/session-persistence
+      '@deepseek-ai/dsh-storage-domain':
+        specifier: workspace:^
+        version: link:../../storage/storage-domain
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+      ipaddr.js:
+        specifier: ^2.5.0
+        version: 2.5.0
+      undici:
+        specifier: ^8.10.0
+        version: 8.10.0
+      zod:
+        specifier: ^4.4.3
+        version: 4.4.3
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-store':
+        specifier: workspace:^
+        version: link:../../client/store
+      '@deepseek-ai/dsh-client-ui-conversation':
+        specifier: workspace:^
+        version: link:../../client/ui-conversation
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
+        specifier: workspace:^
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-sidebar':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-client-ui-workspace':
+        specifier: workspace:^
+        version: link:../../client/ui-workspace
+      '@deepseek-ai/dsh-experimental-usage-dashboard':
+        specifier: workspace:^
+        version: link:../usage-dashboard
+      '@deepseek-ai/dsh-storage':
+        specifier: workspace:^
+        version: link:../../storage/storage
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/page-login:
+    dependencies:
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-host-webserver':
+        specifier: workspace:^
+        version: link:../../host/webserver
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-primitives':
+        specifier: workspace:^
+        version: link:../../client/ui-primitives
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-sidebar':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@types/node':
+        specifier: ^22.20.0
+        version: 22.20.0
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/personal-instructions:
     dependencies:
+      '@deepseek-ai/dsh-host-webserver':
+        specifier: workspace:^
+        version: link:../../host/webserver
       '@deepseek-ai/schemastery':
         specifier: link:../../../vendor/schemastery
         version: link:../../../vendor/schemastery
     devDependencies:
       '@deepseek-ai/cordis':
         specifier: workspace:^
-        version: link:../../../vendor/cordis
-      '@deepseek-ai/dsh-ptc-runtime':
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-settings':
+        specifier: workspace:^
+        version: link:../../client/ui-settings
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-system-prompt':
+        specifier: workspace:^
+        version: link:../../core/system-prompt
+      '@types/node':
+        specifier: ^22.20.0
+        version: 22.20.0
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/ptc-runtime-python:
+    dependencies:
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-ptc-runtime':
+        specifier: workspace:^
+        version: link:../../ptc-runtime/ptc-runtime
+      '@deepseek-ai/dsh-timeout':
+        specifier: workspace:^
+        version: link:../../util/timeout
+      '@deepseek-ai/dsh-util-values':
+        specifier: workspace:^
+        version: link:../../util/values
+
+  packages/experimental/task-board:
+    dependencies:
+      '@deepseek-ai/dsh-api-session-controller':
+        specifier: workspace:^
+        version: link:../../api/session-controller
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-storage-domain':
+        specifier: workspace:^
+        version: link:../../storage/storage-domain
+      '@deepseek-ai/dsh-workspace':
+        specifier: workspace:^
+        version: link:../../workspace/workspace
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+      zod:
+        specifier: ^4.4.3
+        version: 4.4.3
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
+        specifier: workspace:^
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-sidebar':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-client-ui-workspace':
+        specifier: workspace:^
+        version: link:../../client/ui-workspace
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/tool-agent-team:
+    dependencies:
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-agent':
+        specifier: workspace:^
+        version: link:../../core/agent
+      '@deepseek-ai/dsh-agent-loop':
+        specifier: workspace:^
+        version: link:../../core/agent-loop
+      '@deepseek-ai/dsh-agent-loop-testkit':
+        specifier: workspace:^
+        version: link:../../test-support/agent-loop-testkit
+      '@deepseek-ai/dsh-experimental-agent-team':
+        specifier: workspace:^
+        version: link:../agent-team
+      '@deepseek-ai/dsh-llm':
+        specifier: workspace:^
+        version: link:../../llm/llm
+      '@deepseek-ai/dsh-llm-deepseek':
+        specifier: workspace:^
+        version: link:../../llm/llm-deepseek
+      '@deepseek-ai/dsh-scope':
+        specifier: workspace:^
+        version: link:../../core/scope
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence-jsonl':
+        specifier: workspace:^
+        version: link:../../session/session-persistence-jsonl
+      '@deepseek-ai/dsh-session-projection':
+        specifier: workspace:^
+        version: link:../../session/session-projection
+      '@deepseek-ai/dsh-session-query':
+        specifier: workspace:^
+        version: link:../../session-query/session-query
+      '@deepseek-ai/dsh-subagent':
+        specifier: workspace:^
+        version: link:../../subagent/subagent
+      '@deepseek-ai/dsh-subagent-fork-in-process':
+        specifier: workspace:^
+        version: link:../../subagent/subagent-fork-in-process
+      '@deepseek-ai/dsh-subagent-spawn-in-process':
+        specifier: workspace:^
+        version: link:../../subagent/subagent-spawn-in-process
+      '@deepseek-ai/dsh-system-prompt':
+        specifier: workspace:^
+        version: link:../../core/system-prompt
+      '@deepseek-ai/dsh-tool-subagent-control':
+        specifier: workspace:^
+        version: link:../../subagent/tool-subagent-control
+      '@deepseek-ai/dsh-tools':
+        specifier: workspace:^
+        version: link:../../core/tools
+
+  packages/experimental/usage-billing:
+    dependencies:
+      '@deepseek-ai/dsh-api-session-controller':
+        specifier: workspace:^
+        version: link:../../api/session-controller
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-credentials':
+        specifier: workspace:^
+        version: link:../../credentials/credentials
+      '@deepseek-ai/dsh-llm':
+        specifier: workspace:^
+        version: link:../../llm/llm
+      '@deepseek-ai/dsh-llm-retry':
+        specifier: workspace:^
+        version: link:../../llm/llm-retry
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
+        specifier: workspace:^
+        version: link:../../session/session-persistence
+      '@deepseek-ai/dsh-storage-domain':
+        specifier: workspace:^
+        version: link:../../storage/storage-domain
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+      zod:
+        specifier: ^4.4.3
+        version: 4.4.3
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-store':
+        specifier: workspace:^
+        version: link:../../client/store
+      '@deepseek-ai/dsh-client-ui-conversation':
+        specifier: workspace:^
+        version: link:../../client/ui-conversation
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
+        specifier: workspace:^
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-sidebar':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-client-ui-workspace':
+        specifier: workspace:^
+        version: link:../../client/ui-workspace
+      '@deepseek-ai/dsh-experimental-usage-dashboard':
+        specifier: workspace:^
+        version: link:../usage-dashboard
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/usage-dashboard:
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-store':
+        specifier: workspace:^
+        version: link:../../client/store
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-sidebar':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/webworker-packer:
+    dependencies:
+      '@deepseek-ai/cordis-plugin-include':
+        specifier: workspace:^
+        version: link:../../../vendor/include
+      '@deepseek-ai/dsh-experimental-webworker-runtime':
+        specifier: workspace:^
+        version: link:../webworker-runtime
+      '@deepseek-ai/dsh-home-paths':
+        specifier: workspace:^
+        version: link:../../util/home-paths
+      js-yaml:
+        specifier: ^4.2.0
+        version: 4.3.1
+      picomatch:
+        specifier: ^4.0.4
+        version: 4.0.4
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@types/js-yaml':
+        specifier: ^4.0.9
+        version: 4.0.9
+      '@types/picomatch':
+        specifier: ^3.0.2
+        version: 3.0.2
+
+  packages/experimental/webworker-runtime:
+    dependencies:
+      '@deepseek-ai/dsh-client-web':
+        specifier: workspace:^
+        version: link:../../client/web
+      '@deepseek-ai/dsh-util-crypto':
+        specifier: workspace:^
+        version: link:../../util/crypto
+      '@noble/hashes':
+        specifier: ^2.3.0
+        version: 2.3.0
+      '@yarnpkg/parsers':
+        specifier: ^3.1.0
+        version: 3.1.0
+      acorn:
+        specifier: ^8.17.0
+        version: 8.17.0
+      buffer:
+        specifier: ^6.0.3
+        version: 6.0.3
+      picomatch:
+        specifier: ^4.0.4
+        version: 4.0.4
+      readable-stream:
+        specifier: ^4.7.0
+        version: 4.7.0
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/cordis-plugin-loader':
+        specifier: workspace:^
+        version: link:../../../vendor/loader
+      '@deepseek-ai/dsh-api-gateway':
+        specifier: workspace:^
+        version: link:../../api/gateway
+      '@deepseek-ai/dsh-bash-sandbox':
+        specifier: workspace:^
+        version: link:../../shell/bash-sandbox
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-client-file-upload':
+        specifier: workspace:^
+        version: link:../../client/file-upload
+      '@deepseek-ai/dsh-client-modules':
+        specifier: workspace:^
+        version: link:../../client/modules
+      '@deepseek-ai/dsh-host-webserver':
+        specifier: workspace:^
+        version: link:../../host/webserver
+      '@deepseek-ai/dsh-sandbox-local':
+        specifier: workspace:^
+        version: link:../../sandbox/sandbox-local
+      '@deepseek-ai/dsh-sandbox-policy':
+        specifier: workspace:^
+        version: link:../../sandbox/sandbox-policy
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence-jsonl':
+        specifier: workspace:^
+        version: link:../../session/session-persistence-jsonl
+      '@deepseek-ai/dsh-session-projection':
+        specifier: workspace:^
+        version: link:../../session/session-projection
+      '@deepseek-ai/dsh-subagent':
+        specifier: workspace:^
+        version: link:../../subagent/subagent
+      '@deepseek-ai/dsh-subprocess-local':
+        specifier: workspace:^
+        version: link:../../subprocess/subprocess-local
+      '@deepseek-ai/node-addon-system':
+        specifier: workspace:^
+        version: link:../../../native/system/packages/entry
+      '@types/picomatch':
+        specifier: ^3.0.2
+        version: 3.0.2
+      '@types/readable-stream':
+        specifier: ^4.0.24
+        version: 4.0.24
+      chokidar:
+        specifier: ^5.0.0
+        version: 5.0.0
+
+  packages/experimental/workspace-download:
+    dependencies:
+      '@deepseek-ai/dsh-api-workspace-files':
+        specifier: workspace:^
+        version: link:../../api/workspace-files
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
+        specifier: workspace:^
+        version: link:../../session/session-persistence
+      '@deepseek-ai/dsh-system-prompt':
+        specifier: workspace:^
+        version: link:../../core/system-prompt
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+      fflate:
+        specifier: ^0.8.2
+        version: 0.8.3
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-store':
+        specifier: workspace:^
+        version: link:../../client/store
+      '@deepseek-ai/dsh-client-ui-chat':
+        specifier: workspace:^
+        version: link:../../client/ui-chat
+      '@deepseek-ai/dsh-client-ui-conversation':
+        specifier: workspace:^
+        version: link:../../client/ui-conversation
+      '@deepseek-ai/dsh-client-ui-deliverables':
+        specifier: workspace:^
+        version: link:../../client/ui-deliverables
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-primitives':
+        specifier: workspace:^
+        version: link:../../client/ui-primitives
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
+        specifier: workspace:^
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-sidebar':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-sidebar-files':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar-files
+      '@deepseek-ai/dsh-client-ui-sidebar-right':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar-right
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-util-workspace-path':
+        specifier: workspace:^
+        version: link:../../util/workspace-path
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/workspace-editor:
+    dependencies:
+      '@deepseek-ai/dsh-client-connection':
+        specifier: workspace:^
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-session':
+        specifier: workspace:^
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
+        specifier: workspace:^
+        version: link:../../session/session-persistence
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
+      zod:
+        specifier: ^4.4.3
+        version: 4.4.3
+    devDependencies:
+      '@deepseek-ai/cordis':
+        specifier: workspace:^
+        version: link:../../../vendor/cordis
+      '@deepseek-ai/dsh-client-locale':
+        specifier: workspace:^
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-deliverables':
+        specifier: workspace:^
+        version: link:../../client/ui-deliverables
+      '@deepseek-ai/dsh-client-ui-layout':
+        specifier: workspace:^
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-primitives':
+        specifier: workspace:^
+        version: link:../../client/ui-primitives
+      '@deepseek-ai/dsh-client-ui-renderer':
+        specifier: workspace:^
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
+        specifier: workspace:^
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-sidebar-files':
+        specifier: workspace:^
+        version: link:../../client/ui-sidebar-files
+      '@deepseek-ai/dsh-client-ui-slots':
+        specifier: workspace:^
+        version: link:../../client/ui-slots
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
+
+  packages/experimental/workspace-management:
+    dependencies:
+      '@deepseek-ai/dsh-api-session-controller':
+        specifier: workspace:^
+        version: link:../../api/session-controller
+      '@deepseek-ai/dsh-client-connection':
         specifier: workspace:^
-        version: link:../../ptc-runtime/ptc-runtime
-      '@deepseek-ai/dsh-timeout':
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-session':
         specifier: workspace:^
-        version: link:../../util/timeout
-      '@deepseek-ai/dsh-util-values':
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
         specifier: workspace:^
-        version: link:../../util/values
-
-  packages/experimental/tool-agent-team:
-    dependencies:
+        version: link:../../session/session-persistence
+      '@deepseek-ai/dsh-workspace':
+        specifier: workspace:^
+        version: link:../../workspace/workspace
       '@deepseek-ai/schemastery':
         specifier: link:../../../vendor/schemastery
         version: link:../../../vendor/schemastery
+      zod:
+        specifier: ^4.4.3
+        version: 4.4.3
     devDependencies:
       '@deepseek-ai/cordis':
         specifier: workspace:^
         version: link:../../../vendor/cordis
-      '@deepseek-ai/dsh-agent':
-        specifier: workspace:^
-        version: link:../../core/agent
-      '@deepseek-ai/dsh-agent-loop':
-        specifier: workspace:^
-        version: link:../../core/agent-loop
-      '@deepseek-ai/dsh-agent-loop-testkit':
-        specifier: workspace:^
-        version: link:../../test-support/agent-loop-testkit
-      '@deepseek-ai/dsh-experimental-agent-team':
-        specifier: workspace:^
-        version: link:../agent-team
-      '@deepseek-ai/dsh-llm':
-        specifier: workspace:^
-        version: link:../../llm/llm
-      '@deepseek-ai/dsh-llm-deepseek':
-        specifier: workspace:^
-        version: link:../../llm/llm-deepseek
-      '@deepseek-ai/dsh-scope':
+      '@deepseek-ai/dsh-client-locale':
         specifier: workspace:^
-        version: link:../../core/scope
-      '@deepseek-ai/dsh-session':
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-store':
         specifier: workspace:^
-        version: link:../../core/session
-      '@deepseek-ai/dsh-session-persistence-jsonl':
+        version: link:../../client/store
+      '@deepseek-ai/dsh-client-ui-conversation':
         specifier: workspace:^
-        version: link:../../session/session-persistence-jsonl
-      '@deepseek-ai/dsh-session-projection':
+        version: link:../../client/ui-conversation
+      '@deepseek-ai/dsh-client-ui-layout':
         specifier: workspace:^
-        version: link:../../session/session-projection
-      '@deepseek-ai/dsh-session-query':
+        version: link:../../client/ui-layout
+      '@deepseek-ai/dsh-client-ui-primitives':
         specifier: workspace:^
-        version: link:../../session-query/session-query
-      '@deepseek-ai/dsh-subagent':
+        version: link:../../client/ui-primitives
+      '@deepseek-ai/dsh-client-ui-renderer':
         specifier: workspace:^
-        version: link:../../subagent/subagent
-      '@deepseek-ai/dsh-subagent-fork-in-process':
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
         specifier: workspace:^
-        version: link:../../subagent/subagent-fork-in-process
-      '@deepseek-ai/dsh-subagent-spawn-in-process':
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-settings-unarchive-sessions':
         specifier: workspace:^
-        version: link:../../subagent/subagent-spawn-in-process
-      '@deepseek-ai/dsh-system-prompt':
+        version: link:../../client/ui-settings-unarchive-sessions
+      '@deepseek-ai/dsh-client-ui-sidebar':
         specifier: workspace:^
-        version: link:../../core/system-prompt
-      '@deepseek-ai/dsh-tool-subagent-control':
+        version: link:../../client/ui-sidebar
+      '@deepseek-ai/dsh-client-ui-slots':
         specifier: workspace:^
-        version: link:../../subagent/tool-subagent-control
-      '@deepseek-ai/dsh-tools':
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-client-ui-workspace':
         specifier: workspace:^
-        version: link:../../core/tools
+        version: link:../../client/ui-workspace
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
 
-  packages/experimental/webworker-packer:
+  packages/experimental/workspace-pages:
     dependencies:
-      '@deepseek-ai/cordis-plugin-include':
-        specifier: workspace:^
-        version: link:../../../vendor/include
-      '@deepseek-ai/dsh-experimental-webworker-runtime':
-        specifier: workspace:^
-        version: link:../webworker-runtime
-      '@deepseek-ai/dsh-home-paths':
+      '@deepseek-ai/dsh-client-connection':
         specifier: workspace:^
-        version: link:../../util/home-paths
-      js-yaml:
-        specifier: ^4.2.0
-        version: 4.3.1
-      picomatch:
-        specifier: ^4.0.4
-        version: 4.0.4
-    devDependencies:
-      '@deepseek-ai/cordis':
+        version: link:../../client/connection
+      '@deepseek-ai/dsh-session':
         specifier: workspace:^
-        version: link:../../../vendor/cordis
-      '@types/js-yaml':
-        specifier: ^4.0.9
-        version: 4.0.9
-      '@types/picomatch':
-        specifier: ^3.0.2
-        version: 3.0.2
-
-  packages/experimental/webworker-runtime:
-    dependencies:
-      '@deepseek-ai/dsh-client-web':
+        version: link:../../core/session
+      '@deepseek-ai/dsh-session-persistence':
         specifier: workspace:^
-        version: link:../../client/web
-      '@deepseek-ai/dsh-util-crypto':
+        version: link:../../session/session-persistence
+      '@deepseek-ai/dsh-system-prompt':
         specifier: workspace:^
-        version: link:../../util/crypto
-      '@noble/hashes':
-        specifier: ^2.3.0
-        version: 2.3.0
-      '@yarnpkg/parsers':
-        specifier: ^3.1.0
-        version: 3.1.0
-      acorn:
-        specifier: ^8.17.0
-        version: 8.17.0
-      buffer:
-        specifier: ^6.0.3
-        version: 6.0.3
-      picomatch:
-        specifier: ^4.0.4
-        version: 4.0.4
-      readable-stream:
-        specifier: ^4.7.0
-        version: 4.7.0
+        version: link:../../core/system-prompt
+      '@deepseek-ai/schemastery':
+        specifier: link:../../../vendor/schemastery
+        version: link:../../../vendor/schemastery
     devDependencies:
       '@deepseek-ai/cordis':
         specifier: workspace:^
         version: link:../../../vendor/cordis
-      '@deepseek-ai/cordis-plugin-loader':
-        specifier: workspace:^
-        version: link:../../../vendor/loader
-      '@deepseek-ai/dsh-api-gateway':
-        specifier: workspace:^
-        version: link:../../api/gateway
-      '@deepseek-ai/dsh-bash-sandbox':
-        specifier: workspace:^
-        version: link:../../shell/bash-sandbox
-      '@deepseek-ai/dsh-client-connection':
-        specifier: workspace:^
-        version: link:../../client/connection
-      '@deepseek-ai/dsh-client-file-upload':
-        specifier: workspace:^
-        version: link:../../client/file-upload
-      '@deepseek-ai/dsh-client-modules':
+      '@deepseek-ai/dsh-client-locale':
         specifier: workspace:^
-        version: link:../../client/modules
-      '@deepseek-ai/dsh-host-webserver':
+        version: link:../../client/locale
+      '@deepseek-ai/dsh-client-ui-chat':
         specifier: workspace:^
-        version: link:../../host/webserver
-      '@deepseek-ai/dsh-sandbox-local':
+        version: link:../../client/ui-chat
+      '@deepseek-ai/dsh-client-ui-conversation':
         specifier: workspace:^
-        version: link:../../sandbox/sandbox-local
-      '@deepseek-ai/dsh-sandbox-policy':
+        version: link:../../client/ui-conversation
+      '@deepseek-ai/dsh-client-ui-deliverables':
         specifier: workspace:^
-        version: link:../../sandbox/sandbox-policy
-      '@deepseek-ai/dsh-session':
+        version: link:../../client/ui-deliverables
+      '@deepseek-ai/dsh-client-ui-renderer':
         specifier: workspace:^
-        version: link:../../core/session
-      '@deepseek-ai/dsh-session-persistence-jsonl':
+        version: link:../../client/ui-renderer
+      '@deepseek-ai/dsh-client-ui-session':
         specifier: workspace:^
-        version: link:../../session/session-persistence-jsonl
-      '@deepseek-ai/dsh-session-projection':
+        version: link:../../client/ui-session
+      '@deepseek-ai/dsh-client-ui-sidebar-files':
         specifier: workspace:^
-        version: link:../../session/session-projection
-      '@deepseek-ai/dsh-subagent':
+        version: link:../../client/ui-sidebar-files
+      '@deepseek-ai/dsh-client-ui-sidebar-right':
         specifier: workspace:^
-        version: link:../../subagent/subagent
-      '@deepseek-ai/dsh-subprocess-local':
+        version: link:../../client/ui-sidebar-right
+      '@deepseek-ai/dsh-client-ui-slots':
         specifier: workspace:^
-        version: link:../../subprocess/subprocess-local
-      '@deepseek-ai/node-addon-system':
+        version: link:../../client/ui-slots
+      '@deepseek-ai/dsh-util-workspace-path':
         specifier: workspace:^
-        version: link:../../../native/system/packages/entry
-      '@types/picomatch':
-        specifier: ^3.0.2
-        version: 3.0.2
-      '@types/readable-stream':
-        specifier: ^4.0.24
-        version: 4.0.24
-      chokidar:
-        specifier: ^5.0.0
-        version: 5.0.0
+        version: link:../../util/workspace-path
+      '@types/react':
+        specifier: ~18.3.1
+        version: 18.3.31
+      react:
+        specifier: ^18.2.0
+        version: 18.3.1
 
   packages/extensions/cordis-client-runner:
     devDependencies:
@@ -7856,7 +8578,7 @@ importers:
         version: link:../../../vendor/schemastery
       compression:
         specifier: ^1.8.1
-        version: 1.8.1(supports-color@9.4.0)
+        version: 1.8.1
       negotiator:
         specifier: ^1.0.0
         version: 1.0.0
@@ -8680,7 +9402,7 @@ importers:
         version: link:../../../vendor/schemastery
       js-yaml:
         specifier: ^4.1.0
-        version: 4.2.0
+        version: 4.3.1
       zod:
         specifier: ^4.4.3
         version: 4.4.3
@@ -11472,7 +12194,7 @@ importers:
         version: 4.3.1
       vitest:
         specifier: ^4.1.8
-        version: 4.1.8(@opentelemetry/api@1.9.1)(@types/node@25.9.3)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
+        version: 4.1.8(@opentelemetry/api@1.9.1)(@types/node@26.1.2)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
     devDependencies:
       '@deepseek-ai/cordis':
         specifier: workspace:^
@@ -11674,10 +12396,10 @@ importers:
         version: link:../../session/session-format
       js-yaml:
         specifier: ^4.2.0
-        version: 4.2.0
+        version: 4.3.1
       vitest:
         specifier: ^4.1.8
-        version: 4.1.8(@opentelemetry/api@1.9.1)(@types/node@25.9.3)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
+        version: 4.1.8(@opentelemetry/api@1.9.1)(@types/node@26.1.2)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
     devDependencies:
       '@deepseek-ai/cordis':
         specifier: workspace:^
@@ -12878,7 +13600,7 @@ importers:
         version: link:../cosmokit
       js-yaml:
         specifier: ^4.1.0
-        version: 4.2.0
+        version: 4.3.1
 
   vendor/loader:
     dependencies:
@@ -12944,19 +13666,19 @@ importers:
         version: 1.11.21
       debug:
         specifier: 4.4.3
-        version: 4.4.3(supports-color@9.4.0)
+        version: 4.4.3
       mermaid:
         specifier: 11.16.0
         version: 11.16.0
       vite:
         specifier: ^5.4.14
-        version: 5.4.21(@types/node@25.9.3)(lightningcss@1.32.0)
+        version: 5.4.21(@types/node@26.1.2)(lightningcss@1.32.0)
       vitepress:
         specifier: ^1.6.4
-        version: 1.6.4(@algolia/client-search@5.55.2)(@types/node@25.9.3)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3)
+        version: 1.6.4(@algolia/client-search@5.55.2)(@types/node@26.1.2)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3)
       vitepress-plugin-mermaid:
         specifier: ^2.0.17
-        version: 2.0.17(mermaid@11.16.0)(vitepress@1.6.4(@algolia/client-search@5.55.2)(@types/node@25.9.3)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3))
+        version: 2.0.17(mermaid@11.16.0)(vitepress@1.6.4(@algolia/client-search@5.55.2)(@types/node@26.1.2)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3))
       vue:
         specifier: 3.5.39
         version: 3.5.39(typescript@6.0.3)
@@ -15781,9 +16503,6 @@ packages:
   '@types/node@24.13.3':
     resolution: {integrity: sha512-Dh8vAsV36ig5wa9OX4pXvMc9D3Veibfw2wix0CUwYODLD8nkj9UsLjASr49nPg+2eKzxhBV+v7L8pXvT4e639Q==}
 
-  '@types/node@25.9.3':
-    resolution: {integrity: sha512-603BddQMv3pUcr4U2dhujk83N2tTDVr/34wII2B6bJy6g+8WD6yUb11jszNs0gdi4PesVWl7ABt8nYMVpnLUcg==}
-
   '@types/node@26.1.2':
     resolution: {integrity: sha512-Vu4a5UFA9rIIFJ7rB/Vaafh9lrCQszopTCx6KjFboXTGQbPNasehVR5TEiithSDGyd1DEiUByggTZsg8jukeIg==}
 
@@ -17786,10 +18505,6 @@ packages:
   js-tokens@4.0.0:
     resolution: {integrity: sha512-RdJUflcE3cUzKiMqQgsCu06FPu9UdIJO0beYbPhHN4k6apgJtifcoCtT9bcxOpYBtpD2kCM6Sbzg4CausW/PKQ==}
 
-  js-yaml@4.2.0:
-    resolution: {integrity: sha512-ePWsvanv0DWuDRsW8dnt+R4jQ31SCRCQ7hhNcPXZPsoBZiemuZNYGf7adZdqX2D86j6rvKp3RpCxVTSb8WQlOw==}
-    hasBin: true
-
   js-yaml@4.3.1:
     resolution: {integrity: sha512-CY6crGq313MX8GkwvB7tzgp99vjQxY1++5y10/BKN/GUfHqWaOGQMNZkBvqSzsZKWk/ijwHlWzzkLulsGHhjWQ==}
     hasBin: true
@@ -20462,20 +21177,20 @@ snapshots:
 
   '@babel/compat-data@7.29.7': {}
 
-  '@babel/core@7.29.7(supports-color@9.4.0)':
+  '@babel/core@7.29.7':
     dependencies:
       '@babel/code-frame': 7.29.7
       '@babel/generator': 7.29.7
       '@babel/helper-compilation-targets': 7.29.7
-      '@babel/helper-module-transforms': 7.29.7(@babel/core@7.29.7(supports-color@9.4.0))
+      '@babel/helper-module-transforms': 7.29.7(@babel/core@7.29.7)
       '@babel/helpers': 7.29.7
       '@babel/parser': 7.29.7
       '@babel/template': 7.29.7
-      '@babel/traverse': 7.29.7(supports-color@9.4.0)
+      '@babel/traverse': 7.29.7
       '@babel/types': 7.29.7
       '@jridgewell/remapping': 2.3.5
       convert-source-map: 2.0.0
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       gensync: 1.0.0-beta.2
       json5: 2.2.3
       semver: 6.3.1
@@ -20511,17 +21226,17 @@ snapshots:
 
   '@babel/helper-module-imports@7.29.7':
     dependencies:
-      '@babel/traverse': 7.29.7(supports-color@9.4.0)
+      '@babel/traverse': 7.29.7
       '@babel/types': 7.29.7
     transitivePeerDependencies:
       - supports-color
 
-  '@babel/helper-module-transforms@7.29.7(@babel/core@7.29.7(supports-color@9.4.0))':
+  '@babel/helper-module-transforms@7.29.7(@babel/core@7.29.7)':
     dependencies:
-      '@babel/core': 7.29.7(supports-color@9.4.0)
+      '@babel/core': 7.29.7
       '@babel/helper-module-imports': 7.29.7
       '@babel/helper-validator-identifier': 7.29.7
-      '@babel/traverse': 7.29.7(supports-color@9.4.0)
+      '@babel/traverse': 7.29.7
     transitivePeerDependencies:
       - supports-color
 
@@ -20550,14 +21265,14 @@ snapshots:
     dependencies:
       '@babel/types': 8.0.0-rc.6
 
-  '@babel/plugin-transform-react-jsx-self@7.29.7(@babel/core@7.29.7(supports-color@9.4.0))':
+  '@babel/plugin-transform-react-jsx-self@7.29.7(@babel/core@7.29.7)':
     dependencies:
-      '@babel/core': 7.29.7(supports-color@9.4.0)
+      '@babel/core': 7.29.7
       '@babel/helper-plugin-utils': 7.29.7
 
-  '@babel/plugin-transform-react-jsx-source@7.29.7(@babel/core@7.29.7(supports-color@9.4.0))':
+  '@babel/plugin-transform-react-jsx-source@7.29.7(@babel/core@7.29.7)':
     dependencies:
-      '@babel/core': 7.29.7(supports-color@9.4.0)
+      '@babel/core': 7.29.7
       '@babel/helper-plugin-utils': 7.29.7
 
   '@babel/runtime@7.29.7': {}
@@ -20568,7 +21283,7 @@ snapshots:
       '@babel/parser': 7.29.7
       '@babel/types': 7.29.7
 
-  '@babel/traverse@7.29.7(supports-color@9.4.0)':
+  '@babel/traverse@7.29.7':
     dependencies:
       '@babel/code-frame': 7.29.7
       '@babel/generator': 7.29.7
@@ -20576,7 +21291,7 @@ snapshots:
       '@babel/parser': 7.29.7
       '@babel/template': 7.29.7
       '@babel/types': 7.29.7
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
     transitivePeerDependencies:
       - supports-color
 
@@ -20741,7 +21456,7 @@ snapshots:
 
   '@electron/get@3.1.0':
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       env-paths: 2.2.1
       fs-extra: 8.1.0
       got: 11.8.6
@@ -20753,9 +21468,9 @@ snapshots:
     transitivePeerDependencies:
       - supports-color
 
-  '@electron/get@5.1.0(supports-color@9.4.0)':
+  '@electron/get@5.1.0':
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       env-paths: 3.0.0
       graceful-fs: 4.2.11
       progress: 2.0.3
@@ -20768,7 +21483,7 @@ snapshots:
 
   '@electron/notarize@2.5.0':
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       fs-extra: 9.1.0
       promise-retry: 2.0.1
     transitivePeerDependencies:
@@ -20777,7 +21492,7 @@ snapshots:
   '@electron/osx-sign@1.3.3(patch_hash=280e7b2816caf01423097d24e79107a66afeb563a348630ce9928ae8499a5861)':
     dependencies:
       compare-version: 0.1.2
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       fs-extra: 10.1.0
       isbinaryfile: 4.0.10
       minimist: 1.2.8
@@ -20788,7 +21503,7 @@ snapshots:
   '@electron/rebuild@4.2.0':
     dependencies:
       '@malept/cross-spawn-promise': 2.0.0
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       node-abi: 4.34.0
       node-api-version: 0.2.1
       node-gyp: 12.4.0
@@ -20800,7 +21515,7 @@ snapshots:
     dependencies:
       '@electron/asar': 3.4.1
       '@malept/cross-spawn-promise': 2.0.0
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       dir-compare: 4.2.0
       fs-extra: 11.4.0
       minimatch: 9.0.9
@@ -20811,7 +21526,7 @@ snapshots:
   '@electron/windows-sign@1.2.2':
     dependencies:
       cross-dirname: 0.1.0
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       fs-extra: 11.4.0
       minimist: 1.2.8
       postject: 1.0.0-alpha.6
@@ -21091,7 +21806,7 @@ snapshots:
   '@eslint/config-array@0.23.5':
     dependencies:
       '@eslint/object-schema': 3.0.5
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       minimatch: 10.2.5
     transitivePeerDependencies:
       - supports-color
@@ -21456,7 +22171,7 @@ snapshots:
 
   '@malept/flatpak-bundler@0.4.0':
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       fs-extra: 9.1.0
       lodash: 4.18.1
       tmp-promise: 3.0.3
@@ -22560,7 +23275,7 @@ snapshots:
 
   '@types/jsdom@28.0.3':
     dependencies:
-      '@types/node': 25.9.3
+      '@types/node': 22.20.0
       '@types/tough-cookie': 4.0.5
       parse5: 8.0.1
       undici-types: 7.24.6
@@ -22611,10 +23326,6 @@ snapshots:
     dependencies:
       undici-types: 7.18.2
 
-  '@types/node@25.9.3':
-    dependencies:
-      undici-types: 7.24.6
-
   '@types/node@26.1.2':
     dependencies:
       undici-types: 8.3.0
@@ -22730,11 +23441,11 @@ snapshots:
       d3-selection: 3.0.0
       d3-transition: 3.0.1(d3-selection@3.0.0)
 
-  '@vitejs/plugin-react@4.7.0(supports-color@9.4.0)(vite@6.4.3(@types/node@22.20.0)(jiti@2.7.0)(lightningcss@1.32.0)(tsx@4.22.4)(yaml@2.9.0))':
+  '@vitejs/plugin-react@4.7.0(vite@6.4.3(@types/node@22.20.0)(jiti@2.7.0)(lightningcss@1.32.0)(tsx@4.22.4)(yaml@2.9.0))':
     dependencies:
-      '@babel/core': 7.29.7(supports-color@9.4.0)
-      '@babel/plugin-transform-react-jsx-self': 7.29.7(@babel/core@7.29.7(supports-color@9.4.0))
-      '@babel/plugin-transform-react-jsx-source': 7.29.7(@babel/core@7.29.7(supports-color@9.4.0))
+      '@babel/core': 7.29.7
+      '@babel/plugin-transform-react-jsx-self': 7.29.7(@babel/core@7.29.7)
+      '@babel/plugin-transform-react-jsx-source': 7.29.7(@babel/core@7.29.7)
       '@rolldown/pluginutils': 1.0.0-beta.27
       '@types/babel__core': 7.20.5
       react-refresh: 0.17.0
@@ -22742,9 +23453,9 @@ snapshots:
     transitivePeerDependencies:
       - supports-color
 
-  '@vitejs/plugin-vue@5.2.4(vite@5.4.21(@types/node@25.9.3)(lightningcss@1.32.0))(vue@3.5.39(typescript@6.0.3))':
+  '@vitejs/plugin-vue@5.2.4(vite@5.4.21(@types/node@26.1.2)(lightningcss@1.32.0))(vue@3.5.39(typescript@6.0.3))':
     dependencies:
-      vite: 5.4.21(@types/node@25.9.3)(lightningcss@1.32.0)
+      vite: 5.4.21(@types/node@26.1.2)(lightningcss@1.32.0)
       vue: 3.5.39(typescript@6.0.3)
 
   '@vitest/coverage-v8@4.1.8(vitest@4.1.8)':
@@ -22991,11 +23702,11 @@ snapshots:
       - bare-buffer
       - react-native-b4a
 
-  '@yao-pkg/pkg@6.21.0(patch_hash=07f08c630d0d3886d81fd4600116d07e4871ddcd57f92cb22746b6d246cdc337)(supports-color@9.4.0)':
+  '@yao-pkg/pkg@6.21.0(patch_hash=07f08c630d0d3886d81fd4600116d07e4871ddcd57f92cb22746b6d246cdc337)':
     dependencies:
       '@babel/generator': 7.29.7
       '@babel/parser': 7.29.7
-      '@babel/traverse': 7.29.7(supports-color@9.4.0)
+      '@babel/traverse': 7.29.7
       '@babel/types': 7.29.7
       '@roberts_lando/vfs': 0.3.3
       '@yao-pkg/pkg-fetch': 3.6.4
@@ -23121,7 +23832,7 @@ snapshots:
       builder-util-runtime: 9.7.0
       chromium-pickle-js: 0.2.0
       ci-info: 4.3.1
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       dmg-builder: 26.15.3(electron-builder-squirrel-windows@26.15.3)
       dotenv: 16.6.1
       dotenv-expand: 11.0.7
@@ -23262,7 +23973,7 @@ snapshots:
     dependencies:
       bytes: 3.1.2
       content-type: 2.0.0
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       http-errors: 2.0.1
       iconv-lite: 0.7.3
       on-finished: 2.4.1
@@ -23322,7 +24033,7 @@ snapshots:
 
   builder-util-runtime@9.7.0:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       sax: 1.6.1
     transitivePeerDependencies:
       - supports-color
@@ -23333,7 +24044,7 @@ snapshots:
       builder-util-runtime: 9.7.0
       chalk: 4.1.2
       cross-spawn: 7.0.6
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       fs-extra: 10.1.0
       http-proxy-agent: 7.0.2
       https-proxy-agent: 7.0.6
@@ -23473,11 +24184,11 @@ snapshots:
     dependencies:
       mime-db: 1.54.0
 
-  compression@1.8.1(supports-color@9.4.0):
+  compression@1.8.1:
     dependencies:
       bytes: 3.1.2
       compressible: 2.0.18
-      debug: 2.6.9(supports-color@9.4.0)
+      debug: 2.6.9
       negotiator: 0.6.4
       on-headers: 1.1.0
       safe-buffer: 5.2.1
@@ -23768,17 +24479,13 @@ snapshots:
 
   dayjs@1.11.21: {}
 
-  debug@2.6.9(supports-color@9.4.0):
+  debug@2.6.9:
     dependencies:
       ms: 2.0.0
-    optionalDependencies:
-      supports-color: 9.4.0
 
-  debug@4.4.3(supports-color@9.4.0):
+  debug@4.4.3:
     dependencies:
       ms: 2.1.3
-    optionalDependencies:
-      supports-color: 9.4.0
 
   decimal.js@10.6.0: {}
 
@@ -23961,7 +24668,7 @@ snapshots:
   electron-winstaller@5.4.0:
     dependencies:
       '@electron/asar': 3.4.1
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       fs-extra: 7.0.1
       lodash: 4.18.1
       temp: 0.9.4
@@ -23973,7 +24680,7 @@ snapshots:
   electron@44.0.0:
     dependencies:
       '@electron-internal/extract-zip': 1.0.5
-      '@electron/get': 5.1.0(supports-color@9.4.0)
+      '@electron/get': 5.1.0
       '@types/node': 24.13.3
     transitivePeerDependencies:
       - supports-color
@@ -24162,7 +24869,7 @@ snapshots:
       '@types/estree': 1.0.9
       ajv: 6.15.0
       cross-spawn: 7.0.6
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       escape-string-regexp: 4.0.0
       eslint-scope: 9.1.2
       eslint-visitor-keys: 5.0.1
@@ -24270,7 +24977,7 @@ snapshots:
       content-type: 1.0.5
       cookie: 0.7.2
       cookie-signature: 1.2.2
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       depd: 2.0.0
       encodeurl: 2.0.0
       escape-html: 1.0.3
@@ -24297,9 +25004,9 @@ snapshots:
 
   extend@3.0.2: {}
 
-  extract-zip@2.0.1(supports-color@9.4.0):
+  extract-zip@2.0.1:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       get-stream: 5.2.0
       yauzl: 3.4.0
     optionalDependencies:
@@ -24364,7 +25071,7 @@ snapshots:
 
   finalhandler@2.1.1:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       encodeurl: 2.0.0
       escape-html: 1.0.3
       on-finished: 2.4.1
@@ -24714,7 +25421,7 @@ snapshots:
   http-proxy-agent@7.0.2:
     dependencies:
       agent-base: 7.1.4
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
     transitivePeerDependencies:
       - supports-color
 
@@ -24726,7 +25433,7 @@ snapshots:
     transitivePeerDependencies:
       - debug
 
-  http-server@14.1.1(supports-color@9.4.0):
+  http-server@14.1.1:
     dependencies:
       basic-auth: 2.0.1
       chalk: 4.1.2
@@ -24737,7 +25444,7 @@ snapshots:
       mime: 1.6.0
       minimist: 1.2.8
       opener: 1.5.2
-      portfinder: 1.0.38(supports-color@9.4.0)
+      portfinder: 1.0.38
       secure-compare: 3.0.1
       union: 0.5.0
       url-join: 4.0.1
@@ -24759,7 +25466,7 @@ snapshots:
   https-proxy-agent@7.0.6:
     dependencies:
       agent-base: 7.1.4
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
     transitivePeerDependencies:
       - supports-color
 
@@ -24897,10 +25604,6 @@ snapshots:
 
   js-tokens@4.0.0: {}
 
-  js-yaml@4.2.0:
-    dependencies:
-      argparse: 2.0.1
-
   js-yaml@4.3.1:
     dependencies:
       argparse: 2.0.1
@@ -25237,14 +25940,14 @@ snapshots:
       unist-util-is: 6.0.1
       unist-util-visit-parents: 6.0.2
 
-  mdast-util-from-markdown@2.0.3(supports-color@9.4.0):
+  mdast-util-from-markdown@2.0.3:
     dependencies:
       '@types/mdast': 4.0.4
       '@types/unist': 3.0.3
       decode-named-character-reference: 1.3.0
       devlop: 1.1.0
       mdast-util-to-string: 4.0.0
-      micromark: 4.0.2(supports-color@9.4.0)
+      micromark: 4.0.2
       micromark-util-decode-numeric-character-reference: 2.0.2
       micromark-util-decode-string: 2.0.1
       micromark-util-normalize-identifier: 2.0.1
@@ -25266,7 +25969,7 @@ snapshots:
     dependencies:
       '@types/mdast': 4.0.4
       devlop: 1.1.0
-      mdast-util-from-markdown: 2.0.3(supports-color@9.4.0)
+      mdast-util-from-markdown: 2.0.3
       mdast-util-to-markdown: 2.1.2
       micromark-util-normalize-identifier: 2.0.1
     transitivePeerDependencies:
@@ -25275,7 +25978,7 @@ snapshots:
   mdast-util-gfm-strikethrough@2.0.0:
     dependencies:
       '@types/mdast': 4.0.4
-      mdast-util-from-markdown: 2.0.3(supports-color@9.4.0)
+      mdast-util-from-markdown: 2.0.3
       mdast-util-to-markdown: 2.1.2
     transitivePeerDependencies:
       - supports-color
@@ -25285,7 +25988,7 @@ snapshots:
       '@types/mdast': 4.0.4
       devlop: 1.1.0
       markdown-table: 3.0.4
-      mdast-util-from-markdown: 2.0.3(supports-color@9.4.0)
+      mdast-util-from-markdown: 2.0.3
       mdast-util-to-markdown: 2.1.2
     transitivePeerDependencies:
       - supports-color
@@ -25294,14 +25997,14 @@ snapshots:
     dependencies:
       '@types/mdast': 4.0.4
       devlop: 1.1.0
-      mdast-util-from-markdown: 2.0.3(supports-color@9.4.0)
+      mdast-util-from-markdown: 2.0.3
       mdast-util-to-markdown: 2.1.2
     transitivePeerDependencies:
       - supports-color
 
   mdast-util-gfm@3.1.0:
     dependencies:
-      mdast-util-from-markdown: 2.0.3(supports-color@9.4.0)
+      mdast-util-from-markdown: 2.0.3
       mdast-util-gfm-autolink-literal: 2.0.1
       mdast-util-gfm-footnote: 2.1.0
       mdast-util-gfm-strikethrough: 2.0.0
@@ -25317,7 +26020,7 @@ snapshots:
       '@types/mdast': 4.0.4
       devlop: 1.1.0
       longest-streak: 3.1.0
-      mdast-util-from-markdown: 2.0.3(supports-color@9.4.0)
+      mdast-util-from-markdown: 2.0.3
       mdast-util-to-markdown: 2.1.2
       unist-util-remove-position: 5.0.0
     transitivePeerDependencies:
@@ -25565,10 +26268,10 @@ snapshots:
 
   micromark-util-types@2.0.2: {}
 
-  micromark@4.0.2(supports-color@9.4.0):
+  micromark@4.0.2:
     dependencies:
       '@types/debug': 4.1.13
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       decode-named-character-reference: 1.3.0
       devlop: 1.1.0
       micromark-core-commonmark: 2.0.3
@@ -26001,10 +26704,10 @@ snapshots:
       path-data-parser: 0.1.0
       points-on-curve: 0.2.0
 
-  portfinder@1.0.38(supports-color@9.4.0):
+  portfinder@1.0.38:
     dependencies:
       async: 3.2.6
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
     transitivePeerDependencies:
       - supports-color
 
@@ -26158,7 +26861,7 @@ snapshots:
 
   read-binary-file-arch@1.0.6:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
     transitivePeerDependencies:
       - supports-color
 
@@ -26358,7 +27061,7 @@ snapshots:
 
   router@2.2.0:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       depd: 2.0.0
       is-promise: 4.0.0
       parseurl: 1.3.3
@@ -26419,7 +27122,7 @@ snapshots:
 
   send@1.2.1:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       encodeurl: 2.0.0
       escape-html: 1.0.3
       etag: 1.8.1
@@ -26683,7 +27386,7 @@ snapshots:
 
   sumchecker@3.0.1:
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
     transitivePeerDependencies:
       - supports-color
 
@@ -27035,9 +27738,9 @@ snapshots:
       '@types/unist': 3.0.3
       vfile-message: 4.0.3
 
-  vite-tsconfig-paths@6.1.1(supports-color@9.4.0)(typescript@6.0.3)(vite@8.0.16(@types/node@22.20.0)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0)):
+  vite-tsconfig-paths@6.1.1(typescript@6.0.3)(vite@8.0.16(@types/node@22.20.0)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0)):
     dependencies:
-      debug: 4.4.3(supports-color@9.4.0)
+      debug: 4.4.3
       globrex: 0.1.2
       tsconfck: 3.1.6(typescript@6.0.3)
       vite: 8.0.16(@types/node@22.20.0)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0)
@@ -27045,13 +27748,13 @@ snapshots:
       - supports-color
       - typescript
 
-  vite@5.4.21(@types/node@25.9.3)(lightningcss@1.32.0):
+  vite@5.4.21(@types/node@26.1.2)(lightningcss@1.32.0):
     dependencies:
       esbuild: 0.21.5
       postcss: 8.5.15
       rollup: 4.62.2
     optionalDependencies:
-      '@types/node': 25.9.3
+      '@types/node': 26.1.2
       fsevents: 2.3.3
       lightningcss: 1.32.0
 
@@ -27101,14 +27804,14 @@ snapshots:
       tsx: 4.22.4
       yaml: 2.9.0
 
-  vitepress-plugin-mermaid@2.0.17(mermaid@11.16.0)(vitepress@1.6.4(@algolia/client-search@5.55.2)(@types/node@25.9.3)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3)):
+  vitepress-plugin-mermaid@2.0.17(mermaid@11.16.0)(vitepress@1.6.4(@algolia/client-search@5.55.2)(@types/node@26.1.2)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3)):
     dependencies:
       mermaid: 11.16.0
-      vitepress: 1.6.4(@algolia/client-search@5.55.2)(@types/node@25.9.3)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3)
+      vitepress: 1.6.4(@algolia/client-search@5.55.2)(@types/node@26.1.2)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3)
     optionalDependencies:
       '@mermaid-js/mermaid-mindmap': 9.3.0
 
-  vitepress@1.6.4(@algolia/client-search@5.55.2)(@types/node@25.9.3)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3):
+  vitepress@1.6.4(@algolia/client-search@5.55.2)(@types/node@26.1.2)(@types/react@18.3.31)(lightningcss@1.32.0)(postcss@8.5.15)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)(typescript@6.0.3):
     dependencies:
       '@docsearch/css': 3.8.2
       '@docsearch/js': 3.8.2(@algolia/client-search@5.55.2)(@types/react@18.3.31)(react-dom@18.3.1(react@18.3.1))(react@18.3.1)(search-insights@2.17.3)
@@ -27117,7 +27820,7 @@ snapshots:
       '@shikijs/transformers': 2.5.0
       '@shikijs/types': 2.5.0
       '@types/markdown-it': 14.1.2
-      '@vitejs/plugin-vue': 5.2.4(vite@5.4.21(@types/node@25.9.3)(lightningcss@1.32.0))(vue@3.5.39(typescript@6.0.3))
+      '@vitejs/plugin-vue': 5.2.4(vite@5.4.21(@types/node@26.1.2)(lightningcss@1.32.0))(vue@3.5.39(typescript@6.0.3))
       '@vue/devtools-api': 7.7.10
       '@vue/shared': 3.5.39
       '@vueuse/core': 12.8.2(typescript@6.0.3)
@@ -27126,7 +27829,7 @@ snapshots:
       mark.js: 8.11.1
       minisearch: 7.2.0
       shiki: 2.5.0
-      vite: 5.4.21(@types/node@25.9.3)(lightningcss@1.32.0)
+      vite: 5.4.21(@types/node@26.1.2)(lightningcss@1.32.0)
       vue: 3.5.39(typescript@6.0.3)
     optionalDependencies:
       postcss: 8.5.15
@@ -27220,7 +27923,7 @@ snapshots:
     transitivePeerDependencies:
       - msw
 
-  vitest@4.1.8(@opentelemetry/api@1.9.1)(@types/node@25.9.3)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0)):
+  vitest@4.1.8(@opentelemetry/api@1.9.1)(@types/node@26.1.2)(@vitest/coverage-v8@4.1.8)(happy-dom@20.11.6)(jsdom@29.1.1(@noble/hashes@2.3.0))(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0)):
     dependencies:
       '@vitest/expect': 4.1.8
       '@vitest/mocker': 4.1.8(vite@8.0.16(@types/node@26.1.2)(esbuild@0.28.1)(jiti@2.7.0)(tsx@4.22.4)(yaml@2.9.0))
@@ -27244,7 +27947,7 @@ snapshots:
       why-is-node-running: 2.3.0
     optionalDependencies:
       '@opentelemetry/api': 1.9.1
-      '@types/node': 25.9.3
+      '@types/node': 26.1.2
       '@vitest/coverage-v8': 4.1.8(vitest@4.1.8)
       happy-dom: 20.11.6
       jsdom: 29.1.1(@noble/hashes@2.3.0)
diff --git a/scripts/gen-cordis-catalog.ts b/scripts/gen-cordis-catalog.ts
index 2c7f7968cb..470037b481 100644
--- a/scripts/gen-cordis-catalog.ts
+++ b/scripts/gen-cordis-catalog.ts
@@ -122,6 +122,8 @@ export const SERVICE_PAGE: Record<string, string> = {
   sessionTelemetry: 'session-telemetry.md',
   agentTeams: 'agent-team.md',
   tokenMeter: 'token-meter.md',
+  networkUsage: 'network-usage.md',
+  networkProxy: 'network-usage.md',
   toolResultPruner: 'compaction.md',
   tools: 'tools.md',
   typert: 'typert.md',
@@ -740,6 +742,8 @@ export const FOUNDATION_TYPE_NAMES: ReadonlySet<string> = new Set([
 /** Project types deliberately documented outside the subsystems catalog. */
 export const TYPE_LINK_EXEMPTIONS: Readonly<Record<string, string>> = {
   ConnectionFetchHandler: 'shared Fetch dispatch is owned by packages/client/connection/src/rpc.ts',
+  ConnectionFetchRequest: 'authenticated body wrapping is owned by packages/client/connection/README.md#browser-authentication-and-request-trust',
+  TrafficOwner: 'Session and background traffic ownership is documented in packages/experimental/network-usage/README.md#understand-the-implementation',
   ConnectionRequestRejection: 'transport rejection status is owned by packages/client/connection/src/rpc.ts',
   ConnectionTrustRequest: 'transport authentication input is owned by packages/client/connection/src/rpc.ts',
   ConnectionIndexRequest: 'frontend authentication request is owned by packages/client/connection/src/rpc.ts',
diff --git a/scripts/gen-doc-graphs.ts b/scripts/gen-doc-graphs.ts
index 5b32da720a..c3480ddd41 100644
--- a/scripts/gen-doc-graphs.ts
+++ b/scripts/gen-doc-graphs.ts
@@ -99,6 +99,20 @@ const GROUP_ORDER = [
 ]
 
 const SERVICE_ROLES: ServiceRole[] = [
+  {
+    key: 'networkUsage',
+    pkg: 'network-usage',
+    title: 'Optional traffic ledger',
+    mode: 'core',
+    note: 'Owns retained per-Session traffic totals, rolling-window pacing, and explicit daily spending consent.',
+  },
+  {
+    key: 'networkProxy',
+    pkg: 'network-usage',
+    title: 'Metered network proxy',
+    mode: 'core',
+    note: 'Owns authenticated Host proxy pools and Session-specific Unix sockets for network-confined shell commands.',
+  },
   {
     key: 'hmr',
     pkg: 'hmr',
diff --git a/scripts/verify-package-readme-model-experience.ts b/scripts/verify-package-readme-model-experience.ts
index a3c030bc0b..21bbee9c44 100644
--- a/scripts/verify-package-readme-model-experience.ts
+++ b/scripts/verify-package-readme-model-experience.ts
@@ -84,6 +84,10 @@ const SENTENCE_MODEL_EXPERIENCE: Readonly<Record<string, SentenceContract>> = {
   'packages/experimental/webworker-runtime': { kind: 'none', reason: 'Browser-side host runtime and Node-compatibility layer; the plugins it boots own every model-facing registration.' },
   'packages/experimental/webworker-packer': { kind: 'none', reason: 'Build-time image writer; its output reaches a model only through the tree the worker then boots.' },
   'packages/experimental/inspector': { kind: 'none', reason: 'Developer diagnostics transport; it observes runtime activity without changing model requests.' },
+  'packages/experimental/network-usage': { kind: 'none', reason: 'Traffic accounting and transport admission add no model-facing input.' },
+  'packages/experimental/usage-billing': { kind: 'none', reason: 'The ledger reads reported usage; balance queries do not invoke a model.' },
+  'packages/experimental/usage-dashboard': { kind: 'none', reason: 'Browser layout contributes no model-facing input.' },
+  'packages/experimental/workspace-management': { kind: 'none', reason: 'Authenticated browser deletion adds no model-facing input.' },
   'packages/client/ui-slots': { kind: 'none', reason: 'Browser-side UI plugin layer; registers nothing model-facing.' },
   'packages/client/ui-attachment': { kind: 'none', reason: 'Browser-side UI plugin layer; registers nothing model-facing.' },
   'packages/client/ui-primitives': { kind: 'none', reason: 'Browser-side UI plugin layer; registers nothing model-facing.' },
diff --git a/tsconfig.base.json b/tsconfig.base.json
index 6ec529b39d..386acef425 100644
--- a/tsconfig.base.json
+++ b/tsconfig.base.json
@@ -28,6 +28,22 @@
     // not declaration path aliases, keep each package/vendor source compiled
     // under its own tsconfig boundary.
     "paths": {
+      "@deepseek-ai/dsh-experimental-page-login": ["./packages/experimental/page-login/src"],
+      "@deepseek-ai/dsh-experimental-page-login/client": ["./packages/experimental/page-login/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-personal-instructions": ["./packages/experimental/personal-instructions/src"],
+      "@deepseek-ai/dsh-experimental-personal-instructions/client": ["./packages/experimental/personal-instructions/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-glass-skin": ["./packages/experimental/glass-skin/src"],
+      "@deepseek-ai/dsh-experimental-glass-skin/client": ["./packages/experimental/glass-skin/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-workspace-editor": ["./packages/experimental/workspace-editor/src"],
+      "@deepseek-ai/dsh-experimental-workspace-editor/client": ["./packages/experimental/workspace-editor/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-task-board": ["./packages/experimental/task-board/src"],
+      "@deepseek-ai/dsh-experimental-task-board/client": ["./packages/experimental/task-board/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-interactive-results": ["./packages/experimental/interactive-results/src"],
+      "@deepseek-ai/dsh-experimental-interactive-results/client": ["./packages/experimental/interactive-results/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-mobile-layout": ["./packages/experimental/mobile-layout/src"],
+      "@deepseek-ai/dsh-experimental-mobile-layout/client": ["./packages/experimental/mobile-layout/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-workspace-pages": ["./packages/experimental/workspace-pages/src"],
+      "@deepseek-ai/dsh-experimental-workspace-pages/client": ["./packages/experimental/workspace-pages/src/client/index.ts"],
       "@deepseek-ai/dsh/profile-boot": ["./apps/cli/src/profile-boot.ts"],
       "@deepseek-ai/dsh-browser-use": ["./packages/browser-use/browser-use/src/index.ts"],
       "@deepseek-ai/dsh-browser-use/brand": ["./packages/browser-use/browser-use/src/brand.ts"],
@@ -277,6 +293,18 @@
       "@deepseek-ai/dsh-experimental-webworker-runtime": ["./packages/experimental/webworker-runtime/src"],
       "@deepseek-ai/dsh-experimental-webworker-packer": ["./packages/experimental/webworker-packer/src"],
       "@deepseek-ai/dsh-experimental-inspector": ["./packages/experimental/inspector/src"],
+      "@deepseek-ai/dsh-experimental-usage-billing": ["./packages/experimental/usage-billing/src"],
+      "@deepseek-ai/dsh-experimental-usage-dashboard": ["./packages/experimental/usage-dashboard/src"],
+      "@deepseek-ai/dsh-experimental-usage-dashboard/client": ["./packages/experimental/usage-dashboard/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-usage-billing/client": ["./packages/experimental/usage-billing/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-network-usage": ["./packages/experimental/network-usage/src"],
+      "@deepseek-ai/dsh-experimental-network-usage/proxy": ["./packages/experimental/network-usage/src/proxy-plugin.ts"],
+      "@deepseek-ai/dsh-experimental-network-usage/sandbox": ["./packages/experimental/network-usage/src/sandbox-plugin.ts"],
+      "@deepseek-ai/dsh-experimental-network-usage/client": ["./packages/experimental/network-usage/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-workspace-download": ["./packages/experimental/workspace-download/src"],
+      "@deepseek-ai/dsh-experimental-workspace-download/client": ["./packages/experimental/workspace-download/src/client/index.ts"],
+      "@deepseek-ai/dsh-experimental-workspace-management": ["./packages/experimental/workspace-management/src"],
+      "@deepseek-ai/dsh-experimental-workspace-management/client": ["./packages/experimental/workspace-management/src/client/index.ts"],
       "@deepseek-ai/dsh-experimental-inspector/client": ["./packages/experimental/inspector/src/client/index.ts"],
       "@deepseek-ai/dsh-experimental-ptc-runtime-python": ["./packages/experimental/ptc-runtime-python/src"],
       "@deepseek-ai/dsh-util-crypto": ["./packages/util/crypto/src"],
diff --git a/tsconfig.client.json b/tsconfig.client.json
index b830d85b2f..6bd9bb3b7a 100644
--- a/tsconfig.client.json
+++ b/tsconfig.client.json
@@ -42,6 +42,15 @@
     "packages/client/*/tests/**/*.host.spec.ts"
   ],
   "references": [
+    { "path": "./packages/experimental/glass-skin/tsconfig.client.json" },
+    { "path": "./packages/experimental/page-login/tsconfig.client.json" },
+    { "path": "./packages/experimental/personal-instructions/tsconfig.client.json" },
+    { "path": "./packages/experimental/workspace-editor/tsconfig.client.json" },
+    { "path": "./packages/experimental/task-board/tsconfig.client.json" },
+    { "path": "./packages/experimental/interactive-results/tsconfig.client.json" },
+    { "path": "./packages/experimental/mobile-layout/tsconfig.client.json" },
+    { "path": "./packages/experimental/workspace-pages/tsconfig.client.json" },
+    { "path": "./packages/experimental/usage-dashboard/tsconfig.client.json" },
     { "path": "./packages/client/ui-sidebar-terminal" },
     { "path": "./packages/api/terminal-controller/tsconfig.client.json" },
     // Producer-to-UI spill tests use the ptc-runtime contract with the real tool registry.
@@ -122,6 +131,10 @@
     { "path": "./packages/api/workspace-files/tsconfig.client.json" },
     { "path": "./packages/client/web" },
     { "path": "./packages/context/file-reference" },
-    { "path": "./apps/web" }
+    { "path": "./apps/web" },
+    { "path": "./packages/experimental/usage-billing/tsconfig.client.json" },
+    { "path": "./packages/experimental/network-usage/tsconfig.client.json" },
+    { "path": "./packages/experimental/workspace-download/tsconfig.client.json" },
+    { "path": "./packages/experimental/workspace-management/tsconfig.client.json" }
   ]
 }
diff --git a/tsconfig.host.json b/tsconfig.host.json
index 2b98af6f58..4a81171ee5 100644
--- a/tsconfig.host.json
+++ b/tsconfig.host.json
@@ -159,6 +159,15 @@
     "scripts/client-bundle-purity.spec.ts"
   ],
   "references": [
+    {"path":"./packages/experimental/page-login/tsconfig.host.json"},
+    {"path":"./packages/experimental/personal-instructions/tsconfig.host.json"},
+    { "path": "./packages/experimental/glass-skin/tsconfig.host.json" },
+    { "path": "./packages/experimental/workspace-editor/tsconfig.host.json" },
+    { "path": "./packages/experimental/task-board/tsconfig.host.json" },
+    { "path": "./packages/experimental/interactive-results/tsconfig.host.json" },
+    { "path": "./packages/experimental/mobile-layout/tsconfig.host.json" },
+    { "path": "./packages/experimental/workspace-pages/tsconfig.host.json" },
+    { "path": "./packages/experimental/usage-dashboard/tsconfig.host.json" },
     { "path": "./packages/client/connection/tsconfig.host.json" },
     { "path": "./packages/skill/skill-office" },
     { "path": "./packages/browser-use/browser-use" },
@@ -405,6 +414,10 @@
     { "path": "./packages/lsp/tool-lsp" },
     { "path": "./apps/cli" },
     { "path": "./apps/desktop-host" },
-    { "path": "./apps/desktop" }
+    { "path": "./apps/desktop" },
+    { "path": "./packages/experimental/usage-billing/tsconfig.host.json" },
+    { "path": "./packages/experimental/network-usage/tsconfig.host.json" },
+    { "path": "./packages/experimental/workspace-download/tsconfig.host.json" },
+    { "path": "./packages/experimental/workspace-management/tsconfig.host.json" }
   ]
 }
