import fs from 'node:fs';import path from 'node:path';import{execFileSync}from'node:child_process';import{fileURLToPath}from'node:url';
const base=path.dirname(fileURLToPath(import.meta.url)),target=path.resolve(process.argv[2]||'.');const meta=JSON.parse(fs.readFileSync(base+'/upstream.json'));const head=execFileSync('git',['rev-parse','HEAD'],{cwd:target,encoding:'utf8'}).trim();if(head!==meta.commit)throw Error('Expected upstream commit '+meta.commit);execFileSync('git',['apply','--check',base+'/patches/harness.patch'],{cwd:target,stdio:'inherit'});for(const name of meta.plugins)fs.cpSync(base+'/plugins/'+name,target+'/packages/experimental/'+name,{recursive:true});fs.cpSync(base+'/extras',target,{recursive:true});execFileSync('git',['apply',base+'/patches/harness.patch'],{cwd:target,stdio:'inherit'});console.log('Source installed. Run pnpm install and pnpm run build in the Harness checkout.');
