/** Host entry for an independently removable browser layout plugin. */
import type { Context } from '@deepseek-ai/cordis'
/** @param _ctx - Profile context; browser effects own this feature. */
export function apply(_ctx: Context): void {}
